# 当前推荐系统分析

## 一、餐厅推荐方式

### 1. 推荐流程
```
用户行程 → 计算每天景点中间位置 → 搜索1公里范围内餐厅 → 过滤不合适餐厅 → 按评分排序 → 返回前3个
```

### 2. 核心算法

#### 2.1 中心点计算
- **策略**：取当天景点的中间索引位置
- **实现**：
  - 若只有1个景点，以该景点为中心
  - 若有偶数个景点，取靠前的中间索引（如4个景点取索引1）
  - 若有奇数个景点，取正中间索引（如5个景点取索引2）

#### 2.2 搜索范围
- **默认半径**：1公里（1000米）
- **搜索数量**：最多20个餐厅
- **数据来源**：优先从数据库缓存查询，缓存未命中则调用高德API

#### 2.3 过滤策略
**排除的餐厅类型**：
- 快餐、小吃店、学校食堂、外卖
- 咖啡厅、奶茶、甜品、酒吧、KTV
- 网吧、棋牌、足浴、按摩、SPA
- 美容美发、洗衣、照相、快递
- 停车场、加油站、药店、医院
- 超市、商场、市场、大排档

**保留的餐厅类型**：
- 中餐厅、西餐厅、饭店、酒楼
- 日料、韩料、料理
- 火锅、烧烤、海鲜、自助餐
- 各地方菜系（川菜、湘菜、粤菜等）
- 私房菜、特色菜、家常菜

#### 2.4 排序规则
```
优先级：有评分的餐厅 > 无评分的餐厅
有评分：按评分降序排列
无评分：按距离升序排列
```

#### 2.5 推荐数量
- **固定返回**：每天3个餐厅
- **降级策略**：若过滤后不足3个，放宽过滤条件（仅排除学校、医院等）

### 3. 存在的问题
- ❌ 仅基于位置推荐，未考虑用户偏好
- ❌ 未考虑菜系多样性，可能推荐同类型餐厅
- ❌ 未考虑价格因素
- ❌ 搜索范围固定，可能遗漏优质餐厅
- ❌ 缺乏推荐理由说明

---

## 二、酒店推荐方式

### 1. 推荐流程
```
用户行程+预算 → 计算所有景点地理重心 → 根据预算确定档次 → 搜索5公里范围内酒店 → 档次过滤 → 计算平均距离 → 综合评分排序 → 返回前3个
```

### 2. 核心算法

#### 2.1 地理重心计算
- **策略**：计算所有景点坐标的平均值
- **实现**：
  ```
  重心经度 = Σ(景点经度) / 景点数量
  重心纬度 = Σ(景点纬度) / 景点数量
  ```

#### 2.2 档次确定
根据预算计算每晚预算，确定酒店档次：
```
每晚预算 = 总预算 × 40% ÷ 3晚

经济型：< 200元/晚（如家、7天、汉庭等）
舒适型：200-500元/晚（亚朵、全季、维也纳等）
豪华型：> 500元/晚（希尔顿、万豪、洲际等）
```

#### 2.3 搜索范围
- **默认半径**：5公里（5000米）
- **搜索数量**：最多30个酒店
- **数据来源**：优先从数据库缓存查询，缓存未命中则调用高德API

#### 2.4 档次过滤
根据确定的档次，使用关键词匹配过滤：
- **经济型关键词**：快捷、经济、如家、7天、汉庭、锦江之星等
- **舒适型关键词**：商务、三星、亚朵、全季、维也纳等
- **豪华型关键词**：四星、五星、豪华、希尔顿、万豪等

**降级策略**：若过滤后无结果，返回所有酒店的前10个

#### 2.5 距离计算
计算酒店到各景点的距离：
```
平均距离 = Σ(酒店到各景点距离) / 景点数量
```

#### 2.6 综合评分
```
总分 = 距离评分(60%) + 评分评分(40%)

距离评分 = (1 - (当前距离 - 最小距离) / 距离范围) × 60
评分评分 = (高德评分 / 5) × 40
```

#### 2.7 推荐数量
- **固定返回**：3个酒店
- **降级策略**：若档次过滤后无结果，返回所有酒店

### 3. 存在的问题
- ❌ 仅考虑平均距离，未考虑交通便利性
- ❌ 评分权重固定，无法根据用户偏好调整
- ❌ 档次匹配过于简单，可能遗漏优质酒店
- ❌ 未考虑周边配套设施
- ❌ 推荐数量固定，用户选择有限
- ❌ 缺乏推荐理由说明

---

## 三、对比分析

| 维度 | 餐厅推荐 | 酒店推荐 |
|------|---------|---------|
| **定位策略** | 每天景点中间位置 | 所有景点地理重心 |
| **搜索范围** | 1公里 | 5公里 |
| **过滤维度** | 餐厅类型排除 | 档次关键词匹配 |
| **评分因素** | 评分 + 距离 | 距离(60%) + 评分(40%) |
| **推荐数量** | 每天3个 | 总共3个 |
| **个性化** | ❌ 无 | ❌ 无 |
| **多样性** | ❌ 无 | ❌ 无 |
| **价格考虑** | ❌ 无 | ⚠️ 仅用于档次确定 |

---

## 四、优化建议

### 餐厅推荐优化方向
1. **多因素综合评分**：评分(40%) + 距离(30%) + 菜系匹配(20%) + 价格匹配(10%)
2. **菜系多样性**：确保推荐的3个餐厅菜系不同
3. **智能搜索范围**：根据搜索结果数量动态调整范围
4. **价格区间匹配**：根据用户预算推荐合适价位的餐厅
5. **推荐理由展示**：说明为何推荐该餐厅

### 酒店推荐优化方向
1. **交通便利性评估**：考虑到各景点的距离分布，不仅是平均值
2. **评分权重优化**：距离(40%) + 评分(35%) + 价格匹配(15%) + 档次匹配(10%)
3. **档次智能匹配**：允许跨档次推荐，提供更多选择
4. **推荐数量动态调整**：根据符合条件酒店数量返回3-5个
5. **推荐理由展示**：说明为何推荐该酒店

### 共同优化方向
1. **权重可配置**：通过配置文件调整各因素权重
2. **A/B测试支持**：支持不同推荐策略的对比测试
3. **日志记录**：记录推荐决策过程，便于分析和优化
4. **前端展示优化**：展示评分详情和推荐理由
