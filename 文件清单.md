# LiveTrip 项目文件清单

> 生成时间：2026-04-27
> 项目：LiveTrip - AI + IoT 智能旅行规划系统

---

## 项目根目录

```
LiveTrip/
├── README.md                    # 项目说明文档，包含项目简介、技术栈、快速开始、数据库设计等完整说明
├── .gitignore                   # Git忽略规则，排除node_modules、dist、.env等文件
├── backend/                     # 后端项目
├── frontend/                    # 前端项目
└── 文件清单.md                   # 本文件
```

---

## 一、后端项目（backend/）

### 1.1 配置文件

| 文件 | 说明 |
|------|------|
| `package.json` | 后端依赖与脚本定义，包含Express、Prisma、Socket.io等核心依赖及prisma/dev/lint等脚本 |
| `package-lock.json` | npm依赖锁定文件，确保依赖版本一致性 |
| `.env` | 后端环境变量配置（含API密钥、数据库连接等，不提交到Git） |
| `.env.example` | 环境变量配置示例，包含DATABASE_URL、JWT_SECRET、智谱AI/高德地图/Cloudinary/OpenWeatherMap等配置项模板 |
| `.eslintrc.cjs` | ESLint代码规范配置 |
| `.prettierrc` | Prettier代码格式化配置 |
| `tsconfig.json` | TypeScript编译配置 |

### 1.2 入口文件

| 文件 | 说明 |
|------|------|
| `src/index.ts` | 后端主入口文件，配置Express服务器、CORS、Helmet安全中间件、Socket.io、定时任务调度器，挂载API路由和错误处理 |

### 1.3 配置模块（src/config/）

| 文件 | 说明 |
|------|------|
| `cloudinary.ts` | Cloudinary图片存储配置，从环境变量读取cloud_name/api_key/api_secret并初始化，含配置完整性校验 |

### 1.4 工具库（src/lib/）

| 文件 | 说明 |
|------|------|
| `prisma.ts` | PrismaClient单例工厂，避免多个数据库连接导致SQLite锁定问题，全局复用同一实例 |

### 1.5 中间件（src/middleware/）

| 文件 | 说明 |
|------|------|
| `adminAuthMiddleware.ts` | 管理员权限验证中间件，提供requireAdmin（强制管理员）、optionalAdmin（可选验证）、requireRole（角色验证）、requireOwnershipOrAdmin（资源所有权或管理员）四种权限校验 |
| `errorHandler.ts` | 统一错误处理中间件，处理运行时错误（含开发环境堆栈输出）和404路由未找到 |
| `fileUploadMiddleware.ts` | 文件上传中间件，基于Multer实现内存存储，支持单张/多张图片上传，限制20MB大小，仅允许JPG/PNG/WEBP格式 |

### 1.6 控制器层（src/controllers/）

| 文件 | 说明 |
|------|------|
| `planController.ts` | 行程规划控制器，处理行程计划创建、获取行程详情和行程调整的HTTP请求 |
| `tripController.ts` | 行程管理控制器，处理行程的增删改查、预算计算、酒店/餐厅更新、行程完成等操作 |
| `authController.ts` | 用户认证控制器，处理用户注册、登录、JWT令牌生成与验证、获取当前用户信息 |
| `iotController.ts` | IoT数据控制器，处理IoT实时数据（人群密度、天气、降雨概率等）的查询请求 |
| `destinationController.ts` | 热门目的地控制器，提供热门城市列表和城市景点数据查询接口 |
| `spotSyncController.ts` | 景点同步控制器，处理从前端同步单个/批量景点数据到数据库 |
| `favoriteController.ts` | 收藏控制器，处理收藏的增删查及收藏计数等API请求 |
| `shareController.ts` | 分享控制器，处理行程分享链接生成、获取公开行程、克隆分享行程的HTTP请求 |
| `blogController.ts` | Blog控制器，处理博客文章的创建、查询、更新、删除、点赞、评论等API请求 |
| `imageController.ts` | 图片控制器，处理景点图片上传（含Cloudinary云存储）、批量查询和审核相关API请求 |
| `adminController.ts` | 管理员控制器，处理管理员仪表板统计、景点配图状态、图片审批等后台管理请求 |
| `packingController.ts` | 打包清单控制器，处理行程打包清单的增删改查、批量保存、初始化、进度查询等API请求 |
| `agentController.ts` | Agent控制器，处理AI Agent对话（支持智谱AI Function Calling）请求 |
| `agentSSEController.ts` | Agent SSE控制器，支持Server-Sent Events流式响应，将Agent处理步骤实时推送到前端 |
| `collabController.ts` | 协同规划控制器，处理协同房间创建/加入/详情、草稿提交、消息发送等协同规划HTTP请求 |
| `userController.ts` | 用户信息控制器，处理用户个人信息及统计数据（行程数/城市数等）的CRUD操作 |
| `advisorController.ts` | 旅行顾问控制器，处理AI旅行顾问聊天及会话管理相关请求 |
| `recommendationController.ts` | 推荐控制器，处理酒店和餐厅推荐及自定义搜索请求 |
| `locationCacheController.ts` | 地点缓存控制器，处理带缓存的地点搜索、热门地点查询和缓存清理请求 |
| `hotSpotController.ts` | 热门景点控制器，处理热门景点和热门城市的查询请求 |
| `budgetController.ts` | 预算控制器，提供预算调整、项目价格更新、预算历史查询和实时预算状态等API接口 |
| `notificationController.ts` | 通知控制器，处理通知列表获取、标记已读/全部已读、删除及传感器触发等通知相关请求 |
| `uploadController.ts` | 图片上传控制器，处理图片上传到Cloudinary及图片删除请求 |
| `testNotificationController.ts` | 测试通知控制器，用于开发测试IoT通知功能的发送效果 |

### 1.7 路由层（src/routes/）

| 文件 | 说明 |
|------|------|
| `index.ts` | API路由聚合文件，统一挂载所有子路由到/api前缀，并定义打包清单独立路由 |
| `planRoutes.ts` | 行程规划路由，定义POST /plan（创建行程）、GET /itinerary/:id（获取行程）、POST /adjust（调整行程）端点 |
| `tripRoutes.ts` | 行程管理路由，定义行程增删改查、预算计算/调整、酒店/餐厅更新、分享、完成等API端点 |
| `authRoutes.ts` | 用户认证路由，定义POST /register（注册）、POST /login（登录）、GET /me（当前用户）、PUT /profile（更新资料）端点 |
| `iotRoutes.ts` | IoT数据路由，定义GET /iot/data（全部景点IoT数据）和GET /iot/spot/:id（指定景点IoT数据）端点 |
| `destinationRoutes.ts` | 热门目的地路由，定义获取热门城市列表、城市热门景点、城市全部景点等API端点 |
| `spotRoutes.ts` | 景点路由，定义获取城市景点列表、景点搜索、备选景点查询等API端点 |
| `spotSyncRoutes.ts` | 景点同步路由，定义POST /sync（单个同步）和POST /sync/batch（批量同步）端点 |
| `favoriteRoutes.ts` | 收藏路由，定义收藏列表、添加、删除、检查和计数等API端点 |
| `shareRoutes.ts` | 分享路由，定义行程分享、获取分享行程和克隆分享行程的API端点 |
| `blogRoutes.ts` | Blog路由，定义博客文章创建、列表、详情、更新、删除、点赞、评论、标签等API端点 |
| `imageRoutes.ts` | 图片路由，定义博客图片上传、景点图片上传、批量查询等API端点 |
| `adminRoutes.ts` | 管理员路由，定义仪表板统计、景点配图状态、图片审批等后台管理API端点（需管理员权限） |
| `agentRoutes.ts` | Agent路由，定义POST /chat（普通对话）和POST /chat/stream（SSE流式对话）端点 |
| `collabRoutes.ts` | 协同规划路由，定义房间创建/加入/详情、草案管理、消息发送、行程保存等API端点 |
| `userRoutes.ts` | 用户路由，定义用户信息获取/更新和头像上传等API端点 |
| `advisorRoutes.ts` | 旅行顾问路由，定义AI顾问聊天、会话列表、会话消息获取和删除等API端点 |
| `recommendationRoutes.ts` | 推荐路由，定义酒店推荐、餐厅推荐、自定义搜索等API端点 |
| `locationCacheRoutes.ts` | 地点缓存路由，定义地点搜索、热门地点、缓存清理的API端点 |
| `hotSpotRoutes.ts` | 热门景点路由，定义热门景点、热门城市及含景点信息的热门城市等API端点 |
| `chatHistoryRoutes.ts` | 对话历史路由，定义用户对话历史列表和会话消息查询等API端点 |
| `notificationRoutes.ts` | 通知路由，定义通知列表、标记已读/全部已读、删除、传感器触发及测试通知等API端点 |
| `uploadRoutes.ts` | 图片上传路由，定义图片上传和图片删除的API端点，使用multer处理文件上传 |

### 1.8 服务层（src/services/）

| 文件 | 说明 |
|------|------|
| `scoringEngine.ts` | 多因素评分引擎，综合偏好匹配（Jaccard相似度）、质量评分、IoT评分、人群适配度等多维度计算景点综合评分 |
| `clusteringService.ts` | K-means地理聚类服务，按经纬度将景点分组聚类，减少每日交通时间 |
| `diversityService.ts` | 多样性约束服务，通过贪心算法确保每天景点类型多样（同类型≤2个、≤50%总量、≥2种类型） |
| `routeOptimizer.ts` | 路径优化服务，贪心算法生成初始路线+2-opt算法局部优化，考虑景点类型与体力消耗优化游览顺序 |
| `budgetOptimizer.ts` | 预算优化服务，根据城市等级和季节性系数动态计算预算分配，生成预算优化建议 |
| `budgetCalculator.ts` | 预算计算服务，根据实际选择的酒店/餐厅/景点等计算真实预算明细和各分类占比 |
| `budgetTrackingService.ts` | 预算追踪服务，记录预算变更历史、调整预算金额、查询变更记录，支持实时预算监控 |
| `iotCheckService.ts` | IoT实时检查服务，根据IoT数据（人流拥挤/天气恶劣/景点关闭）实时检测并替换问题景点 |
| `iotNotificationService.ts` | IoT通知增强服务，多维度通知触发机制（临界状态预警、趋势预警、收藏景点状态变化等） |
| `sensorScheduler.ts` | 定时任务调度器（node-cron），定时执行环境感知任务：IoT数据更新、天气数据采集、人流模拟、环境通知触发 |
| `agentService.ts` | Agent服务，实现智谱AI Function Calling功能，支持自然语言创建行程和二次确认流程 |
| `traditionalRecommender.ts` | 传统推荐服务，使用多因素评分引擎+聚类+多样性约束+路径优化的完整流水线进行行程推荐 |
| `itineraryAdjustService.ts` | 行程调整服务，根据IoT数据和调整原因（人流拥挤/天气恶劣/景点关闭）动态替换景点并更新备选池 |
| `weatherService.ts` | 真实天气数据服务，调用OpenWeatherMap API获取天气并叠加微气候调整，支持批量查询 |
| `notificationService.ts` | 推送通知服务，通过Socket.io WebSocket和站内Notification表向用户发送环境感知通知 |
| `collabService.ts` | 协同规划服务，处理协同房间创建/加入、成员管理、草案提交/合并、消息发送和最终行程保存 |
| `blogService.ts` | Blog服务，处理博客文章的创建、查询、更新、删除、点赞/取消、评论管理和标签统计 |
| `shareService.ts` | 分享服务，生成唯一分享Token、创建公开行程只读视图、支持克隆分享行程为用户自有行程 |
| `packingService.ts` | 打包清单服务，提供预设物品分类和默认物品列表，处理打包清单的增删改查业务逻辑 |
| `spotService.ts` | 景点数据服务，整合高德API和数据库缓存，提供景点与IoT数据的统一查询访问接口 |
| `spotDataService.ts` | 景点数据查询服务，为AI Agent提供景点和IoT数据的结构化查询接口（Function Calling数据源） |
| `imageService.ts` | 图片服务，处理景点图片的上传（含去重、SHA256哈希校验）、Cloudinary存储和审核流程 |
| `cloudinaryService.ts` | Cloudinary服务封装，提供图片上传、删除、URL优化等Cloudinary API操作 |
| `amapService.ts` | 高德地图API服务封装，提供POI搜索、地理编码、逆地理编码、路径规划等高德地图接口调用 |
| `amapPOICacheService.ts` | 高德地图POI缓存服务，永久缓存高德景点数据到AmapPOICache表，减少API调用并提升响应速度 |
| `locationCacheService.ts` | 地点缓存服务，缓存用户搜索过的地点到LocationCache表，支持搜索计数和过期管理 |
| `hotelRecommender.ts` | 酒店推荐服务，基于行程景点位置和用户预算，调用高德POI搜索推荐附近酒店 |
| `hotelCacheService.ts` | 酒店缓存服务，永久缓存高德地图的酒店数据到Hotel表，避免重复API调用 |
| `restaurantRecommender.ts` | 餐厅推荐服务，基于每天行程景点位置，调用高德POI搜索推荐附近午餐餐厅 |
| `restaurantCacheService.ts` | 餐厅缓存服务，永久缓存高德地图的餐厅数据到Restaurant表，避免重复API调用 |
| `crowdSimulator.ts` | 人流模拟服务，基于时段（早/午/晚/夜）、日期（工作日/周末/节假日）和景点热度系数模拟人流密度 |
| `microclimateService.ts` | 微气候调整服务，基于景点特征（山区降温/湖泊增湿/海边风力等）对天气数据进行精细化调整 |
| `userProfileService.ts` | 用户画像服务，从数据库中提取用户历史行程和偏好信息，为AI对话提供个性化上下文 |
| `advisorService.ts` | 旅行顾问服务，使用智谱AI ChatGLM提供旅行规划问答建议，支持多轮对话和会话管理 |
| `aiTripRecommender.ts` | AI推荐服务，使用智谱AI推荐行程（用于AI对话模式中的行程创建），调用评分引擎进行后续优化 |
| `alternativeRecommender.ts` | 备选景点推荐服务，基于数据库SpotAlternative关系、IoT数据、相似度和距离等因素推荐备选替代景点 |
| `constraintAwarePlanner.ts` | 约束感知行程规划服务，支持必选景点（用户指定）和地理合理性校验，确保行程满足用户硬性需求 |
| `mustVisitSpotExtractor.ts` | 必选景点提取服务，从用户自然语言输入中识别景点名称并匹配数据库中具体景点 |
| `chatHistoryService.ts` | 对话历史服务，管理AI对话会话（ChatSession）和消息（ChatMessage），支持草稿状态和二次确认流程 |
| `favoriteService.ts` | 收藏服务，处理景点收藏/取消收藏/查询收藏列表/检查收藏状态等业务逻辑 |

### 1.9 Socket.io实时通信（src/socket/）

| 文件 | 说明 |
|------|------|
| `socketService.ts` | Socket.io服务器初始化与事件处理，JWT认证中间件，处理协同房间加入/离开、光标移动、草案更新/提交、房间锁定、消息发送等实时事件，维护用户Socket映射 |

### 1.10 类型定义（src/types/）

| 文件 | 说明 |
|------|------|
| `index.ts` | 后端核心TypeScript类型定义，包含CategoryTag（景点类别枚举）、GroupType（群体类型）、Pace/EnergyLevel（节奏/体力）、UserPreferences、CreatePlanRequest、FullItinerary、SpotScore、SpotCluster、AlternativeSpot等核心接口 |

### 1.11 工具函数（src/utils/）

| 文件 | 说明 |
|------|------|
| `apiRateLimiter.ts` | API速率限制器，控制高德地图API的并发请求数和最小请求间隔，防止超过API调用频率限制 |
| `dateParser.ts` | 日期解析工具，支持绝对日期（YYYY-MM-DD）和相对日期（今天/明天/后天/下周X等中文自然语言）解析 |
| `hashGenerator.ts` | 文件哈希生成工具，基于crypto SHA256生成文件哈希值，用于图片去重校验 |
| `imageValidator.ts` | 图片验证工具，校验图片MIME类型（JPG/PNG/WEBP）、文件大小和哈希去重 |
| `retry.ts` | 重试工具，指数退避重试机制，用于网络请求失败时自动重试 |
| `spotDeduplication.ts` | 景点去重工具，基于Levenshtein编辑距离计算名称相似度，过滤重复景点 |
| `tokenGenerator.ts` | Token生成工具，基于crypto生成安全的随机分享Token和邀请Token |

### 1.12 静态数据（src/data/）

| 文件 | 说明 |
|------|------|
| `alternativeSpots.ts` | 后端备选景点静态数据，用于行程调整时的替换推荐（作为数据库查询的补充数据源） |

### 1.13 数据库（prisma/）

| 文件 | 说明 |
|------|------|
| `schema.prisma` | Prisma数据库模型定义，包含User、Trip、Day、ItineraryItem、Spot、SpotIoTData、SpotImage、SpotAlternative、Favorite、Budget、BudgetRecord、BlogPost、BlogComment、BlogLike、BlogCommentLike、Hotel、Restaurant、ChatSession、ChatMessage、CollabRoom、TripMember、DraftRoute、CollabMessage、Notification、EnvironmentSensorLog、AmapPOICache、LocationCache、DestinationCache、PackingItem等29个模型 |
| `seed.ts` | 数据库种子数据脚本，创建管理员账号等初始数据 |
| `dev.db` | SQLite开发数据库文件 |
| `dev.db.backup_current` | SQLite数据库当前备份文件 |

#### 数据库迁移（prisma/migrations/）

| 目录 | 说明 |
|------|------|
| `20260202222751_init/` | 初始化迁移，创建User、Trip、Day、ItineraryItem等基础表 |
| `20260205140338_add_amap_poi_cache/` | 新增AmapPOICache高德POI缓存表 |
| `20260206164540_simplify_schema/` | 简化数据库Schema结构调整 |
| `20260206173553_add_location_cache/` | 新增LocationCache地点缓存表 |
| `20260207184505_add_spot_and_iot_tables/` | 新增Spot景点表和SpotIoTData IoT数据表 |
| `20260207204723_add_spot_alternatives_table/` | 新增SpotAlternative备选景点关系表 |
| `20260208133400_add_favorites_table/` | 新增Favorite收藏表 |
| `20260209124128_add_destination_cache/` | 新增DestinationCache目的地缓存表 |
| `20260302114213_add_hotel_fields/` | 为Trip表新增酒店相关字段 |
| `20260302133543_add_restaurant_fields/` | 为Day表新增餐厅相关字段 |
| `20260304025538_add_budget_tracking/` | 新增Budget预算表和预算追踪相关字段 |
| `20260306013252_add_share_fields/` | 为Trip表新增分享相关字段（shareToken、isPublic） |
| `20260308093802_add_recommendations_cache/` | 为Trip和Day表新增推荐缓存字段 |
| `20260310024620_add_image_support/` | 新增SpotImage景点图片表 |
| `20260310035603_add_blog_tables/` | 新增BlogPost和BlogComment博客表 |
| `20260310112858_add_review_like/` | 新增评价点赞功能字段 |
| `20260311072520_add_blog_comment_like/` | 新增BlogCommentLike评论点赞表 |
| `20260313085107_add_image_management/` | 图片管理增强，新增审核状态/哈希/计数等字段 |
| `20260313090921_add_completed_at_field/` | 为Trip表新增completedAt完成时间字段 |
| `20260314112947_add_spotid_and_ishot/` | 为Spot表新增spotId和isHot热门标记字段 |
| `20260314113545_add_spotid_to_itinerary_item/` | 为ItineraryItem表新增spotId关联字段 |
| `20260316063917_add_hotel_restaurant_tables/` | 新增Hotel酒店表和Restaurant餐厅表 |
| `20260324171133_add_packing_list/` | 新增PackingItem打包清单表 |
| `20260326052854_add_weather_fields/` | 为SpotIoTData表新增天气描述和图标字段 |
| `20260326224655_add_chat_tables/` | 新增ChatSession和ChatMessage对话表（第一次） |
| `20260326225037_add_chat_tables/` | 新增ChatSession和ChatMessage对话表（修正） |
| `20260403023842_add_collab_tables/` | 新增CollabRoom、TripMember、DraftRoute、CollabMessage协同规划表 |
| `20260403133603_add_trip_source/` | 为Trip表新增source来源字段 |
| `20260411105504_add_cover_image_field/` | 为BlogPost表新增coverImage封面图字段 |
| `20260413070319_add_user_profile_fields/` | 为User表新增nickname/gender/bio等个人资料字段 |
| `20260414073318_livetrip/` | 综合迁移调整 |
| `20260415103743_iot/` | IoT相关迁移调整 |
| `20260421112420_add_blog_author_and_favorite/` | 新增BlogLike博客点赞表和收藏功能增强 |

> 每个迁移目录下包含 `migration.sql` 文件，记录该次迁移的SQL语句。

| 文件 | 说明 |
|------|------|
| `migration_lock.toml` | Prisma迁移锁文件，确保迁移不会并发执行 |

### 1.14 工具脚本（scripts/）

| 文件 | 说明 |
|------|------|
| `backup-db.ts` | 数据库备份脚本，将当前SQLite数据库导出为JSON格式，便于版本控制和恢复 |
| `restore-db.ts` | 数据库恢复脚本，从JSON备份文件恢复数据库数据 |
| `createAdmin.ts` | 创建管理员账号脚本，交互式创建具有admin角色的用户 |
| `check-stats.ts` | 数据库统计检查脚本，查询并输出景点数、用户数等统计数据 |

---

## 二、前端项目（frontend/）

### 2.1 配置文件

| 文件 | 说明 |
|------|------|
| `package.json` | 前端依赖与脚本定义，包含React、Ant Design、Zustand、TipTap等核心依赖及dev/build/lint/type-check等脚本 |
| `package-lock.json` | npm依赖锁定文件，确保依赖版本一致性 |
| `.env` | 前端环境变量配置（含API地址、高德地图Key等，不提交到Git） |
| `.env.example` | 环境变量配置示例，包含VITE_AMAP_WS_KEY/JS_KEY/JS_SECRET和VITE_API_BASE_URL等配置项模板 |
| `.eslintrc.cjs` | ESLint代码规范配置，含React/React Hooks插件 |
| `.prettierrc` | Prettier代码格式化配置 |
| `tsconfig.json` | TypeScript项目引用配置 |
| `tsconfig.app.json` | 应用代码TypeScript编译配置 |
| `tsconfig.node.json` | Node工具代码TypeScript编译配置 |
| `vite.config.ts` | Vite构建配置，定义开发服务器代理和构建选项 |
| `tailwind.config.js` | Tailwind CSS配置，定义品牌色（松绿色#145F39）、毛玻璃效果、动画等自定义主题 |
| `postcss.config.cjs` | PostCSS配置，集成Tailwind CSS和Autoprefixer |
| `components.json` | shadcn/ui组件配置，定义组件样式别名和路径 |
| `index.html` | 应用HTML入口，定义页面标题和根节点 |
| `.gitignore` | 前端Git忽略规则 |

### 2.2 入口文件

| 文件 | 说明 |
|------|------|
| `src/main.tsx` | 前端主入口，创建React根节点并渲染App组件（StrictMode包裹） |
| `src/App.tsx` | 应用主组件，配置React Router路由，包含首页/规划/行程/博客/收藏/协同/管理员等所有页面路由，使用AuthGuard和AdminGuard进行路由保护 |
| `src/index.css` | 全局样式文件，引入Tailwind CSS指令和自定义CSS变量/毛玻璃效果/滚动条样式等 |
| `src/vite-env.d.ts` | Vite环境类型声明 |

### 2.3 API层（src/api/）

| 文件 | 说明 |
|------|------|
| `client.ts` | API客户端核心，创建Axios实例并配置请求/响应拦截器（JWT Token注入、userId注入），定义所有核心类型接口（AttractionItem/DailyItinerary/FullItinerary/PlanRequest/IoTSpotData/AlternativeSpot等），封装行程规划/管理/IoT/收藏/分享/评价/Blog等所有主要API函数 |
| `adminApi.ts` | 管理员API客户端，封装景点管理、图片审核、图片上传等后台管理接口 |
| `collabApi.ts` | 协同规划API客户端，封装房间CRUD、草案管理、消息发送、投票、最终行程保存等协同规划接口 |
| `recommendationApi.ts` | 酒店和餐厅推荐API，封装基于高德POI的酒店推荐和餐厅推荐接口 |
| `notification.ts` | 通知API服务，获取通知列表、标记已读/全部已读、删除通知、获取未读数等接口 |

### 2.4 配置模块（src/config/）

| 文件 | 说明 |
|------|------|
| `api.ts` | API配置文件，统一管理API基础地址（VITE_API_BASE_URL）和Socket.io连接地址 |
| `presetItems.ts` | 预设物品清单配置，定义证件/衣物/洗漱/药品/电子/其他等分类及各分类下的默认物品项 |

### 2.5 状态管理（src/store/）

| 文件 | 说明 |
|------|------|
| `index.ts` | 应用全局状态管理（Zustand），管理currentItinerary（当前行程）、user（当前用户）、isLoading（加载状态）、completeTrip（完成行程）等核心状态 |
| `collabStore.ts` | 协同规划状态管理（Zustand），管理协同房间信息、成员列表、草案数据、聊天消息等协同规划全局状态 |

### 2.6 服务层（src/services/）

| 文件 | 说明 |
|------|------|
| `aiService.ts` | AI对话统一服务，封装advisor（旅行顾问）和agent（AI Agent）两种模式的会话管理和消息收发 |
| `alternativeRecommender.ts` | 备选景点推荐服务，基于IoT数据/相似度/距离等因素推荐备选景点（前端侧逻辑） |
| `collabSocket.ts` | Socket.io客户端服务，处理协同规划的实时通信（连接/断开、加入/离开房间、草案同步、消息收发） |

### 2.7 自定义Hooks（src/hooks/）

| 文件 | 说明 |
|------|------|
| `useCollabMap.ts` | 协同规划地图Hook，封装高德地图的初始化/标记添加/路线绘制/图层切换等操作 |
| `useHomepageData.ts` | 首页数据管理Hook，统一管理首页所有数据获取（用户行程/打包清单/天气数据/IoT数据等） |
| `useSocket.ts` | Socket.io连接Hook，提供WebSocket连接实例和自动重连机制 |

### 2.8 静态数据（src/data/）

| 文件 | 说明 |
|------|------|
| `destinationsData.ts` | 目的地及景点静态数据，定义北京/上海/厦门/成都/杭州/西安/武汉/三亚/丽江等城市的景点详细信息 |
| `popularDestinations.ts` | 热门目的地数据，城市级别的推荐列表（名称/描述/封面图/景点数等） |
| `alternativeSpots.ts` | 备选景点静态数据（已废弃，改用API动态获取），保留仅作参考 |

### 2.9 类型定义（src/types/）

| 文件 | 说明 |
|------|------|
| `admin.ts` | 管理员模块类型定义，包含SpotListItem（景点列表项）、ImageInfo（图片信息）、ReviewStatus（审核状态）等接口 |
| `destination.ts` | 目的地相关类型定义，包含Spot（景点）、DestinationDetail（目的地详情）、Tag（标签）等接口 |

### 2.10 工具函数（src/utils/）

| 文件 | 说明 |
|------|------|
| `auth.ts` | 认证工具函数，从localStorage获取用户ID和Token，提供认证状态判断 |
| `blogContentUtils.ts` | 博客内容处理工具，从HTML中提取图片URL、计算字数和预估阅读时长 |
| `cacheManager.ts` | 缓存管理器，内存级缓存实现，支持TTL过期策略和按键管理/清理 |
| `exportPDF.ts` | PDF导出工具，将Markdown博客内容渲染为HTML后转换为PDF文件下载 |
| `mapScreenshot.ts` | 地图截图工具，使用高德JS API和html2canvas生成行程地图截图 |

### 2.11 工具库（src/lib/）

| 文件 | 说明 |
|------|------|
| `utils.ts` | Tailwind CSS工具函数，提供cn函数（基于clsx + tailwind-merge合并类名） |

### 2.12 页面组件（src/pages/）

| 文件 | 说明 |
|------|------|
| `Home.tsx` | 智能旅行规划首页，集成毛玻璃卡片组件（天气/预算/日历/地图/打包清单/即将出行/搜索栏），展示用户个性化首页 |
| `Plan.tsx` | 创建行程页面，分步骤引导用户选择出发地/目的地、日期范围、预算、群体类型，支持AI顾问辅助和图片裁剪 |
| `Itinerary.tsx` | 行程规划编辑页面，集成时间轴卡片、步骤导航、全屏地图、预算条等组件，支持拖拽排序景点 |
| `TripDetail.tsx` | 行程详情页面，展示完整行程信息，支持备选景点替换、行程分享、景点图片上传、打包清单管理 |
| `MyTrips.tsx` | 我的行程列表页面，支持行程搜索/删除/分享，展示预算详情弹窗和行程状态 |
| `Auth.tsx` | 登录/注册页面，支持登录和注册表单切换，含表单验证 |
| `Profile.tsx` | 个人信息编辑页面，支持修改用户名/邮箱/头像/昵称/性别/简介等 |
| `DestinationDetail.tsx` | 目的地详情页面，展示景点信息/评分/评论，支持收藏和分类标签筛选 |
| `Favorites.tsx` | 我的收藏页面，展示收藏的景点列表，支持取消收藏 |
| `Destinations.tsx` | 热门目的地页面，展示城市列表和景点卡片，支持分类标签筛选 |
| `SharedTrip.tsx` | 公开行程只读页面，用于展示分享的行程，复用TripDetail的UI组件 |
| `Blogs.tsx` | 旅行博客列表页面，展示博客卡片列表，支持搜索/排序/删除 |
| `BlogCreate.tsx` | 博客创建/编辑页面，封装BlogEditor组件，检查登录状态 |
| `BlogDetail.tsx` | 博客详情页面，展示博客内容/评论/点赞，支持导出PDF |
| `AIFeatures.tsx` | AI功能对话页面，支持AI旅行顾问和Agent模式的对话交互，含快捷提问入口 |
| `Today.tsx` | 当前行程页面（三栏布局），展示景点IoT实时数据、打包清单编辑、地图和预算信息 |
| `CollabEntry.tsx` | 协同规划入口页面，提供创建房间和加入房间的操作入口 |

#### 协同规划页面（src/pages/collab/）

| 文件 | 说明 |
|------|------|
| `CollabSetup.tsx` | 协同规划设置页面，填写房间名/目的地/日期/群体等信息后创建协同房间 |
| `JoinCollabRoom.tsx` | 加入协同房间页面，通过邀请链接或Token加入协同规划 |
| `CollabRoom.tsx` | 协同编辑主页面，多人协同规划行程的核心界面，集成地图/聊天/草案编辑/图层控制/景点统计 |

#### 管理员页面（src/pages/admin/）

| 文件 | 说明 |
|------|------|
| `SpotManagePage.tsx` | 管理员景点图片管理页面，支持景点搜索/图片上传/删除/查看 |
| `ReviewPage.tsx` | 管理员图片审核页面，审核待处理的景点图片（通过/驳回） |

### 2.13 组件模块（src/components/）

#### 通用组件（src/components/common/）

| 文件 | 说明 |
|------|------|
| `LandingHeroSection.tsx` | 着陆页英雄区域组件，视频背景+滚动视差效果+模糊文字动画，营造沉浸式首屏体验 |
| `BlurText.tsx` | 模糊文字动画组件，逐词从模糊到清晰的渐变动画效果 |
| `EmptyState.tsx` | 空状态提示组件，用于博客/收藏/草稿等列表为空时的友好提示显示 |
| `SettingsModal.tsx` | 设置弹窗组件，支持更换/移除背景图片和退出登录 |
| `ShareButton.tsx` | 分享按钮组件，生成行程分享链接和二维码 |

#### 认证组件（src/components/auth/）

| 文件 | 说明 |
|------|------|
| `AuthGuard.tsx` | 路由守卫组件，保护需要登录的路由，未登录时跳转到登录页 |

#### 管理员组件（src/components/admin/）

| 文件 | 说明 |
|------|------|
| `AdminGuard.tsx` | 管理员路由守卫组件，仅允许admin角色用户访问 |
| `AdminSidebar.tsx` | 管理员侧边栏组件，提供管理后台导航（图片管理/审核） |

#### AI组件（src/components/ai/）

| 文件 | 说明 |
|------|------|
| `AIAdvisor.tsx` | AI旅行顾问组件，支持advisor/agent两种对话模式，提供快捷提问入口和对话界面 |

#### 输入组件（src/components/input/）

| 文件 | 说明 |
|------|------|
| `DoubleCalendar.tsx` | 双月日历组件，并排显示两个月日历，支持选择起止日期范围 |

#### 行程组件（src/components/itinerary/）

| 文件 | 说明 |
|------|------|
| `SpotCardV2.tsx` | 改进版景点卡片组件，更大尺寸、高对比度设计，支持展开Read more和IoT实时数据展示 |
| `DayMap.tsx` | 当日地图组件，基于高德地图，支持动态标记/酒店/餐厅/信息卡片点击交互 |
| `FullscreenMap.tsx` | 可全屏地图组件，支持原地展开/收起（上下左右拉伸），复用DayMap |
| `ActionButton.tsx` | 操作按钮组件，固定右下角，包含上一步/下一步/保存行程按钮 |
| `LinearStepNavigation.tsx` | 线性步骤导航组件，按天交替展示：景点→餐厅→酒店→打包 |
| `PackingStep.tsx` | 行李打包步骤组件，支持添加/编辑/删除/勾选打包物品，含预设分类选择 |
| `PackingListWidget.tsx` | 打包清单小组件，可嵌入页面的精简版打包清单管理 |
| `BudgetBar.tsx` | 预算分布组件，以进度条展示各分类预算占比和已用/总预算情况 |
| `Timeline.tsx` | 动态时间轴组件，时间段横向显示，与景点卡片高度对齐 |
| `TimelineWithCards.tsx` | 时间轴+景点卡片整体组件，支持拖拽排序和只读模式展示 |
| `CompactAlternativeSpotCard.tsx` | 紧凑版备选景点卡片，与SpotCardV2样式一致但尺寸更小，用于备选列表展示 |
| `InlineAlternativeAttractions.tsx` | 内联备选景点组件，展开在景点卡片下方，支持一键替换操作 |

#### 首页组件（src/components/home/）

| 文件 | 说明 |
|------|------|
| `BudgetCard.tsx` | 预算卡片组件（毛玻璃风格），展示各分类预算项和占比 |
| `CalendarCard.tsx` | 月历卡片组件（毛玻璃风格），标注行程日期 |
| `GlassCard.tsx` | 毛玻璃卡片组件，通用的玻璃拟态效果容器（backdrop-blur），项目核心视觉组件 |
| `SearchBar.tsx` | 搜索栏组件，搜索热门目的地/行程/博客，支持热门城市快捷入口 |
| `WeatherCard.tsx` | 天气卡片组件（毛玻璃风格），展示温度/湿度/风速/气压等天气信息 |
| `MapWidget.tsx` | 地图控件组件，展示中国地图和用户足迹城市标记 |
| `PackingList.tsx` | 打包清单组件，展示行李物品列表和勾选状态 |
| `UpcomingTourCard.tsx` | 即将出行卡片组件（毛玻璃风格），展示即将到来的行程概览 |
| `LogoutButton.tsx` | 退出登录按钮组件，在侧边栏左下角显示 |
| `index.ts` | 首页组件统一导出文件 |

#### 首页图片资源（src/components/home/image/）

| 文件 | 说明 |
|------|------|
| `PackingList/1775651205434.png` | 打包清单组件使用的示例/占位图片 |

#### 布局组件（src/components/layout/）

| 文件 | 说明 |
|------|------|
| `GlassLayout.tsx` | 沉浸式毛玻璃布局组件，所有页面的统一外层布局容器，提供背景图和毛玻璃效果 |
| `GlobalSidebar.tsx` | 全局侧边栏组件，统一所有页面的导航菜单，支持隐藏/显示切换 |
| `SharedSidebar.tsx` | 统一侧边栏组件，确保各页面显示完整的导航链接 |

#### 博客组件（src/components/blog/）

| 文件 | 说明 |
|------|------|
| `BlogList.tsx` | 博客列表组件，支持分页/排序/点赞/删除，基于Ant Design |
| `BlogDetail.tsx` | 博客详情组件，展示内容/评论/点赞，支持浏览量统计 |
| `BlogEditor.tsx` | 博客编辑器组件，基于TipTap富文本编辑器，两列布局（编辑区+配置面板） |
| `BlogSkeleton.tsx` | 博客骨架屏加载组件，用于博客列表加载状态的占位显示 |

#### 预算组件（src/components/budget/）

| 文件 | 说明 |
|------|------|
| `BudgetDetailModal.tsx` | 预算详情弹窗，展示预算使用情况/分类明细，支持调整预算 |
| `BudgetWidget.tsx` | 统一预算管理小组件，显示预算状态并提供记账和调整入口 |
| `ExpenseRecordModal.tsx` | 开支记录弹窗，记账本式的开支记录功能，支持添加/查看历史记录 |
| `SimpleBudgetAdjustModal.tsx` | 简单预算调整弹窗，仅调整总预算金额 |

#### 协同组件（src/components/collab/）

| 文件 | 说明 |
|------|------|
| `DayRoutePlanner.tsx` | 按天路线规划组件，支持添加/删除/排序景点，展示景点统计信息 |
| `LayerControl.tsx` | 图层控制组件，控制地图上显示哪些协同成员的草案图层 |
| `RouteEditor.tsx` | 路线编辑器组件，可拖拽排序的路线点列表（基于dnd-kit） |
| `SpotStatsPanel.tsx` | 景点统计面板，展示各景点被选择的次数和热度排行 |

#### 地图组件（src/components/map/）

| 文件 | 说明 |
|------|------|
| `MapCopyright.tsx` | 地图审图号组件，确保高德地图审图号清晰可见（合规要求） |

#### 媒体组件（src/components/media/）

| 文件 | 说明 |
|------|------|
| `AvatarCropper.tsx` | 头像裁剪组件，正方形裁剪框，基于react-easy-crop |
| `ImageCropper.tsx` | 图片裁剪组件，适配屏幕比例裁剪，基于react-easy-crop |

#### 通知组件（src/components/notification/）

| 文件 | 说明 |
|------|------|
| `NotificationBell.tsx` | 通知铃铛组件，展示未读通知数角标，下拉展示通知列表，支持标记已读/全部已读 |

#### 评价组件（src/components/review/）

| 文件 | 说明 |
|------|------|
| `ReviewForm.tsx` | 评价表单组件，支持评分/文字评论/图片上传的评价提交 |
| `ReviewList.tsx` | 评价列表组件，展示评价列表/评分分布/点赞，支持删除评价 |

#### 景点组件（src/components/spot/）

| 文件 | 说明 |
|------|------|
| `SpotCard.tsx` | 景点卡片组件，带展开描述功能，支持收藏/取消收藏 |
| `DestinationAttractionCard.tsx` | 目的地景点卡片组件，展示景点信息，支持收藏切换 |
| `SpotImageUploadModal.tsx` | 景点图片上传弹窗，支持多图上传/预览/删除 |

#### 行程组件（src/components/trip/）

| 文件 | 说明 |
|------|------|
| `PackingListDrawer.tsx` | 打包清单抽屉组件，侧边抽屉形式的打包清单管理（添加/编辑/勾选） |

#### 用户组件（src/components/user/）

| 文件 | 说明 |
|------|------|
| `UserProfileEditModal.tsx` | 用户信息编辑弹窗，修改用户名/昵称/性别/简介/头像 |

#### UI基础组件 - shadcn/ui（src/components/ui/）

| 文件 | 说明 |
|------|------|
| `avatar.tsx` | shadcn Avatar头像组件，基于Radix UI，支持sm/default/lg三种尺寸 |
| `badge.tsx` | shadcn Badge徽章组件，支持default/secondary/destructive/outline/ghost等变体 |
| `button.tsx` | shadcn Button按钮组件，支持default/outline/secondary/ghost/destructive等变体和sm/default/lg尺寸 |
| `card.tsx` | shadcn Card卡片组件，支持default/sm两种尺寸和毛玻璃效果变体 |
| `checkbox.tsx` | shadcn Checkbox复选框组件，基于Radix UI |
| `input.tsx` | shadcn Input输入框组件，统一表单输入样式 |
| `tabs.tsx` | shadcn Tabs标签页组件，基于Radix UI，支持水平/垂直方向 |
| `textarea.tsx` | shadcn Textarea文本域组件，统一多行文本输入样式 |

### 2.14 公共静态资源（public/）

| 文件 | 说明 |
|------|------|
| `logo.png` | 应用Logo图片 |
| `homepage-bg.jpg` | 首页背景图片 |
| `images/login-bg.png` | 登录页背景图片 |
| `images/login-logo.png` | 登录页Logo图片 |
| `videos/herovideo2.mp4` | 首页英雄区域背景视频 |

---

## 三、文件统计

| 类别 | 文件数 |
|------|--------|
| 后端源码（src/） | 63 |
| 后端配置文件 | 7 |
| 后端数据库（prisma/） | 36 |
| 后端脚本（scripts/） | 4 |
| 前端源码（src/） | 82 |
| 前端配置文件 | 14 |
| 前端公共资源（public/） | 5 |
| 项目根文件 | 2 |
| **合计** | **213** |

> 注：统计不含 node_modules/、.git/、.codeartsdoer/、.arts/ 目录，不含 package-lock.json 和 dev.db 数据库文件。数据库迁移目录按独立目录计数（含migration.sql）。
