// AI辅助生成：GLM-5, 2026-04-25 21:24
// 描述：LiveTrip Agent功能问题诊断与解决方案文档，详细分析两个核心问题并提供完整的修复方案。

# LiveTrip Agent 功能问题诊断与解决方案

## 📋 问题概述

**生成时间**：2026-04-25 21:24  
**问题数量**：2个核心问题  
**影响范围**：Agent工具调用机制、系统提示词设计

---

## 🔍 问题1：Agent无法回答行程数量问题

### 问题描述

用户询问"我有多少个行程"，Agent无法正确回答，未能调用`list_user_trips`工具。

### 根本原因分析

#### 1. 系统提示词问题

**当前系统提示词**（agentService.ts:406-414）：
```
【工具使用规则】

1. 用户提到具体景点 → 调用extract_must_visit_spots提取景点,然后自动创建行程

2. 用户未提到具体景点 → 直接调用create_trip创建行程

3. 用户要查看行程 → 调用list_user_trips

4. 用户要生成博客 → 调用generate_blog
```

**问题所在**：
- ❌ 规则2过于宽泛："用户未提到具体景点 → 直接调用create_trip创建行程"
- ❌ 这导致AI认为任何不包含景点的用户输入都应该创建行程
- ❌ 规则3的触发条件不够明确："用户要查看行程"过于模糊
- ❌ 缺少对"询问行程数量"、"统计行程"等场景的明确规则

#### 2. 工具描述问题

**当前工具描述**（agentService.ts:261-267）：
```typescript
name: 'list_user_trips',
description: `查看用户的行程列表。

【触发条件】
- 用户想查看行程（如"查看我的行程"、"有哪些行程"）

【参数说明】
- status: 行程状态筛选（可选）`
```

**问题所在**：
- ❌ 触发条件示例不足，缺少"我有多少个行程"、"行程数量"等场景
- ❌ 没有明确说明该工具可以返回行程数量统计

### 解决方案

#### 方案1：优化系统提示词（推荐）

**修改位置**：`agentService.ts:406-414`

**修改前**：
```typescript
【工具使用规则】

1. 用户提到具体景点 → 调用extract_must_visit_spots提取景点,然后自动创建行程

2. 用户未提到具体景点 → 直接调用create_trip创建行程

3. 用户要查看行程 → 调用list_user_trips

4. 用户要生成博客 → 调用generate_blog
```

**修改后**：
```typescript
【工具使用规则】

1. 用户明确表达创建行程意图（如"我想去北京"、"计划旅行"）→ 调用create_smart_trip

2. 用户提到具体景点并表达旅行意图 → 调用extract_must_visit_spots提取景点,然后自动创建行程

3. 用户想查看、询问行程（如"查看我的行程"、"有哪些行程"、"我有多少个行程"、"行程列表"）→ 调用list_user_trips

4. 用户想写游记、生成博客（如"为上次旅行写游记"、"生成博客"）→ 调用manage_blog,action=generate

5. 用户想发布博客（如"发布这篇博客"、"发布游记"）→ 调用manage_blog,action=publish

6. 用户确认操作（如"保存"、"确认"、"好的"）→ 调用confirm_action

7. 用户不满意想重新生成（如"重新规划"、"重新生成"）→ 调用regenerate

【重要】不要在用户询问行程数量或查看行程时创建新行程！
```

#### 方案2：优化工具描述

**修改位置**：`agentService.ts:261-267`

**修改前**：
```typescript
name: 'list_user_trips',
description: `查看用户的行程列表。

【触发条件】
- 用户想查看行程（如"查看我的行程"、"有哪些行程"）

【参数说明】
- status: 行程状态筛选（可选）`
```

**修改后**：
```typescript
name: 'list_user_trips',
description: `查看用户的行程列表，返回行程数量和详细信息。

【触发条件】
- 用户想查看行程（如"查看我的行程"、"有哪些行程"）
- 用户询问行程数量（如"我有多少个行程"、"行程数量"）
- 用户想了解行程概况（如"我的行程列表"、"行程统计"）

【返回信息】
- 行程数量统计
- 每个行程的详细信息（标题、目的地、日期、状态等）

【参数说明】
- status: 行程状态筛选（可选），可选值："planning"（规划中）、"ongoing"（进行中）、"completed"（已完成）`
```

---

## 🔍 问题2：博客发布功能失败，Agent误生成行程

### 问题描述

用户提出"发表博客"的需求，Agent却生成了一个行程，而不是调用博客管理工具。

### 根本原因分析

#### 1. 系统提示词问题（同问题1）

**问题所在**：
- ❌ 规则2："用户未提到具体景点 → 直接调用create_trip创建行程"
- ❌ 当用户说"发表博客"时，AI判断"未提到具体景点"，触发了创建行程逻辑
- ❌ 规则4："用户要生成博客 → 调用generate_blog"过于简单
  - 没有区分"生成博客"和"发布博客"两种操作
  - 没有说明应该调用`manage_blog`工具

#### 2. 工具描述问题

**当前工具描述**（agentService.ts:285-296）：
```typescript
name: 'manage_blog',
description: `管理旅行博客：生成或发布博客。

【触发条件】
- 用户想写游记（如"为上次旅行写游记"）
- 用户想发布博客（如"发布这篇博客"）

【参数说明】
- action: 操作类型（必填）
  * generate: 生成博客
  * publish: 发布博客
- tripId: 行程ID（生成博客时必填）
- blogId: 博客ID（发布博客时必填）`
```

**问题所在**：
- ❌ 触发条件示例不够丰富
- ❌ 没有明确说明"发表"和"发布"是同义词
- ❌ 没有说明当用户说"发表博客"时，应该设置`action=publish`

#### 3. 博客功能依赖问题

**数据库字段分析**（schema.prisma:333-364）：
```prisma
model BlogPost {
  id           String        @id @default(cuid())
  userId       String
  title        String
  content      String
  coverImage   String?
  tags         String
  city         String?
  spotIds      String?
  imageIds     String?
  viewCount    Int           @default(0)
  likeCount    Int           @default(0)
  commentCount Int           @default(0)
  isPublished  Boolean       @default(false)
  publishedAt  DateTime?
  status       String        @default("pending")  // ⚠️ 注意：默认状态是pending
  reviewedBy   String?
  reviewedAt   DateTime?
  reviewNote   String?
  // ...
}
```

**问题所在**：
- ⚠️ 博客默认状态是`pending`（待审核），不是`draft`（草稿）
- ⚠️ 博客发布逻辑（agentService.ts:1714-1722）：
  ```typescript
  const updatedBlog = await prisma.blogPost.update({
    where: { id: params.blogId },
    data: {
      status: 'published',
      isPublished: true,
      publishedAt: new Date(),
    },
  });
  ```
  - 直接将状态改为`published`，跳过了审核流程
  - 可能与业务逻辑冲突（如果有审核机制）

### 解决方案

#### 方案1：优化系统提示词（同问题1，已包含）

**修改后的规则**：
```typescript
4. 用户想写游记、生成博客（如"为上次旅行写游记"、"生成博客"）→ 调用manage_blog,action=generate

5. 用户想发布博客（如"发布这篇博客"、"发布游记"、"发表博客"）→ 调用manage_blog,action=publish
```

#### 方案2：优化工具描述

**修改位置**：`agentService.ts:285-296`

**修改前**：
```typescript
name: 'manage_blog',
description: `管理旅行博客：生成或发布博客。

【触发条件】
- 用户想写游记（如"为上次旅行写游记"）
- 用户想发布博客（如"发布这篇博客"）

【参数说明】
- action: 操作类型（必填）
  * generate: 生成博客
  * publish: 发布博客
- tripId: 行程ID（生成博客时必填）
- blogId: 博客ID（发布博客时必填）`
```

**修改后**：
```typescript
name: 'manage_blog',
description: `管理旅行博客：生成或发布博客。

【触发条件】
- 用户想写游记、生成博客（如"为上次旅行写游记"、"写一篇游记"、"生成博客"）
  → 设置 action=generate，需要提供 tripId
- 用户想发布、发表博客（如"发布这篇博客"、"发表博客"、"发布游记"）
  → 设置 action=publish，需要提供 blogId

【参数说明】
- action: 操作类型（必填）
  * generate: 生成博客（需要tripId）
  * publish: 发布博客（需要blogId）
- tripId: 行程ID（生成博客时必填，从用户最近的已完成行程中获取）
- blogId: 博客ID（发布博客时必填，从当前会话的博客草稿中获取）
- title: 博客标题（可选）
- style: 博客风格（可选）：详细记录/简洁总结/情感抒情

【重要】
- 生成博客前，需要先确认用户有已完成的行程
- 发布博客前，需要先有博客草稿（通过生成博客创建）`
```

#### 方案3：优化博客发布逻辑（可选）

**问题**：博客状态管理混乱（pending/draft/published）

**建议**：
1. **统一状态定义**：
   ```typescript
   // 博客状态枚举
   enum BlogStatus {
     DRAFT = 'draft',        // 草稿
     PENDING = 'pending',    // 待审核
     PUBLISHED = 'published' // 已发布
   }
   ```

2. **修改生成博客逻辑**（agentService.ts:1498-1510）：
   ```typescript
   const blog = await prisma.blogPost.create({
     data: {
       userId: trip.userId,
       title: blogTitle,
       content: blogContent,
       city: trip.destination,
       spotIds: tripInfo.spots.map(s => s.name).join(','),
       tags: '旅行,游记',
       status: 'draft',  // ✅ 改为draft，而不是pending
       isPublished: false,
     },
   });
   ```

3. **修改发布博客逻辑**（agentService.ts:1714-1722）：
   ```typescript
   // 检查博客状态
   if (blog.status === 'published') {
     return { success: true, data: { message: '博客已发布' } };
   }
   
   // 发布博客（根据是否有审核机制）
   const updatedBlog = await prisma.blogPost.update({
     where: { id: params.blogId },
     data: {
       status: 'published',  // 或 'pending' 如果需要审核
       isPublished: true,
       publishedAt: new Date(),
     },
   });
   ```

---

## 🛠️ 完整修复方案

### 修复步骤

#### 步骤1：修改系统提示词

**文件**：`backend/src/services/agentService.ts`  
**位置**：第406-414行

**修改内容**：
```typescript
【工具使用规则】

1. 用户明确表达创建行程意图（如"我想去北京"、"计划旅行"）→ 调用create_smart_trip

2. 用户提到具体景点并表达旅行意图 → 调用extract_must_visit_spots提取景点,然后自动创建行程

3. 用户想查看、询问行程（如"查看我的行程"、"有哪些行程"、"我有多少个行程"、"行程列表"）→ 调用list_user_trips

4. 用户想写游记、生成博客（如"为上次旅行写游记"、"写一篇游记"、"生成博客"）→ 调用manage_blog,action=generate

5. 用户想发布、发表博客（如"发布这篇博客"、"发表博客"、"发布游记"）→ 调用manage_blog,action=publish

6. 用户确认操作（如"保存"、"确认"、"好的"）→ 调用confirm_action

7. 用户不满意想重新生成（如"重新规划"、"重新生成"）→ 调用regenerate

【重要】
- 不要在用户询问行程数量或查看行程时创建新行程！
- 不要在用户想发布博客时创建行程！
- 仔细理解用户意图，选择正确的工具！
```

#### 步骤2：修改工具描述

**文件**：`backend/src/services/agentService.ts`

**修改1：list_user_trips工具**（第261-267行）：
```typescript
name: 'list_user_trips',
description: `查看用户的行程列表，返回行程数量和详细信息。

【触发条件】
- 用户想查看行程（如"查看我的行程"、"有哪些行程"）
- 用户询问行程数量（如"我有多少个行程"、"行程数量"）
- 用户想了解行程概况（如"我的行程列表"、"行程统计"）

【返回信息】
- 行程数量统计
- 每个行程的详细信息（标题、目的地、日期、状态等）

【参数说明】
- status: 行程状态筛选（可选），可选值："planning"（规划中）、"ongoing"（进行中）、"completed"（已完成）`
```

**修改2：manage_blog工具**（第285-296行）：
```typescript
name: 'manage_blog',
description: `管理旅行博客：生成或发布博客。

【触发条件】
- 用户想写游记、生成博客（如"为上次旅行写游记"、"写一篇游记"、"生成博客"）
  → 设置 action=generate，需要提供 tripId
- 用户想发布、发表博客（如"发布这篇博客"、"发表博客"、"发布游记"）
  → 设置 action=publish，需要提供 blogId

【参数说明】
- action: 操作类型（必填）
  * generate: 生成博客（需要tripId）
  * publish: 发布博客（需要blogId）
- tripId: 行程ID（生成博客时必填，从用户最近的已完成行程中获取）
- blogId: 博客ID（发布博客时必填，从当前会话的博客草稿中获取）
- title: 博客标题（可选）
- style: 博客风格（可选）：详细记录/简洁总结/情感抒情

【重要】
- 生成博客前，需要先确认用户有已完成的行程
- 发布博客前，需要先有博客草稿（通过生成博客创建）`
```

#### 步骤3：优化博客状态管理（可选）

**文件**：`backend/src/services/agentService.ts`

**修改1：生成博客时设置正确状态**（第1498-1510行）：
```typescript
const blog = await prisma.blogPost.create({
  data: {
    userId: trip.userId,
    title: blogTitle,
    content: blogContent,
    city: trip.destination,
    spotIds: tripInfo.spots.map(s => s.name).join(','),
    tags: '旅行,游记',
    status: 'draft',  // ✅ 改为draft
    isPublished: false,
  },
});
```

**修改2：发布博客时增加状态检查**（第1690-1722行）：
```typescript
// 验证博客状态
if (blog.status === 'published') {
  return {
    success: true,
    data: {
      id: blog.id,
      title: blog.title,
      status: blog.status,
      message: '博客已经是发布状态',
    },
  };
}

// 检查是否是草稿状态
if (blog.status !== 'draft' && blog.status !== 'pending') {
  return {
    success: false,
    error: `博客状态异常：${blog.status}，无法发布`,
  };
}

// 发布博客
const updatedBlog = await prisma.blogPost.update({
  where: { id: params.blogId },
  data: {
    status: 'published',
    isPublished: true,
    publishedAt: new Date(),
  },
});
```

---

## 📊 修复效果预期

### 问题1修复效果

**修复前**：
```
用户: "我有多少个行程"
Agent: [创建了一个新行程] ❌
```

**修复后**：
```
用户: "我有多少个行程"
Agent: [调用list_user_trips]
       "您目前有3个行程：
        1. 北京之旅（规划中）
        2. 上海三日游（已完成）
        3. 杭州周末游（进行中）" ✅
```

### 问题2修复效果

**修复前**：
```
用户: "发表博客"
Agent: [创建了一个新行程] ❌
```

**修复后**：
```
用户: "发表博客"
Agent: [调用manage_blog, action=publish]
       "请提供要发布的博客ID，或者先生成一篇博客草稿。" ✅
       
用户: "为我的上海行程写一篇游记"
Agent: [调用manage_blog, action=generate]
       "✅ 博客生成成功！
        标题：上海之旅
        状态：草稿
        
        是否发布这篇博客？"
       
用户: "发布"
Agent: [调用manage_blog, action=publish]
       "✅ 博客发布成功！
        访问链接：https://livetrip.com/blog/xxx" ✅
```

---

## 🧪 测试建议

### 测试场景1：行程查询

**测试用例**：
1. "我有多少个行程"
2. "查看我的行程"
3. "有哪些行程"
4. "行程列表"
5. "我的行程统计"

**预期结果**：所有用例都应调用`list_user_trips`工具，返回行程列表和数量。

### 测试场景2：博客管理

**测试用例**：
1. "为上次旅行写游记"
2. "写一篇博客"
3. "生成博客"
4. "发布这篇博客"
5. "发表博客"

**预期结果**：
- 用例1-3：调用`manage_blog`，`action=generate`
- 用例4-5：调用`manage_blog`，`action=publish`

### 测试场景3：混合场景

**测试用例**：
1. "我想去北京玩三天" → 创建行程
2. "我有多少个行程" → 查看行程
3. "为北京行程写游记" → 生成博客
4. "发布" → 发布博客

**预期结果**：每个用例都应调用正确的工具。

---

## 📝 总结

### 核心问题

1. **系统提示词设计缺陷**：
   - 规则过于宽泛，导致误判
   - 触发条件不明确
   - 缺少重要场景的规则

2. **工具描述不够详细**：
   - 触发示例不足
   - 参数说明不清晰
   - 缺少重要提示

### 解决方案核心

1. **明确工具使用规则**：
   - 区分不同场景的触发条件
   - 添加反例规则（不要做什么）
   - 提供丰富的示例

2. **优化工具描述**：
   - 详细的触发条件
   - 清晰的参数说明
   - 重要的使用提示

3. **统一状态管理**：
   - 明确博客状态流转
   - 增加状态检查
   - 优化错误提示

### 修复优先级

1. **P0（必须修复）**：
   - 修改系统提示词（步骤1）
   - 修改工具描述（步骤2）

2. **P1（建议修复）**：
   - 优化博客状态管理（步骤3）

3. **P2（可选优化）**：
   - 增加更多测试用例
   - 优化错误提示文案

---

**文档版本**：v1.0  
**最后更新**：2026-04-25 21:24  
**维护者**：LiveTrip开发团队
