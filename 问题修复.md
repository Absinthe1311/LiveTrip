# 🔍 硬编码问题排查报告

**排查时间**：2026-04-16  
**项目名称**：LiveTrip - AI + IoT 智能旅行规划系统

---

## 📋 问题清单

### 一、前端代码中的硬编码问题

#### 1. Socket.io 连接地址硬编码

| 文件路径 | 行号 | 硬编码内容 | 问题类型 |
|---------|------|-----------|---------|
| `frontend/src/services/collabSocket.ts` | 18 | `io('http://localhost:3003', ...)` | Socket连接地址 |
| `frontend/src/services/collabSocket.ts` | 49 | `fetch('http://localhost:3003/api/collab/rooms/...')` | API请求地址 |
| `frontend/src/services/collabSocket.ts` | 76 | `fetch('http://localhost:3003/api/collab/rooms/...')` | API请求地址 |
| `frontend/src/services/collabSocket.ts` | 132 | `fetch('http://localhost:3003/api/collab/rooms/.../drafts/all')` | API请求地址 |
| `frontend/src/hooks/useSocket.ts` | 19 | `io('http://localhost:3003', ...)` | Socket连接地址 |

#### 2. AI服务地址硬编码

| 文件路径 | 行号 | 硬编码内容 | 问题类型 |
|---------|------|-----------|---------|
| `frontend/src/services/aiService.ts` | 45 | `private baseUrl = 'http://localhost:3003/api'` | API基础地址 |

#### 3. 页面组件中的API请求硬编码

| 文件路径 | 行号 | 硬编码内容 | 问题类型 |
|---------|------|-----------|---------|
| `frontend/src/pages/PlanGlass.tsx` | 117 | `fetch('http://localhost:3003/api/upload/image')` | 图片上传API |
| `frontend/src/pages/Home.tsx` | 81 | `fetch('http://localhost:3003/api/users/profile')` | 用户信息API |
| `frontend/src/pages/collab/CollabSetup.tsx` | 126 | `fetch('http://localhost:3003/api/trips')` | 行程保存API |
| `frontend/src/pages/AIFeaturesGlass.tsx` | 481 | `fetch('http://localhost:3003/api/agent/confirm-trip')` | AI确认API |
| `frontend/src/pages/AIFeaturesGlass.tsx` | 514 | `fetch('http://localhost:3003/api/agent/cancel-draft')` | AI取消API |

#### 4. 组件中的API请求硬编码

| 文件路径 | 行号 | 硬编码内容 | 问题类型 |
|---------|------|-----------|---------|
| `frontend/src/components/UserProfileEditModal.tsx` | 87 | `fetch('http://localhost:3003/api/users/avatar')` | 头像上传API |
| `frontend/src/components/UserProfileEditModal.tsx` | 115 | `fetch('http://localhost:3003/api/users/profile')` | 用户信息API |
| `frontend/src/components/SettingsModal.tsx` | 77 | `fetch('http://localhost:3003/api/upload/image')` | 图片上传API |
| `frontend/src/components/GlassLayout.tsx` | 82 | `fetch('http://localhost:3003/api/upload/image')` | 图片上传API |
| `frontend/src/components/BlogEditorGlass.tsx` | 410 | `fetch('http://localhost:3003/api/upload/image')` | 图片上传API |

#### 5. 已使用环境变量但有默认值的文件（需要统一）

| 文件路径 | 行号 | 当前写法 | 说明 |
|---------|------|---------|------|
| `frontend/src/api/client.ts` | 6 | `import.meta.env.VITE_API_BASE_URL \|\| 'http://localhost:3003/api'` | ✅ 正确使用环境变量 |
| `frontend/src/api/adminImageClient.ts` | 3 | `import.meta.env.VITE_API_URL \|\| 'http://localhost:3003/api'` | ⚠️ 环境变量名不一致 |
| `frontend/src/components/SpotImageUploadModal.tsx` | 87, 137 | `import.meta.env.VITE_API_BASE_URL \|\| 'http://localhost:3003/api'` | ✅ 正确使用环境变量 |
| `frontend/src/components/PopularDestinations.tsx` | 259 | `import.meta.env.VITE_API_BASE_URL \|\| 'http://localhost:3003/api'` | ✅ 正确使用环境变量 |

---

### 二、后端代码中的硬编码问题

| 文件路径 | 行号 | 硬编码内容 | 问题类型 |
|---------|------|-----------|---------|
| `backend/src/socket/socketService.ts` | 25 | `process.env.CORS_ORIGIN \|\| 'http://localhost:5173'` | CORS配置（有默认值） |
| `backend/src/services/shareService.ts` | 39, 80 | `process.env.FRONTEND_URL \|\| 'http://localhost:5174'` | 前端URL（有默认值） |
| `backend/src/services/collabService.ts` | 73 | `process.env.FRONTEND_URL \|\| 'http://localhost:5173'` | 前端URL（有默认值） |
| `backend/src/index.ts` | 21 | `process.env.PORT \|\| 3001` | 端口配置（有默认值） |

---

### 三、环境变量配置不一致问题

| 文件 | 配置项 | 值 | 问题 |
|-----|-------|-----|------|
| `backend/.env.example` | `PORT` | `3001` | 后端默认端口 |
| `backend/.env.example` | `CORS_ORIGIN` | `http://localhost:5173` | 前端地址 |
| `frontend/.env.example` | `VITE_API_BASE_URL` | `http://localhost:3001` | ✅ 正确 |
| **实际使用** | 多处硬编码 | `http://localhost:3003` | ❌ 与配置不一致！ |

**⚠️ 严重问题**：环境变量配置中后端端口是 `3001`，但代码中大量硬编码使用 `3003`！

---

## 💡 修改建议

### 建议一：统一环境变量配置

#### 1. 前端环境变量 (`frontend/.env` 和 `frontend/.env.example`)

```env
# ============================================
# LiveTrip Frontend 环境变量配置示例
# ============================================
# 请复制此文件为 .env 并填入真实值

# API 配置
VITE_API_BASE_URL=http://localhost:3001/api
VITE_SOCKET_URL=http://localhost:3001

# 高德地图 API 配置
# 前往 https://console.amap.com/dev/key/app 注册申请
VITE_AMAP_JS_KEY=your-amap-js-key-here
VITE_AMAP_JS_SECRET=your-amap-js-secret-here

# 智谱AI 配置（用于AI行程规划）
# 前往 https://open.bigmodel.cn/ 获取 API Key
VITE_ZHIPUAI_API_KEY=your-zhipuai-api-key-here
```

#### 2. 后端环境变量 (`backend/.env` 和 `backend/.env.example`)

```env
# ============================================
# LiveTrip Backend 环境变量配置示例
# ============================================
# 请复制此文件为 .env 并填入真实值

# 数据库配置
DATABASE_URL="file:./dev.db"

# 服务器配置
PORT=3001
NODE_ENV=development

# JWT 配置
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=7d

# 智谱AI 配置（用于AI行程规划）
# 前往 https://open.bigmodel.cn/ 获取 API Key
ZHIPUAI_API_KEY=your-zhipuai-api-key-here

# 高德地图 API 配置
# 前往 https://console.amap.com/dev/key/app 注册申请
AMAP_JS_KEY=your-amap-js-key-here
AMAP_JS_SECRET=your-amap-js-secret-here

# Cloudinary 配置（图片存储服务）
# 前往 https://cloudinary.com/ 获取配置信息
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# OpenWeatherMap 天气数据配置
# 前往 https://openweathermap.org/api 获取 API Key
OPENWEATHERMAP_API_KEY=your-openweathermap-api-key-here
OPENWEATHERMAP_API_URL=https://api.openweathermap.org/data/2.5

# CORS 配置
CORS_ORIGIN=http://localhost:5173

# 前端URL配置（用于生成分享链接、协同邀请链接等）
FRONTEND_URL=http://localhost:5173
```

---

### 建议二：创建统一的配置文件

#### 创建 `frontend/src/config/api.ts`

```typescript
/**
 * API 配置文件
 * 统一管理所有 API 和 Socket 连接地址
 */

// API 基础地址
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:3001/api';

// Socket.io 连接地址
export const SOCKET_URL = import.meta.env.VITE_SOCKET_URL || 'http://localhost:3001';

// 导出默认配置
export default {
  API_BASE_URL,
  SOCKET_URL,
};
```

---

### 建议三：修改前端代码

#### 1. 修改 Socket.io 连接

**文件**：`frontend/src/services/collabSocket.ts`

```typescript
// 修改前（第18行）
socket = io('http://localhost:3003', {
  auth: { token },
  transports: ['websocket'],
});

// 修改后
import { SOCKET_URL, API_BASE_URL } from '../config/api';

socket = io(SOCKET_URL, {
  auth: { token },
  transports: ['websocket'],
});

// 同时修改第49、76、132行的 fetch 调用
// 修改前
const response = await fetch(`http://localhost:3003/api/collab/rooms/${store.currentRoom.id}`, ...);

// 修改后
const response = await fetch(`${API_BASE_URL}/collab/rooms/${store.currentRoom.id}`, ...);
```

**文件**：`frontend/src/hooks/useSocket.ts`

```typescript
// 修改前（第19行）
socket = io('http://localhost:3003', {
  auth: { token },
  transports: ['websocket', 'polling'],
});

// 修改后
import { SOCKET_URL } from '../config/api';

socket = io(SOCKET_URL, {
  auth: { token },
  transports: ['websocket', 'polling'],
});
```

#### 2. 修改 AI 服务

**文件**：`frontend/src/services/aiService.ts`

```typescript
// 修改前（第45行）
private baseUrl = 'http://localhost:3003/api';

// 修改后
import { API_BASE_URL } from '../config/api';

class AIService {
  private baseUrl = API_BASE_URL;
  // ...
}
```

#### 3. 修改页面组件中的 fetch 调用

**文件**：`frontend/src/pages/PlanGlass.tsx`

```typescript
// 修改前（第117行）
const uploadResponse = await fetch('http://localhost:3003/api/upload/image', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const uploadResponse = await fetch(`${API_BASE_URL}/upload/image`, {...});
```

**文件**：`frontend/src/pages/Home.tsx`

```typescript
// 修改前（第81行）
const response = await fetch('http://localhost:3003/api/users/profile', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const response = await fetch(`${API_BASE_URL}/users/profile`, {...});
```

**文件**：`frontend/src/pages/collab/CollabSetup.tsx`

```typescript
// 修改前（第126行）
const saveResponse = await fetch('http://localhost:3003/api/trips', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const saveResponse = await fetch(`${API_BASE_URL}/trips`, {...});
```

**文件**：`frontend/src/pages/AIFeaturesGlass.tsx`

```typescript
// 修改前（第481行）
const res = await fetch('http://localhost:3003/api/agent/confirm-trip', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const res = await fetch(`${API_BASE_URL}/agent/confirm-trip`, {...});

// 修改前（第514行）
const res = await fetch('http://localhost:3003/api/agent/cancel-draft', {...});

// 修改后
const res = await fetch(`${API_BASE_URL}/agent/cancel-draft`, {...});
```

#### 4. 修改组件中的 fetch 调用

**文件**：`frontend/src/components/UserProfileEditModal.tsx`

```typescript
// 修改前（第87行）
const uploadResponse = await fetch('http://localhost:3003/api/users/avatar', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const uploadResponse = await fetch(`${API_BASE_URL}/users/avatar`, {...});

// 修改前（第115行）
const response = await fetch('http://localhost:3003/api/users/profile', {...});

// 修改后
const response = await fetch(`${API_BASE_URL}/users/profile`, {...});
```

**文件**：`frontend/src/components/SettingsModal.tsx`

```typescript
// 修改前（第77行）
const uploadResponse = await fetch('http://localhost:3003/api/upload/image', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const uploadResponse = await fetch(`${API_BASE_URL}/upload/image`, {...});
```

**文件**：`frontend/src/components/GlassLayout.tsx`

```typescript
// 修改前（第82行）
const uploadResponse = await fetch('http://localhost:3003/api/upload/image', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const uploadResponse = await fetch(`${API_BASE_URL}/upload/image`, {...});
```

**文件**：`frontend/src/components/BlogEditorGlass.tsx`

```typescript
// 修改前（第410行）
const uploadResponse = await fetch('http://localhost:3003/api/upload/image', {...});

// 修改后
import { API_BASE_URL } from '../config/api';
const uploadResponse = await fetch(`${API_BASE_URL}/upload/image`, {...});
```

#### 5. 统一环境变量名称

**文件**：`frontend/src/api/adminImageClient.ts`

```typescript
// 修改前（第3行）
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3003/api';

// 修改后
import { API_BASE_URL } from '../config/api';
// 或直接使用统一的配置
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:3001/api';
```

---

### 建议四：部署时的环境变量配置

#### 开发环境配置

**前端** `.env.development`：
```env
VITE_API_BASE_URL=http://localhost:3001/api
VITE_SOCKET_URL=http://localhost:3001
```

**后端** `.env.development`：
```env
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
```

#### 生产环境配置

**前端** `.env.production`：
```env
VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_SOCKET_URL=https://api.yourdomain.com
```

**后端** `.env.production`：
```env
PORT=3001
NODE_ENV=production
CORS_ORIGIN=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com
DATABASE_URL="file:./production.db"
```

---

## 📊 问题统计

| 类型 | 数量 | 严重程度 |
|-----|------|---------|
| Socket.io连接硬编码 | 2处 | 🔴 高 |
| API请求地址硬编码 | 15+处 | 🔴 高 |
| 环境变量名不一致 | 1处 | 🟡 中 |
| 端口号不一致（3001 vs 3003） | 全局 | 🔴 高 |

---

## ✅ 修复步骤总结

### 第一步：创建配置文件
1. 创建 `frontend/src/config/api.ts` 统一管理API地址

### 第二步：修改环境变量
1. 更新 `frontend/.env.example`，添加 `VITE_SOCKET_URL`
2. 更新 `backend/.env.example`，添加 `FRONTEND_URL`
3. 统一端口号为 `3001`

### 第三步：修改前端代码
1. 修改所有 Socket.io 连接地址
2. 修改所有硬编码的 fetch 调用
3. 统一环境变量名称

### 第四步：测试验证
1. 开发环境测试：确保所有功能正常
2. 生产环境测试：使用生产环境变量部署测试

---

## 🎯 核心问题总结

**问题根源**：
1. 前端大量使用硬编码的 `http://localhost:3003`，而环境变量配置的是 `3001`
2. Socket.io 连接地址完全硬编码，无法适应生产环境
3. 缺少统一的环境变量管理机制

**解决方案**：
1. 创建统一的配置文件 `frontend/src/config/api.ts`
2. 将所有硬编码替换为环境变量引用
3. 统一环境变量命名规范
4. 确保开发环境和生产环境配置分离

**预期效果**：
- 所有API地址和Socket连接通过环境变量管理
- 开发环境和生产环境配置完全分离
- 部署时只需修改环境变量，无需修改代码

---

**报告生成时间**：2026-04-16  
**修复优先级**：🔴 高优先级（影响部署）
