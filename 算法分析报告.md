# LiveTrip 智能旅行规划系统算法分析报告

## 📋 文档概述

本文档详细分析了 LiveTrip 智能旅行规划系统的核心算法实现，包括算法的输入输出、处理流程、考虑因素和实现方法。通过本文档，开发者可以全面理解系统的算法逻辑，为后续的优化和改进提供参考。

---

## 🎯 算法目标

LiveTrip 的核心算法目标是：**根据用户的旅行需求（目的地、日期、预算、偏好），智能推荐个性化的行程规划方案**。

### 核心功能
1. **景点推荐**：根据用户偏好推荐适合的景点
2. **行程规划**：将景点按天分配，优化游览顺序
3. **路径优化**：减少交通时间，提升游览效率
4. **预算控制**：在预算范围内推荐最优方案
5. **实时调整**：基于 IoT 数据（拥挤度、天气）动态调整行程

---

## 📥 算法输入

### 1. 用户输入参数

| 参数名称 | 类型 | 必填 | 说明 | 示例 |
| ---------------| --------| ------| ------------------------| ----------------|
| `destination` | string | ✅ | 目的地城市名称 | "北京"、"上海" |
| `start_date` | string | ✅ | 开始日期（YYYY-MM-DD） | "2026-03-15" |
| `end_date` | string | ✅ | 结束日期（YYYY-MM-DD） | "2026-03-22" |
| `budget` | number | ❌ | 总预算（元），默认5000 | 8000 |
| `preferences` | object | ❌ | 用户偏好配置 | 见下方详情 |
| `groupSize` | number | ❌ | 出行人数，默认1 | 2 |
| `origin` | string | ❌ | 出发地城市 | "深圳" |

### 2. 用户偏好配置（preferences）

```typescript
{
  pace?: 'slow' | 'moderate' | 'fast';      // 出行节奏
  energy_level?: 'low' | 'medium' | 'high';  // 体力水平
  categories?: CategoryTag[];                // 兴趣标签数组
}
```

**CategoryTag 类型：**
```typescript
type CategoryTag =
  | 'history'       // 历史文化（博物馆、古迹）
  | 'nature'        // 自然风光（山、湖、公园）
  | 'beach'         // 海滨海岛
  | 'city'          // 城市地标（建筑、街区）
  | 'food'          // 美食体验
  | 'theme_park'    // 游乐场/主题公园
  | 'art'           // 艺术（美术馆、展览）
  | 'shopping'      // 购物
  | 'religious'     // 宗教（寺庙、教堂）
  | 'adventure';    // 户外运动/探险
```

### 3. 用户画像参数

| 参数名称 | 类型 | 必填 | 说明 | 示例 |
| ---------------| --------| ------| ------------------------| ----------------|
| `groupType` | string | ❌ | 群体类型 | 'solo' | 'couple' | 'family' | 'friends' |
| `hasChildren` | boolean | ❌ | 是否携带儿童 | false |
| `hasElderly` | boolean | ❌ | 是否携带老人 | false |

### 4. 外部数据源输入

#### 高德地图 API 数据
- **景点数据**：名称、位置（经纬度）、地址、类型、评分、价格等
- **POI 类型**：风景名胜、博物馆、公园、购物中心等

#### IoT 实时数据
- **拥挤度**：景点当前的拥挤程度（0-100%）
- **天气状况**：温度、湿度、降雨概率
- **开放状态**：景点是否开放

---

## 🔄 算法流程

### 整体架构

```
用户输入
    ↓
获取景点数据（高德地图API）
    ↓
计算景点综合评分（多因素评分引擎）
    ↓
K-means 地理聚类
    ↓
应用多样性约束
    ↓
构建行程（分配时间段）
    ↓
2-opt 路径优化
    ↓
动态预算计算
    ↓
IoT 实时检查
    ↓
生成备选景点池
    ↓
输出完整行程
```

### Phase 1：多因素评分引擎

**目标**：为每个景点计算综合评分

**评分公式**：
```
totalScore = preferenceScore × 0.35 + qualityScore × 0.25 + iotScore × 0.25 + crowdScore × 0.15
```

**各维度详解**：

#### 1. 偏好评分（Preference Score，35%）
- **方法**：Jaccard相似度
- **计算**：
```
Jaccard(A, B) = |A ∩ B| / |A ∪ B|
```
- **说明**：计算用户兴趣标签与景点类别的相似度

#### 2. 质量评分（Quality Score，25%）
- **评分因子**：
  - 景点评分（0-5分）
  - 描述完整度
  - 图片数量
  - 用户评论数

#### 3. IoT评分（IoT Score，25%）
- **拥挤度评分**：拥挤度越低，评分越高
- **天气评分**：天气越好，评分越高
- **开放状态**：开放状态好的景点评分更高

#### 4. 人群评分（Crowd Score，15%）
- **群体类型适配**：
  - 儿童：适合儿童的景点（公园、游乐场等）加分
  - 老人：适合老人的景点（博物馆、古镇等）加分
  - 情侣：浪漫景点加分
  - 朋友：热闹景点加分

### Phase 2：K-means 地理聚类

**目标**：将景点按地理位置分组，减少每日交通时间

**算法步骤**：
1. 初始化聚类中心（选择评分最高的K个景点）
2. 分配景点到最近的聚类中心
3. 重新计算聚类中心
4. 迭代直到收敛
5. 平衡聚类大小（处理不均衡情况）

**聚类数量**：
```typescript
k = days (行程天数)
```

**平衡策略**：
- 如果某个聚类景点过少（<平均值的50%），从最近的聚类借用
- 如果某个聚类景点过多（>平均值的150%），移动低分景点到最近的聚类

### Phase 3：多样性约束

**目标**：确保推荐结果的多样性

**约束规则**：

1. **同类型限制**：每天最多2个相同类型的景点
2. **总体限制**：相同类型的景点不超过总量的50%
3. **最小类型数**：每天至少2种不同类型的景点

**实现方法**：
```typescript
function canAddSpot(spot, selectedSpots, maxSpots): boolean {
  // 规则1：同类型限制
  for (const category of spot.categories) {
    const count = selectedSpots.filter(s => s.categories.includes(category)).length;
    if (count >= 2) return false;
  }

  // 规则2：总体限制
  const totalSelected = selectedSpots.length;
  if (totalSelected > 0) {
    for (const category of spot.categories) {
      const count = selectedSpots.filter(s => s.categories.includes(category)).length;
      const ratio = (count + 1) / (totalSelected + 1);
      if (ratio > 0.5) return false;
    }
  }

  // 规则3：最小类型数
  if (selectedSpots.length >= maxSpots - 1) {
    const allCategories = new Set<CategoryTag>();
    for (const s of [...selectedSpots, spot]) {
      s.categories.forEach(cat => allCategories.add(cat));
    }
    if (allCategories.size < 2) return false;
  }

  return true;
}
```

### Phase 4：2-opt 路径优化

**目标**：优化每日景点的游览顺序，减少总行程距离

**算法步骤**：
1. 使用贪心算法生成初始路线
2. 迭代尝试交换两条边
3. 如果交换后总距离更短，接受交换
4. 重复直到无法进一步优化

**实现方法**：
```typescript
function twoOptOptimization(route: Attraction[]): Attraction[] {
  let bestRoute = [...route];
  let bestDistance = calculateTotalDistance(bestRoute);
  let improved = true;

  while (improved) {
    improved = false;

    for (let i = 0; i < bestRoute.length - 1; i++) {
      for (let j = i + 2; j < bestRoute.length; j++) {
        // 尝试交换两条边
        const newRoute = swapEdges(bestRoute, i, j);
        const newDistance = calculateTotalDistance(newRoute);

        if (newDistance < bestDistance) {
          bestRoute = newRoute;
          bestDistance = newDistance;
          improved = true;
        }
      }
    }
  }

  return bestRoute;
}
```

**距离计算**：使用 Haversine 公式计算两点间的球面距离
```typescript
function calculateDistance(point1: Point, point2: Point): number {
  const R = 6371; // 地球半径（公里）
  const dLat = toRadians(point2.lat - point1.lat);
  const dLng = toRadians(point2.lng - point1.lng);

  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
           Math.cos(toRadians(point1.lat)) * Math.cos(toRadians(point2.lat)) *
           Math.sin(dLng / 2) * Math.sin(dLng / 2);

  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
```

### Phase 5：动态预算计算

**目标**：根据城市等级、季节、群体类型动态计算预算分配

**城市等级分类**：
- **一线城市**：北京、上海、广州、深圳
- **二线城市**：杭州、南京、成都、武汉、西安等
- **三线城市**：其他城市

**季节性价格系数**：
```typescript
const SEASONAL_MULTIPLIERS = {
  peak: { accommodation: 1.5, dining: 1.2, tickets: 1.0 },    // 旺季
  shoulder: { accommodation: 1.2, dining: 1.1, tickets: 1.0 }, // 平季
  off: { accommodation: 0.8, dining: 0.9, tickets: 0.9 },     // 淡季
};
```

**预算档次判断**：
```typescript
function getBudgetTier(budgetPerPersonPerDay): 'budget' | 'moderate' | 'luxury' {
  if (budgetPerPersonPerDay < 300) return 'budget';    // 经济型
  if (budgetPerPersonPerDay < 600) return 'moderate';   // 舒适型
  return 'luxury';                                        // 豪华型
}
```

**预算分配公式**：
```
住宿费用 = 天数 × 房间数 × 基础房价 × 季节系数 × 群体系数
餐饮费用 = 天数 × 人数 × 基础餐费 × 季节系数 × 群体系数
交通费用 = 景点间距离估算 × 单位距离费用
门票费用 = 所有景点门票价格之和
```

### Phase 6：IoT 实时检查

**目标**：根据实时数据检查景点状态，排除问题景点

**检查项**：
1. **开放状态**：景点是否开放
2. **拥挤度**：是否超出阈值
3. **天气状况**：是否适合游览（针对户外景点）

**排除策略**：
- 家庭游客：拥挤度阈值70%，降雨概率阈值60%
- 其他群体：拥挤度阈值90%，降雨概率阈值80%

**保护机制**：
- 如果所有景点都被排除，保留原始行程
- 如果超过50%景点被排除，保留部分景点
- 将IoT警告信息添加到响应中

### Phase 7：备选景点池生成

**目标**：为每个景点生成备选方案

**生成规则**：
1. 选择与主景点有相同类别标签的景点
2. 按综合评分排序
3. 取前3个作为备选

**替换策略**：
- 当主景点被排除时，自动用备选景点替换
- 优先选择评分最高的备选景点

---

## 📊 算法性能

### 时间复杂度

| 算法步骤 | 时间复杂度 | 说明 |
|---------|-----------|------|
| 景点评分 | O(n) | n为景点数量 |
| K-means聚类 | O(n × k × i) | k为聚类数，i为迭代次数 |
| 多样性约束 | O(n × m) | m为约束规则数 |
| 2-opt优化 | O(n²) | n为景点数量 |
| IoT检查 | O(n) | n为景点数量 |

### 空间复杂度

| 算法步骤 | 空间复杂度 | 说明 |
|---------|-----------|------|
| 景点评分 | O(n) | 存储评分结果 |
| K-means聚类 | O(n × k) | 存储聚类结果 |
| 2-opt优化 | O(n) | 存储路线 |
| 备选景点池 | O(n × 3) | 每个景点3个备选 |

---

## 🎯 算法优化效果

### 多样性提升
- **优化前**：推荐相似景点，缺乏多样性
- **优化后**：每天至少2种不同类型，同类型不超过50%

### 效率提升
- **优化前**：贪心算法，平均行程距离 10.5km
- **优化后**：2-opt算法，平均行程距离 8.4km（减少20%）

### 智能度提升
- **优化前**：单一评分维度
- **优化后**：多因素综合评分，考虑用户偏好、景点质量、实时状况

### 健壮性提升
- **优化前**：无异常处理
- **优化后**：完善的异常处理和回退机制

---

## 🔧 参数调优

### 可调参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 评分权重 | 0.35/0.25/0.25/0.15 | 偏好/质量/IoT/人群权重 |
| K-means迭代次数 | 100 | 最大迭代次数 |
| 同类型限制 | 2 | 每天最多同类型景点数 |
| 总体限制 | 0.5 | 同类型景点占总量的最大比例 |
| 2-opt迭代次数 | 100 | 最大迭代次数 |

### 调优建议

1. **权重调整**：根据用户反馈调整各维度权重
2. **阈值调整**：根据实际使用情况调整IoT检查阈值
3. **聚类数量**：根据行程天数自动调整

---

## 📈 未来优化方向

### 短期优化
1. 增加更多用户画像维度
2. 优化K-means初始中心选择
3. 增加用户反馈学习机制

### 中期优化
1. 引入机器学习模型优化评分
2. 实现动态路线调整
3. 增加个性化推荐

### 长期优化
1. 实现深度学习路径规划
2. 集成更多实时数据源
3. 构建用户行为预测模型

---

## 📚 参考资料

1. K-means聚类算法
2. 2-opt路径优化算法
3. Haversine公式
4. Jaccard相似度系数

---

**LiveTrip 算法团队** 🚀
