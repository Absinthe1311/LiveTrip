# LiveTrip 智能旅行规划系统 - 项目交接文档

## 📋 项目概述

**项目名称：** LiveTrip - AI + IoT 智能旅行规划系统

**项目描述：** 基于人工智能和物联网技术的智能行程规划系统，能够根据用户偏好、实时物联网数据动态优化旅行行程。

**项目版本：** v1.0.0

**最后更新：** 2026-03-14

---

## 🛠 技术栈

### 前端技术栈

- **框架**: React 18.3.1
- **语言**: TypeScript 5.7.3
- **构建工具**: Vite 6.0.7
- **UI组件库**: Ant Design 5.22.5
- **状态管理**: Zustand 5.0.2
- **拖拽**: @dnd-kit/core 6.3.1, @dnd-kit/sortable 10.0.0
- **图表**: echarts 6.0.0, echarts-for-react 3.0.6
- **地图**: 高德地图 API (@amap/amap-jsapi-loader 1.0.1)
- **HTTP客户端**: Axios 1.7.9
- **日期处理**: dayjs 1.11.13
- **Markdown编辑**: @uiw/react-md-editor 4.0.4

### 后端技术栈

- **运行环境**: Node.js
- **框架**: Express 4.21.2
- **语言**: TypeScript 5.7.3
- **ORM**: Prisma 6.1.0
- **数据库**: SQLite 3.x
- **图片存储**: Cloudinary
- **API服务**: 高德地图 API, 智谱AI (ChatGLM), Unsplash
- **HTTP客户端**: Axios 1.13.4
- **认证**: bcryptjs 3.0.3, jsonwebtoken 9.0.3
- **安全**: helmet 8.0.0, cors 2.8.5
- **日志**: morgan 1.10.0
- **验证**: express-validator 7.2.1
- **API文档**: swagger-jsdoc 6.2.8, swagger-ui-express 5.0.1
- **开发工具**: nodemon 3.1.9, ts-node 10.9.2

### 开发工具

- **包管理器**: npm
- **代码规范**: ESLint 9.17.0, Prettier 3.4.2
- **类型检查**: TypeScript 5.7.3
- **数据库管理**: Prisma Studio

---

## 📁 项目结构

```
LiveTrip/
├── backend/                          # 后端项目
│   ├── src/                          # 源代码目录
│   │   ├── controllers/              # 控制器层
│   │   │   ├── adminController.ts    # 管理员控制器
│   │   │   ├── advisorController.ts  # 旅行顾问控制器
│   │   │   ├── authController.ts     # 认证控制器
│   │   │   ├── blogController.ts     # 游记控制器
│   │   │   ├── destinationController.ts # 目的地控制器
│   │   │   ├── favoriteController.ts # 收藏控制器
│   │   │   ├── imageController.ts    # 图片控制器
│   │   │   ├── iotController.ts      # IoT数据控制器
│   │   │   ├── locationCacheController.ts # 位置缓存控制器
│   │   │   ├── planController.ts     # 行程规划控制器
│   │   │   ├── recommendationController.ts # 推荐控制器
│   │   │   ├── reviewController.ts   # 审核控制器
│   │   │   ├── shareController.ts    # 分享控制器
│   │   │   ├── spotSyncController.ts # 景点同步控制器
│   │   │   └── tripController.ts     # 行程管理控制器
│   │   ├── services/                 # 服务层
│   │   │   ├── advisorService.ts     # 旅行顾问服务
│   │   │   ├── aiRecommender.ts      # AI推荐服务
│   │   │   ├── alternativeRecommender.ts # 备选景点推荐服务
│   │   │   ├── amapPOICacheService.ts # 高德POI缓存服务
│   │   │   ├── amapService.ts        # 高德地图服务
│   │   │   ├── blogService.ts        # 游记服务
│   │   │   ├── budgetCalculator.ts   # 预算计算服务
│   │   │   ├── cloudinaryService.ts  # Cloudinary图片服务
│   │   │   ├── destinationCacheService.ts # 目的地缓存服务
│   │   │   ├── favoriteService.ts    # 收藏服务
│   │   │   ├── hotelRecommender.ts   # 酒店推荐服务
│   │   │   ├── imageService.ts       # 图片服务
│   │   │   ├── itineraryAdjustService.ts # 行程调整服务
│   │   │   ├── locationCacheService.ts # 地点缓存服务
│   │   │   ├── planService.ts        # 规划服务
│   │   │   ├── restaurantRecommender.ts # 餐厅推荐服务
│   │   │   ├── routeOptimizer.ts     # 路线优化服务
│   │   │   └── spotService.ts        # 景点数据服务
│   │   ├── routes/                   # 路由层
│   │   │   ├── adminRoutes.ts        # 管理员路由
│   │   │   ├── advisorRoutes.ts      # 旅行顾问路由
│   │   │   ├── authRoutes.ts         # 认证路由
│   │   │   ├── blogRoutes.ts         # 游记路由
│   │   │   ├── destinationRoutes.ts  # 目的地路由
│   │   │   ├── favoriteRoutes.ts     # 收藏路由
│   │   │   ├── imageRoutes.ts        # 图片路由
│   │   │   ├── index.ts              # 路由聚合
│   │   │   ├── iotRoutes.ts          # IoT数据路由
│   │   │   ├── locationCacheRoutes.ts # 位置缓存路由
│   │   │   ├── planRoutes.ts         # 行程规划路由
│   │   │   ├── recommendationRoutes.ts # 推荐路由
│   │   │   ├── reviewRoutes.ts       # 审核路由
│   │   │   ├── shareRoutes.ts        # 分享路由
│   │   │   ├── spotRoutes.ts         # 景点路由
│   │   │   ├── spotSyncRoutes.ts     # 景点同步路由
│   │   │   └── tripRoutes.ts         # 行程管理路由
│   │   ├── middleware/               # 中间件
│   │   │   ├── errorHandler.ts       # 错误处理中间件
│   │   │   ├── fileUploadMiddleware.ts # 文件上传中间件
│   │   │   └── validator.ts          # 验证中间件
│   │   ├── config/                   # 配置文件
│   │   │   └── cloudinary.ts         # Cloudinary配置
│   │   ├── iot/                      # IoT相关
│   │   │   ├── iotDataGenerator.ts   # IoT数据生成器
│   │   │   └── iotSimulator.ts       # IoT模拟器
│   │   ├── types/                    # 类型定义
│   │   │   └── index.ts              # 通用类型定义
│   │   ├── utils/                    # 工具函数
│   │   │   ├── apiRateLimiter.ts     # API限流器
│   │   │   └── index.ts              # 工具函数
│   │   └── index.ts                  # 后端入口文件
│   ├── prisma/                       # Prisma配置
│   │   ├── schema.prisma             # 数据库模型定义
│   │   ├── seed.ts                   # 种子数据脚本
│   │   ├── migrations/               # 数据库迁移文件
│   │   └── dev.db                    # SQLite数据库文件
│   ├── scripts/                      # 脚本文件
│   │   ├── apply-migration.js        # 应用迁移脚本
│   │   └── create-admin.js           # 创建管理员脚本
│   ├── dist/                         # 编译输出目录
│   ├── package.json                  # 后端依赖配置
│   ├── tsconfig.json                 # TypeScript配置
│   ├── .env                          # 环境变量
│   └── .env.example                  # 环境变量示例
│
├── frontend/                         # 前端项目
│   ├── src/                          # 源代码目录
│   │   ├── pages/                    # 页面组件
│   │   │   ├── Auth.tsx              # 认证页面
│   │   │   ├── Blogs.tsx             # 游记列表页
│   │   │   ├── CreateBlog.tsx        # 创建游记页
│   │   │   ├── DestinationDetail.tsx # 目的地详情页
│   │   │   ├── Destinations.tsx      # 目的地列表页
│   │   │   ├── Favorites.tsx         # 收藏页
│   │   │   ├── Home.tsx              # 首页
│   │   │   ├── Itinerary.tsx         # 行程页面
│   │   │   ├── MyTrips.tsx           # 我的行程页
│   │   │   ├── Plan.tsx              # 规划页面
│   │   │   ├── SharedTrip.tsx        # 分享行程页
│   │   │   └── admin/                # 管理员页面
│   │   │       ├── SpotManagePage.tsx # 景点管理页
│   │   │       └── ReviewPage.tsx    # 图片审核页
│   │   ├── components/               # 公共组件
│   │   │   ├── admin/                # 管理员组件
│   │   │   │   ├── AdminGuard.tsx    # 管理员路由守卫
│   │   │   │   └── AdminLayout.tsx   # 管理员布局
│   │   │   ├── AlternativeAttractions.tsx # 备选景点组件
│   │   │   ├── AttractionCard.tsx    # 景点卡片组件
│   │   │   ├── BudgetChart.tsx       # 预算图表组件
│   │   │   ├── BudgetRangeSlider.tsx # 预算范围滑块
│   │   │   ├── HotelRecommendations.tsx # 酒店推荐组件
│   │   │   ├── RestaurantRecommendations.tsx # 餐厅推荐组件
│   │   │   ├── ShareButton.tsx       # 分享按钮组件
│   │   │   ├── PDFExportButton.tsx   # PDF导出按钮
│   │   │   ├── SpotImageUploadModal.tsx # 图片上传弹窗
│   │   │   └── ...                   # 其他组件
│   │   ├── api/                      # API客户端
│   │   │   ├── client.ts             # API封装
│   │   │   ├── adminApi.ts           # 管理员API
│   │   │   └── recommendationApi.ts  # 推荐API
│   │   ├── services/                 # 前端服务
│   │   │   ├── alternativeRecommender.ts # 备选景点推荐服务
│   │   │   └── api.ts                # API服务
│   │   ├── store/                    # 状态管理
│   │   │   └── index.ts              # Zustand Store
│   │   ├── types/                    # 类型定义
│   │   │   ├── admin.ts              # 管理员类型
│   │   │   ├── destination.ts        # 目的地类型
│   │   │   └── index.ts              # 通用类型定义
│   │   ├── App.tsx                   # 应用主组件
│   │   └── main.tsx                  # 应用入口
│   ├── package.json                  # 前端依赖配置
│   ├── vite.config.ts                # Vite配置
│   ├── .env                          # 环境变量
│   └── .env.example                  # 环境变量示例
│
├── image/                            # 图片资源
│   └── 前端后端集成测试指南/          # 测试截图
│
├── README.md                         # 项目说明文档
├── 使用说明文档.md                    # 使用说明文档
├── 项目交接文档.md                    # 项目交接文档
└── 智能旅行规划系统_产品设计书.docx   # 产品设计文档
```

---

## 📊 数据库设计

### 数据库模型（共15张表）

#### 1. User - 用户表
存储用户基本信息，包含角色字段区分普通用户和管理员。

#### 2. UserPreferences - 用户偏好表
存储用户旅行偏好设置。

#### 3. Trip - 行程表
存储行程基本信息、酒店信息和状态。

#### 4. Day - 天数表
存储每天的行程和餐厅信息。

#### 5. ItineraryItem - 行程项目表
存储每个景点的详细信息。

#### 6. Budget - 预算表
存储行程预算分配。

#### 7. Spot - 景点表
存储从高德API获取的景点信息。

#### 8. SpotIoTData - IoT数据表
存储每个景点的IoT实时数据。

#### 9. SpotAlternative - 备选关系表
存储景点之间的备选关系。

#### 10. SpotImage - 景点图片表
存储景点图片信息，包含审核状态。

#### 11. Favorite - 收藏表
存储用户收藏的景点。

#### 12. Blog - 游记表
存储用户发布的游记。

#### 13. AmapPOICache - 高德POI缓存表
缓存高德地图POI数据。

#### 14. LocationCache - 位置缓存表
缓存出发地和目的地的搜索结果。

#### 15. DestinationCache - 目的地缓存表
缓存目的地热门景点数据。

---

## ✅ 已实现的功能

### 1. 智能行程规划
- 基于用户偏好生成个性化行程
- 集成智谱AI（ChatGLM）进行智能推荐
- 集成高德地图API获取景点信息
- 支持fallback规则引擎

### 2. 备选景点推荐
- 为每个景点推荐1-2个备选景点
- 排除行程中已有的景点
- 替换后动态更新备选关系

### 3. IoT数据展示
- 为每个景点生成独立的IoT数据
- 显示天气、人流、开放状态

### 4. 数据缓存优化
- 缓存出发地和目的地搜索结果
- 缓存高德POI数据
- 缓存有效期24小时

### 5. 拖拽排序
- 使用@dnd-kit实现拖拽功能
- 自动重新计算时间段

### 6. 地图可视化
- 使用高德地图API显示行程地图
- 显示景点、酒店、餐厅标记

### 7. 预算管理
- 自动计算和分配旅行预算
- 实时预算计算和预警

### 8. 酒店推荐
- 基于目的地推荐酒店
- 支持选择并保存到行程

### 9. 餐厅推荐
- 基于景点位置推荐餐厅
- 支持选择并保存到行程

### 10. 收藏功能
- 收藏喜欢的景点
- 查看收藏列表

### 11. 用户认证
- 用户注册和登录
- JWT认证
- 角色区分（普通用户/管理员）

### 12. 行程分享
- 生成分享链接
- PDF导出功能

### 13. 游记功能
- Markdown编辑器
- 发布和管理游记

### 14. 图片上传
- 用户上传景点图片
- Cloudinary云存储
- 图片大小限制20MB

### 15. 管理员后台
- 景点图片管理
- 图片审核功能
- 管理员直接上传图片

---

## 🔐 环境变量

### 后端环境变量 (.env)

```env
# 服务器配置
PORT=3003
NODE_ENV=development

# 数据库配置
DATABASE_URL="file:./dev.db"

# 高德地图 API 配置
AMAP_API_KEY=your_amap_api_key_here

# 智谱AI配置
ZHIPUAI_API_KEY=your_zhipuai_api_key_here

# Cloudinary 图片存储配置
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Unsplash API 配置（可选）
UNSPLASH_ACCESS_KEY=your_unsplash_key

# CORS 配置
CORS_ORIGIN=http://localhost:5174
```

### 前端环境变量 (.env)

```env
# 高德地图 Key
VITE_AMAP_KEY=your_amap_api_key_here
VITE_AMAP_JS_KEY=your_amap_js_key_here
VITE_AMAP_JS_SECRET=your_amap_js_secret_here

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3003/api
```

---

## 📝 API接口文档

### 认证相关
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录

### 行程相关
- `POST /api/plan` - 创建行程规划
- `POST /api/trips` - 保存行程
- `GET /api/trips` - 获取用户行程列表
- `GET /api/trips/:id` - 获取行程详情
- `DELETE /api/trips/:id` - 删除行程
- `POST /api/trips/:id/complete` - 完成行程
- `POST /api/trips/calculate-budget` - 计算预算

### 景点相关
- `GET /api/spots/alternatives/:spotId` - 获取备选景点
- `POST /api/spots/alternatives/update` - 更新备选关系
- `POST /api/spots/search` - 搜索景点

### 图片相关
- `POST /api/images/upload` - 上传图片
- `GET /api/images/spot/:spotId` - 获取景点图片

### 管理员相关
- `GET /api/admin/spots` - 获取景点列表（含图片统计）
- `GET /api/admin/spots/:spotId/images` - 获取景点图片详情
- `GET /api/admin/images/pending` - 获取待审核图片
- `POST /api/admin/images/:imageId/review` - 审核图片

### 分享相关
- `POST /api/share/create` - 创建分享链接
- `GET /api/share/:token` - 获取分享的行程

### 游记相关
- `GET /api/blogs` - 获取游记列表
- `POST /api/blogs` - 创建游记
- `GET /api/blogs/:id` - 获取游记详情

---

## 🚀 部署指南

### 开发环境部署

1. 克隆项目
```bash
git clone <repository-url>
cd LiveTrip
```

2. 安装依赖
```bash
# 后端
cd backend
npm install

# 前端
cd frontend
npm install
```

3. 配置环境变量

参考"环境变量"章节配置`.env`文件

4. 初始化数据库
```bash
cd backend
npx prisma generate
npx prisma migrate dev
npx ts-node prisma/seed.ts  # 创建管理员账号
```

5. 启动服务
```bash
# 后端
cd backend
npm run dev

# 前端
cd frontend
npm run dev
```

### 生产环境部署

#### 后端部署
```bash
cd backend
npm run build
npm start
```

#### 前端部署
```bash
cd frontend
npm run build
# 将 dist 目录部署到静态文件服务器
```

---

## 🐛 常见问题

### Q1: 数据库连接失败
**A**: 检查`DATABASE_URL`配置是否正确，数据库文件是否存在

### Q2: 高德地图API调用失败
**A**: 检查`AMAP_API_KEY`是否正确配置，网络连接是否正常

### Q3: 图片上传失败
**A**: 检查Cloudinary配置是否正确，图片大小是否超过20MB

### Q4: 管理员登录后跳转错误
**A**: 确保用户角色为`admin`，检查数据库中用户的`role`字段

### Q5: IoT数据不更新
**A**: IoT数据是服务器内存数据，重启服务器后会重新生成

---

## 📚 参考资料

- [React官方文档](https://react.dev/)
- [Ant Design文档](https://ant.design/)
- [Prisma文档](https://www.prisma.io/docs)
- [高德地图API文档](https://lbs.amap.com/api/)
- [Zustand文档](https://github.com/pmndrs/zustand)
- [dnd-kit文档](https://docs.dndkit.com/)
- [智谱AI文档](https://open.bigmodel.cn/)
- [Cloudinary文档](https://cloudinary.com/documentation)

---

**文档版本**: 4.0
**最后更新**: 2026-03-14
**系统版本**: LiveTrip v1.0.0
