# LiveTrip 智能旅行规划系统 - 项目交接文档

## 📋 项目概述

**项目名称：** LiveTrip - AI + IoT 智能旅行规划系统

**项目描述：** 基于人工智能和物联网技术的智能行程规划系统，能够根据用户偏好、实时物联网数据动态优化旅行行程。

**项目版本：** v1.0.0

**最后更新：** 2026-03-05

---

## 🛠 技术栈

### 前端技术栈

- **框架**: React 18.3.1
- **语言**: TypeScript 5.7.3
- **构建工具**: Vite 6.0.7
- **UI组件库**: Ant Design 5.22.5
- **状态管理**: Zustand 5.0.2
- **拖拽**: @dnd-kit/core 6.3.1, @dnd-kit/sortable 10.0.0
- **图表**: echarts 6.0.0, echarts-for-react 3.0.6
- **地图**: 高德地图 API (@amap/amap-jsapi-loader 1.0.1), Mapbox GL 3.18.1
- **HTTP客户端**: Axios 1.7.9
- **日期处理**: dayjs 1.11.13

### 后端技术栈

- **运行环境**: Node.js
- **框架**: Express 4.21.2
- **语言**: TypeScript 5.7.3
- **ORM**: Prisma 6.1.0
- **数据库**: SQLite 3.x
- **API服务**: 高德地图 API, 智谱AI (ChatGLM)
- **HTTP客户端**: Axios 1.13.4
- **认证**: bcryptjs 3.0.3, jsonwebtoken 9.0.3
- **安全**: helmet 8.0.0, cors 2.8.5
- **日志**: morgan 1.10.0
- **验证**: express-validator 7.2.1
- **API文档**: swagger-jsdoc 6.2.8, swagger-ui-express 5.0.1
- **开发工具**: nodemon 3.1.9, ts-node 10.9.2

### 开发工具

- **包管理器**: npm
- **代码规范**: ESLint 9.17.0, Prettier 3.4.2
- **类型检查**: TypeScript 5.7.3
- **数据库管理**: Prisma Studio

---

## 📁 项目结构

```
LiveTrip/
├── backend/                          # 后端项目
│   ├── src/                          # 源代码目录
│   │   ├── controllers/              # 控制器层（10个文件）
│   │   │   ├── advisorController.ts  # 旅行顾问控制器
│   │   │   ├── authController.ts     # 认证控制器
│   │   │   ├── destinationController.ts # 目的地控制器
│   │   │   ├── favoriteController.ts # 收藏控制器
│   │   │   ├── iotController.ts      # IoT数据控制器
│   │   │   ├── locationCacheController.ts # 位置缓存控制器
│   │   │   ├── planController.ts     # 行程规划控制器
│   │   │   ├── recommendationController.ts # 推荐控制器
│   │   │   ├── spotSyncController.ts # 景点同步控制器
│   │   │   └── tripController.ts     # 行程管理控制器
│   │   ├── services/                 # 服务层（15个文件）
│   │   │   ├── advisorService.ts     # 旅行顾问服务
│   │   │   ├── aiRecommender.ts      # AI推荐服务
│   │   │   ├── alternativeRecommender.ts # 备选景点推荐服务
│   │   │   ├── amapPOICacheService.ts # 高德POI缓存服务
│   │   │   ├── amapService.ts        # 高德地图服务
│   │   │   ├── budgetCalculator.ts   # 预算计算服务
│   │   │   ├── destinationCacheService.ts # 目的地缓存服务
│   │   │   ├── favoriteService.ts    # 收藏服务
│   │   │   ├── hotelRecommender.ts   # 酒店推荐服务
│   │   │   ├── itineraryAdjustService.ts # 行程调整服务
│   │   │   ├── locationCacheService.ts # 地点缓存服务
│   │   │   ├── planService.ts        # 规划服务
│   │   │   ├── restaurantRecommender.ts # 餐厅推荐服务
│   │   │   ├── routeOptimizer.ts     # 路线优化服务
│   │   │   └── spotService.ts        # 景点数据服务
│   │   ├── iot/                      # IoT相关
│   │   │   ├── iotDataGenerator.ts   # IoT数据生成器
│   │   │   └── iotSimulator.ts       # IoT模拟器
│   │   ├── routes/                   # 路由层（12个文件）
│   │   │   ├── advisorRoutes.ts      # 旅行顾问路由
│   │   │   ├── authRoutes.ts         # 认证路由
│   │   │   ├── destinationRoutes.ts  # 目的地路由
│   │   │   ├── favoriteRoutes.ts     # 收藏路由
│   │   │   ├── index.ts              # 路由聚合
│   │   │   ├── iotRoutes.ts          # IoT数据路由
│   │   │   ├── locationCacheRoutes.ts # 位置缓存路由
│   │   │   ├── planRoutes.ts         # 行程规划路由
│   │   │   ├── recommendationRoutes.ts # 推荐路由
│   │   │   ├── spotRoutes.ts         # 景点路由
│   │   │   ├── spotSyncRoutes.ts     # 景点同步路由
│   │   │   └── tripRoutes.ts         # 行程管理路由
│   │   ├── middleware/               # 中间件
│   │   │   ├── errorHandler.ts       # 错误处理中间件
│   │   │   └── validator.ts          # 验证中间件
│   │   ├── models/                   # 数据模型
│   │   │   └── index.ts              # Prisma客户端
│   │   ├── types/                    # 类型定义
│   │   │   └── index.ts              # 通用类型定义
│   │   ├── utils/                    # 工具函数
│   │   │   ├── apiRateLimiter.ts     # API限流器
│   │   │   └── index.ts              # 工具函数
│   │   ├── data/                     # 数据文件
│   │   │   └── alternativeSpots.ts   # 备选景点数据
│   │   └── index.ts                  # 后端入口文件
│   ├── prisma/                       # Prisma配置
│   │   ├── schema.prisma             # 数据库模型定义
│   │   ├── migrations/               # 数据库迁移文件（11个迁移）
│   │   └── dev.db                    # SQLite数据库文件
│   ├── dist/                         # 编译输出目录
│   ├── node_modules/                 # 依赖包
│   ├── package.json                  # 后端依赖配置
│   ├── tsconfig.json                 # TypeScript配置
│   ├── .eslintrc.cjs                 # ESLint配置
│   ├── .prettierrc                   # Prettier配置
│   ├── .env                          # 环境变量
│   └── .env.example                  # 环境变量示例
│
├── frontend/                         # 前端项目
│   ├── src/                          # 源代码目录
│   │   ├── pages/                    # 页面组件（8个文件）
│   │   │   ├── Auth.tsx              # 认证页面
│   │   │   ├── DestinationDetail.tsx # 目的地详情页
│   │   │   ├── Destinations.tsx      # 目的地列表页
│   │   │   ├── Favorites.tsx         # 收藏页
│   │   │   ├── Home.tsx              # 首页
│   │   │   ├── Itinerary.tsx         # 行程页面
│   │   │   ├── MyTrips.tsx           # 我的行程页
│   │   │   └── Plan.tsx              # 规划页面
│   │   ├── components/               # 公共组件（21个文件）
│   │   │   ├── AlternativeAttractions.tsx # 备选景点组件
│   │   │   ├── AttractionCard.tsx    # 景点卡片组件
│   │   │   ├── BudgetChart.tsx       # 预算图表组件
│   │   │   ├── BudgetRangeSlider.tsx # 预算范围滑块
│   │   │   ├── CompactProgressBar.tsx # 紧凑进度条
│   │   │   ├── CoreFeatures.tsx      # 核心功能组件
│   │   │   ├── DateRangePicker.tsx   # 日期范围选择器
│   │   │   ├── DestinationAttractionCard.tsx # 目的地景点卡片
│   │   │   ├── FavoritesList.tsx     # 收藏列表组件
│   │   │   ├── Footer.tsx            # 页脚组件
│   │   │   ├── HeroSection.tsx       # 英雄区域组件
│   │   │   ├── HotelRecommendations.tsx # 酒店推荐组件
│   │   │   ├── LocationSearch.tsx    # 地点搜索组件
│   │   │   ├── Navbar.tsx            # 导航栏组件
│   │   │   ├── PageHeader.tsx        # 页面头部组件
│   │   │   ├── PopularDestinations.tsx # 热门目的地组件
│   │   │   ├── PreferenceCards.tsx   # 偏好卡片组件
│   │   │   ├── QuickActions.tsx      # 快速操作组件
│   │   │   ├── RestaurantRecommendations.tsx # 餐厅推荐组件
│   │   │   ├── TravelAdvisor.tsx     # 旅行顾问组件
│   │   │   └── index.ts              # 组件导出
│   │   ├── api/                      # API客户端（2个文件）
│   │   │   ├── client.ts             # API封装
│   │   │   └── recommendationApi.ts  # 推荐API
│   │   ├── services/                 # 前端服务（2个文件）
│   │   │   ├── alternativeRecommender.ts # 备选景点推荐服务
│   │   │   └── api.ts                # API服务
│   │   ├── store/                    # 状态管理
│   │   │   └── index.ts              # Zustand Store
│   │   ├── types/                    # 类型定义（2个文件）
│   │   │   ├── destination.ts        # 目的地类型
│   │   │   └── index.ts              # 通用类型定义
│   │   ├── hooks/                    # 自定义Hooks
│   │   │   └── index.ts              # Hooks导出
│   │   ├── utils/                    # 工具函数
│   │   │   └── index.ts              # 工具函数
│   │   ├── data/                     # 数据文件（3个文件）
│   │   │   ├── alternativeSpots.ts   # 备选景点数据
│   │   │   └── destinationsData.ts   # 目的地数据
│   │   ├── assets/                   # 静态资源
│   │   │   └── react.svg             # React图标
│   │   ├── App.tsx                   # 应用主组件
│   │   ├── App.css                   # 应用样式
│   │   ├── main.tsx                  # 应用入口
│   │   ├── index.css                 # 全局样式
│   │   └── vite-env.d.ts             # Vite类型定义
│   ├── public/                       # 公共资源
│   │   └── vite.svg                  # Vite图标
│   ├── dist/                         # 构建输出目录
│   ├── node_modules/                 # 依赖包
│   ├── package.json                  # 前端依赖配置
│   ├── vite.config.ts                # Vite配置
│   ├── tsconfig.json                 # TypeScript配置
│   ├── tsconfig.app.json             # 应用TS配置
│   ├── tsconfig.node.json            # Node TS配置
│   ├── .eslintrc.cjs                 # ESLint配置
│   ├── .prettierrc                   # Prettier配置
│   ├── .env                          # 环境变量
│   ├── .env.example                  # 环境变量示例
│   └── index.html                    # HTML入口
│
├── image/                            # 图片资源
│   └── 前端后端集成测试指南/          # 测试截图
│       ├── 1770263876634.png
│       └── 1770263882370.png
│
├── .gitignore                        # Git忽略配置
├── package-lock.json                 # 根目录依赖锁定
├── README.md                         # 项目说明文档
├── 使用说明文档.md                    # 使用说明文档
├── 项目交接文档.md                    # 项目交接文档
└── 智能旅行规划系统_产品设计书.docx   # 产品设计文档
```

---

## 📊 数据库设计

### 数据库模型（共12张表）

#### 1. User - 用户表

```prisma
model User {
  id            String    @id @default(cuid())
  username      String    @unique
  email         String?   @unique
  passwordHash  String
  avatar        String?   @default("")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  trips         Trip[]
  preferences   UserPreferences?
}
```

**作用**: 存储用户基本信息

#### 2. UserPreferences - 用户偏好表

```prisma
model UserPreferences {
  id              String   @id @default(cuid())
  userId          String   @unique
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  travelStyle     String   @default("balanced")
  budgetRange     String   @default("medium")
  accommodation   String   @default("hotel")
  transportation  String   @default("mixed")
  dietaryPrefs    String   @default("")
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

**作用**: 存储用户旅行偏好设置

#### 3. Trip - 行程表

```prisma
model Trip {
  id              String    @id @default(cuid())
  userId          String
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  title           String
  description     String?   @default("")
  destination     String
  startDate       DateTime
  endDate         DateTime
  status          String    @default("planning")
  
  totalBudget     Float     @default(0)
  actualBudget    Float     @default(0)
  budgetStatus    String    @default("under_budget")
  
  aiGenerated     Boolean   @default(true)
  
  hotelName       String?
  hotelAddress    String?
  hotelLocation   String?
  hotelTel        String?
  hotelType       String?
  hotelRating     Float?
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  days            Day[]
  budget          Budget?
}
```

**作用**: 存储行程基本信息和酒店信息

#### 4. Day - 天数表

```prisma
model Day {
  id              String    @id @default(cuid())
  tripId          String
  trip            Trip      @relation(fields: [tripId], references: [id], onDelete: Cascade)
  
  dayNumber       Int
  date            DateTime
  notes           String?   @default("")
  
  restaurantName       String?
  restaurantAddress    String?
  restaurantLocation   String?
  restaurantTel        String?
  restaurantType       String?
  restaurantRating     Float?
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  itineraryItems  ItineraryItem[]
}
```

**作用**: 存储每天的行程和餐厅信息

#### 5. ItineraryItem - 行程项目表

```prisma
model ItineraryItem {
  id              String    @id @default(cuid())
  dayId           String
  day             Day       @relation(fields: [dayId], references: [id], onDelete: Cascade)
  
  name            String
  type            String
  category        String?   @default("")
  description     String?   @default("")
  
  startTime       DateTime
  endTime         DateTime
  
  address         String?   @default("")
  latitude        Float?
  longitude       Float?
  
  cost            Float     @default(0)
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

**作用**: 存储每个景点的详细信息

#### 6. Budget - 预算表

```prisma
model Budget {
  id              String   @id @default(cuid())
  tripId          String   @unique
  trip            Trip     @relation(fields: [tripId], references: [id], onDelete: Cascade)
  
  transportation  Float    @default(0)
  accommodation   Float    @default(0)
  food            Float    @default(0)
  tickets         Float    @default(0)
  shopping        Float    @default(0)
  other           Float    @default(0)
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

**作用**: 存储行程预算分配

#### 7. Spot - 景点表

```prisma
model Spot {
  id              String   @id @default(cuid())
  
  amapId          String   @unique
  name            String
  location        String
  address         String?
  city            String
  category        String?
  
  ticketPrice     Float?   @default(0)
  openTime        String?
  rating          Float?
  description     String?
  isOutdoor       Boolean? @default(true)
  
  source          String   @default("amap")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  iotData         SpotIoTData?
  favorites       Favorite[]
  
  @@index([city])
  @@index([category])
  @@index([amapId])
}
```

**作用**: 存储从高德API获取的景点信息

#### 8. SpotIoTData - IoT数据表

```prisma
model SpotIoTData {
  id              String   @id @default(cuid())
  spotId          String   @unique
  spot            Spot     @relation(fields: [spotId], references: [id], onDelete: Cascade)
  
  crowdLevel      Float
  temperature     Float
  rainProbability Float
  isOpen          Boolean
  
  generatedAt     DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([spotId])
  @@index([generatedAt])
}
```

**作用**: 存储每个景点的IoT实时数据

#### 9. SpotAlternative - 备选关系表

```prisma
model SpotAlternative {
  id               String   @id @default(cuid())
  originalSpotId   String
  alternativeSpotId String
  city             String
  priority         Int      @default(0)
  
  createdAt        DateTime @default(now())
  
  @@unique([originalSpotId, alternativeSpotId])
  @@index([originalSpotId])
  @@index([alternativeSpotId])
  @@index([city])
}
```

**作用**: 存储景点之间的备选关系，避免重复推荐

#### 10. Favorite - 收藏表

```prisma
model Favorite {
  id              String   @id @default(cuid())
  spotId          String
  spot            Spot     @relation(fields: [spotId], references: [id], onDelete: Cascade)
  userId          String   @default("default-user")
  notes           String?  @default("")
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([spotId, userId])
  @@index([userId])
  @@index([spotId])
}
```

**作用**: 存储用户收藏的景点

#### 11. AmapPOICache - 高德POI缓存表

```prisma
model AmapPOICache {
  id              String   @id @default(cuid())
  
  poiId           String   @unique
  name            String
  type            String
  typecode        String
  address         String?
  location        String
  tel             String?
  distance        String?
  
  rating          Float?
  cost            String?
  
  city            String
  
  cacheTime       DateTime @default(now())
  expireTime      DateTime
  
  hitCount        Int      @default(0)
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

**作用**: 缓存高德地图POI数据，减少API调用

#### 12. LocationCache - 位置缓存表

```prisma
model LocationCache {
  id              String   @id @default(cuid())
  
  name            String
  type            String   @default("location")
  category        String?
  address         String?
  city            String?
  province        String?
  
  latitude        Float?
  longitude       Float?
  
  keywords        String
  searchCount     Int      @default(1)
  
  cacheTime       DateTime @default(now())
  expireTime      DateTime @default(now())
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([keywords])
  @@index([name])
}
```

**作用**: 缓存出发地和目的地的搜索结果

#### 13. DestinationCache - 目的地缓存表

```prisma
model DestinationCache {
  id          String   @id @default(cuid())
  city        String   @unique
  attractions String
  expiresAt   DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([city])
  @@index([expiresAt])
}
```

**作用**: 缓存目的地热门景点数据

---

## ✅ 已实现的功能

### 1. 智能行程规划

**文件位置**: `backend/src/services/aiRecommender.ts`

**功能描述**:
- 基于用户偏好生成个性化行程
- 集成智谱AI（ChatGLM）进行智能推荐
- 集成高德地图API获取景点信息
- 自动计算路线距离和优化路线
- 自动分配游览时间
- 支持fallback规则引擎（当AI不可用时）

**API接口**: `POST /api/plan`

### 2. 备选景点推荐

**文件位置**: `backend/src/services/alternativeRecommender.ts`, `backend/src/services/spotService.ts`

**功能描述**:
- 为每个景点推荐1-2个备选景点
- 排除行程中已有的景点
- 优先推荐IoT数据良好的景点
- 每个景点独立推荐，避免重复
- 替换后动态更新备选关系

**API接口**: `GET /api/spots/alternatives/:spotId`

### 3. IoT数据展示

**文件位置**: `backend/src/iot/iotDataGenerator.ts`

**功能描述**:
- 为每个景点生成独立的IoT数据
- 数据具有多样性（不同景点有不同的天气、人流、开放状态）
- 实时更新IoT数据
- 基于真实规律生成数据（时间因子、季节因子、城市特性）

**API接口**: `GET /api/iot/data`

**数据特点**:
- 每个景点有独立的降雨概率（10-70%随机）
- 每个景点有独立的人流水平（带随机偏移）
- 每个景点独立更新IoT数据
- 数据动态变化，模拟真实场景

### 4. 数据缓存优化

**文件位置**: `backend/src/services/locationCacheService.ts`, `backend/src/services/amapPOICacheService.ts`, `backend/src/services/destinationCacheService.ts`

**功能描述**:
- 缓存出发地和目的地搜索结果
- 缓存高德POI数据
- 缓存目的地热门景点
- 缓存有效期24小时
- 减少API调用，提升响应速度

**API接口**: `GET /api/location/search`

### 5. 拖拽排序

**文件位置**: `frontend/src/pages/Itinerary.tsx`

**功能描述**:
- 使用@dnd-kit实现拖拽功能
- 拖拽景点卡片改变顺序
- 自动重新计算时间段
- 考虑景点之间的移动时间
- 自动添加午餐时间

### 6. 地图可视化

**文件位置**: `frontend/src/pages/Itinerary.tsx`

**功能描述**:
- 使用高德地图API显示行程地图
- 显示行程景点标记
- 显示周围餐厅和娱乐场所
- 显示道路、建筑、兴趣点等周围环境
- 交互式地图标记

### 7. 预算管理

**文件位置**: `backend/src/services/budgetCalculator.ts`

**功能描述**:
- 自动计算和分配旅行预算
- 实时预算计算
- 预算预警功能
- 预算分配可视化

**API接口**: `POST /api/trips/calculate-budget`

### 8. 酒店推荐

**文件位置**: `backend/src/services/hotelRecommender.ts`

**功能描述**:
- 基于目的地推荐酒店
- 显示酒店详细信息
- 支持选择酒店并保存到行程

**API接口**: `GET /api/recommendations/hotels`

### 9. 餐厅推荐

**文件位置**: `backend/src/services/restaurantRecommender.ts`

**功能描述**:
- 基于景点位置推荐餐厅
- 显示餐厅详细信息
- 支持选择餐厅并保存到行程

**API接口**: `GET /api/recommendations/restaurants`

### 10. 收藏功能

**文件位置**: `backend/src/services/favoriteService.ts`

**功能描述**:
- 收藏喜欢的景点
- 查看收藏列表
- 添加收藏备注

**API接口**: `POST /api/favorites`, `GET /api/favorites`

### 11. 用户认证

**文件位置**: `backend/src/controllers/authController.ts`

**功能描述**:
- 用户注册和登录
- JWT认证
- 密码加密

**API接口**: `POST /api/auth/register`, `POST /api/auth/login`

---

## 🔧 关键文件说明

### 后端关键文件

| 文件路径 | 作用 |
|---------|------|
| `backend/src/index.ts` | 后端入口文件 |
| `backend/src/controllers/planController.ts` | 行程规划控制器 |
| `backend/src/controllers/tripController.ts` | 行程管理控制器 |
| `backend/src/services/spotService.ts` | 景点数据服务，管理景点和备选关系 |
| `backend/src/services/aiRecommender.ts` | AI推荐服务 |
| `backend/src/services/alternativeRecommender.ts` | 备选景点推荐服务 |
| `backend/src/services/budgetCalculator.ts` | 预算计算服务 |
| `backend/src/iot/iotDataGenerator.ts` | IoT数据生成器 |
| `backend/src/routes/index.ts` | 路由聚合 |
| `backend/prisma/schema.prisma` | 数据库模型定义 |

### 前端关键文件

| 文件路径 | 作用 |
|---------|------|
| `frontend/src/App.tsx` | 应用主组件，路由配置 |
| `frontend/src/main.tsx` | 应用入口 |
| `frontend/src/pages/Itinerary.tsx` | 行程页面主组件 |
| `frontend/src/pages/Home.tsx` | 首页 |
| `frontend/src/pages/Plan.tsx` | 规划页面 |
| `frontend/src/components/AttractionCard.tsx` | 景点卡片组件，显示IoT数据 |
| `frontend/src/components/AlternativeAttractions.tsx` | 备选景点组件 |
| `frontend/src/components/LocationSearch.tsx` | 地点搜索组件，支持缓存 |
| `frontend/src/components/HotelRecommendations.tsx` | 酒店推荐组件 |
| `frontend/src/components/RestaurantRecommendations.tsx` | 餐厅推荐组件 |
| `frontend/src/components/BudgetChart.tsx` | 预算图表组件 |
| `frontend/src/api/client.ts` | API客户端封装 |
| `frontend/src/store/index.ts` | Zustand状态管理 |

---

## 🔐 环境变量

### 后端环境变量 (.env)

```env
# 服务器配置
PORT=3003
NODE_ENV=development

# 数据库配置
DATABASE_URL="file:./dev.db"

# 高德地图 API 配置
AMAP_API_KEY=your_amap_api_key_here

# 智谱AI配置
ZHIPUAI_API_KEY=your_zhipuai_api_key_here

# CORS 配置
CORS_ORIGIN=http://localhost:5174
```

### 前端环境变量 (.env)

```env
# 高德地图 Key
VITE_AMAP_KEY=your_amap_api_key_here
VITE_AMAP_JS_KEY=your_amap_js_key_here
VITE_AMAP_JS_SECRET=your_amap_js_secret_here

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3003/api
```

---

## 📝 API接口文档

### 行程规划相关

#### 1. 创建行程规划

**接口**: `POST /api/plan`

**请求参数**:
```json
{
  "origin": "上海",
  "destination": "北京",
  "start_date": "2026-03-15",
  "end_date": "2026-03-17",
  "budget": 10000,
  "preferences": {
    "interests": "历史文化"
  }
}
```

**响应数据**:
```json
{
  "success": true,
  "data": {
    "itinerary": [...],
    "total_cost": 5000,
    "total_distance": 150.5,
    "summary": {
      "origin": "上海",
      "destination": "北京",
      "days": 3,
      "budget": 10000,
      "start_date": "2026-03-15",
      "end_date": "2026-03-17"
    }
  }
}
```

### 行程管理相关

#### 1. 保存行程

**接口**: `POST /api/trips`

**请求参数**:
```json
{
  "summary": {...},
  "itinerary": {...},
  "total_cost": 5000,
  "budget_breakdown": {...},
  "hotel": {...},
  "restaurants": [...]
}
```

**响应数据**:
```json
{
  "success": true,
  "message": "行程保存成功",
  "data": {
    "tripId": "xxx",
    "budgetInfo": {...}
  }
}
```

#### 2. 获取用户行程列表

**接口**: `GET /api/trips`

**响应数据**:
```json
{
  "success": true,
  "data": [...]
}
```

#### 3. 获取行程详情

**接口**: `GET /api/trips/:id`

**响应数据**:
```json
{
  "success": true,
  "data": {...}
}
```

#### 4. 删除行程

**接口**: `DELETE /api/trips/:id`

**响应数据**:
```json
{
  "success": true,
  "message": "行程删除成功"
}
```

### IoT数据相关

#### 1. 获取IoT数据

**接口**: `GET /api/iot/data`

**响应数据**:
```json
{
  "success": true,
  "data": {
    "timestamp": 1770544495995,
    "spots": [
      {
        "id": "cmlcvp5v80000nuggu1a5e3ij",
        "name": "北京路步行街",
        "crowdLevel": 60,
        "temperature": 24.8,
        "rainProbability": 46,
        "isOpen": true
      }
    ]
  }
}
```

### 景点数据相关

#### 1. 获取备选景点

**接口**: `GET /api/spots/alternatives/:spotId?city=广州&excludeSpotIds=[...]`

**响应数据**:
```json
{
  "success": true,
  "data": [
    {
      "id": "cmlcvp5zl000ynugghpks0fx7",
      "name": "广州十三行博物馆",
      "iotData": {
        "crowdLevel": 56,
        "temperature": 32.7,
        "rainProbability": 23,
        "isOpen": true,
        "healthLevel": "info"
      }
    }
  ],
  "count": 1
}
```

#### 2. 更新备选关系

**接口**: `POST /api/spots/alternatives/update`

**请求参数**:
```json
{
  "oldSpotId": "原景点ID",
  "newSpotId": "新景点ID",
  "city": "广州"
}
```

**响应数据**:
```json
{
  "success": true,
  "message": "备选关系更新成功"
}
```

### 地点缓存相关

#### 1. 搜索地点

**接口**: `GET /api/location/search?keywords=北京`

**响应数据**:
```json
{
  "success": true,
  "data": [
    {
      "value": "北京",
      "label": "北京",
      "icon": "📍",
      "province": "北京",
      "rating": 4.8,
      "address": "中国的首都，历史文化名城"
    }
  ],
  "fromCache": true
}
```

### 预算相关

#### 1. 计算实时预算

**接口**: `POST /api/trips/calculate-budget`

**请求参数**:
```json
{
  "totalBudget": 10000,
  "days": 3,
  "groupSize": 2,
  "hotel": {...},
  "restaurants": {...},
  "spots": [...]
}
```

**响应数据**:
```json
{
  "success": true,
  "data": {
    "total": 8500,
    "status": "under_budget",
    "transportation": 1500,
    "accommodation": 3000,
    "dining": 2000,
    "tickets": 2000,
    "warningLevel": "info",
    "warningMessage": "预算充足，可以放心出行"
  }
}
```

### 推荐相关

#### 1. 获取酒店推荐

**接口**: `GET /api/recommendations/hotels?city=北京`

**响应数据**:
```json
{
  "success": true,
  "data": [...]
}
```

#### 2. 获取餐厅推荐

**接口**: `GET /api/recommendations/restaurants?location=116.397428,39.90923`

**响应数据**:
```json
{
  "success": true,
  "data": [...]
}
```

### 收藏相关

#### 1. 添加收藏

**接口**: `POST /api/favorites`

**请求参数**:
```json
{
  "spotId": "景点ID",
  "notes": "收藏备注"
}
```

**响应数据**:
```json
{
  "success": true,
  "message": "收藏成功"
}
```

#### 2. 获取收藏列表

**接口**: `GET /api/favorites`

**响应数据**:
```json
{
  "success": true,
  "data": [...]
}
```

---

## 🚀 部署指南

### 开发环境部署

1. 克隆项目
```bash
git clone <repository-url>
cd LiveTrip
```

2. 安装依赖
```bash
# 后端
cd backend
npm install

# 前端
cd frontend
npm install
```

3. 配置环境变量

参考"环境变量"章节配置`.env`文件

4. 初始化数据库
```bash
cd backend
npx prisma generate
npx prisma migrate dev
```

5. 启动服务
```bash
# 后端
cd backend
npm run dev

# 前端
cd frontend
npm run dev
```

### 生产环境部署

#### 后端部署

1. 构建项目
```bash
cd backend
npm run build
```

2. 启动服务
```bash
npm start
```

#### 前端部署

1. 构建项目
```bash
cd frontend
npm run build
```

2. 部署静态文件
将`dist`目录部署到静态文件服务器

---

## 🐛 常见问题

### Q1: 数据库连接失败

**A**: 检查`DATABASE_URL`配置是否正确，数据库文件是否存在

### Q2: 高德地图API调用失败

**A**: 检查`AMAP_API_KEY`是否正确配置，网络连接是否正常

### Q3: IoT数据不更新

**A**: IoT数据是服务器内存数据，重启服务器后会重新生成

### Q4: 备选景点为空

**A**: 检查是否已生成景点数据，城市景点数量是否充足

### Q5: 地图不显示

**A**: 检查高德地图Key是否正确配置，景点location字段是否正确

### Q6: AI推荐失败

**A**: 检查`ZHIPUAI_API_KEY`是否正确配置，系统会自动降级使用fallback规则引擎

### Q7: 预算计算不准确

**A**: 检查酒店和餐厅价格信息是否正确，预算计算基于选择的项目

---

## 📚 参考资料

- [React官方文档](https://react.dev/)
- [Ant Design文档](https://ant.design/)
- [Prisma文档](https://www.prisma.io/docs)
- [高德地图API文档](https://lbs.amap.com/api/)
- [Zustand文档](https://github.com/pmndrs/zustand)
- [dnd-kit文档](https://docs.dndkit.com/)
- [智谱AI文档](https://open.bigmodel.cn/)

---

**文档版本**: 3.0
**最后更新**: 2026-03-05
**系统版本**: LiveTrip v1.0.0
