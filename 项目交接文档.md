# LiveTrip 项目交接文档

## 📋 文档说明

本文档用于项目交接，包含项目架构、技术选型、部署说明、维护指南等完整信息。

---

## 📖 目录

- [项目概述](#项目概述)
- [技术架构](#技术架构)
- [数据库设计](#数据库设计)
- [API接口文档](#api接口文档)
- [前端架构](#前端架构)
- [部署说明](#部署说明)
- [维护指南](#维护指南)
- [常见问题](#常见问题)

---

## 🎯 项目概述

### 项目简介

**LiveTrip** 是一个基于 AI + IoT 的智能旅行规划系统，为用户提供个性化的旅行规划服务。

### 核心功能

1. **AI智能规划**：基于智谱AI的智能行程规划
2. **IoT实时数据**：景点拥挤度、天气等实时信息
3. **智能推荐**：景点、酒店、餐厅推荐
4. **行程管理**：拖拽排序、备选景点、分享导出
5. **图片管理**：上传、审核、管理景点图片
6. **游记撰写**：Markdown编辑器撰写游记

### 技术栈

**前端**：React 18.3.1 + TypeScript 5.7.3 + Vite 6.0.7 + Tailwind CSS

**后端**：Node.js + Express 4.21.2 + Prisma 6.1.0 + SQLite

**AI服务**：智谱AI (ChatGLM)

**地图服务**：高德地图 API

---

## 🏗️ 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                      用户界面层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  首页    │  │  规划    │  │  行程    │  ...         │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      前端应用层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  React   │  │ Zustand  │  │  Axios   │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      API网关层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Express │  │   JWT    │  │  CORS    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      业务逻辑层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 行程服务 │  │ 推荐服务 │  │ 图片服务 │  ...         │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      数据访问层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Prisma  │  │  SQLite  │  │Cloudinary│              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      外部服务层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 智谱AI   │  │ 高德地图 │  │   IoT    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 目录结构

```
LiveTrip/
├── frontend/                 # 前端项目
│   ├── src/
│   │   ├── api/             # API接口封装
│   │   │   ├── client.ts    # 主API客户端
│   │   │   ├── adminApi.ts  # 管理员API
│   │   │   └── recommendationApi.ts  # 推荐API
│   │   ├── components/      # React组件
│   │   │   ├── SharedSidebar.tsx  # 侧边栏
│   │   ├── pages/           # 页面组件
│   │   │   ├── Home.tsx     # 首页
│   │   │   ├── Plan.tsx     # 规划页
│   │   │   ├── Itinerary.tsx  # 行程页
│   │   │   ├── TripDetail.tsx  # 行程详情
│   │   │   ├── Today.tsx    # 当前行程
│   │   │   └── admin/       # 管理员页面
│   │   ├── store/           # Zustand状态管理
│   │   ├── services/        # 业务服务
│   │   ├── types/           # TypeScript类型
│   │   └── utils/           # 工具函数
│   ├── public/              # 静态资源
│   ├── .env.example         # 环境变量示例
│   └── package.json
│
├── backend/                  # 后端项目
│   ├── src/
│   │   ├── routes/          # API路由
│   │   │   ├── auth.ts      # 认证路由
│   │   │   ├── trip.ts      # 行程路由
│   │   │   ├── spot.ts      # 景点路由
│   │   │   └── admin.ts     # 管理员路由
│   │   ├── middleware/      # 中间件
│   │   │   ├── auth.ts      # JWT认证
│   │   │   └── admin.ts     # 管理员权限
│   │   ├── services/        # 业务服务
│   │   │   ├── ai.ts        # AI服务
│   │   │   ├── iot.ts       # IoT服务
│   │   │   └── recommendation.ts  # 推荐服务
│   │   └── utils/           # 工具函数
│   ├── prisma/              # 数据库
│   │   ├── schema.prisma    # 数据模型
│   │   └── seed.ts          # 初始数据
│   ├── .env.example         # 环境变量示例
│   └── package.json
│
├── README.md                 # 项目说明
├── 使用说明文档.md           # 用户手册
└── 项目交接文档.md           # 本文档
```

---

## 💾 数据库设计

### Prisma Schema

```prisma
// 用户表
model User {
  id        String   @id @default(uuid())
  username  String   @unique
  email     String   @unique
  password  String
  role      String   @default("user")  // "user" | "admin"
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  trips     Trip[]
  blogs     Blog[]
  favorites Favorite[]
}

// 行程表
model Trip {
  id          String   @id @default(uuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  summary     Json     // 行程概要
  itinerary   Json     // 行程详情
  totalCost   Float
  budgetBreakdown Json
  status      String   @default("planning")  // "planning" | "completed"
  shareToken  String?  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  blogs       Blog[]
}

// 景点表
model Spot {
  id          String   @id @default(uuid())
  name        String
  description String?
  location    String   // 经纬度 "lng,lat"
  category    String?
  ticketPrice Float?
  images      SpotImage[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// 景点图片表
model SpotImage {
  id        String   @id @default(uuid())
  spotId    String
  spot      Spot     @relation(fields: [spotId], references: [id])
  imageUrl  String
  uploaderId String
  status    String   @default("pending")  // "pending" | "approved" | "rejected"
  rejectReason String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// 游记表
model Blog {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  tripId    String?
  trip      Trip?    @relation(fields: [tripId], references: [id])
  title     String
  content   String   // Markdown内容
  images    String[] // 图片URL数组
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// 收藏表
model Favorite {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  type      String   // "spot" | "hotel" | "restaurant"
  itemId    String
  itemData  Json
  createdAt DateTime @default(now())
}
```

### 数据库关系

- User 1:N Trip（一个用户多个行程）
- User 1:N Blog（一个用户多篇游记）
- User 1:N Favorite（一个用户多个收藏）
- Trip 1:N Blog（一个行程多篇游记）
- Spot 1:N SpotImage（一个景点多张图片）

---

## 🔌 API接口文档

### 认证接口

#### POST /api/auth/register
注册新用户

**请求体**：
```json
{
  "username": "string",
  "email": "string",
  "password": "string"
}
```

#### POST /api/auth/login
用户登录

**请求体**：
```json
{
  "email": "string",
  "password": "string"
}
```

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "jwt-token",
    "user": { ... }
  }
}
```

### 行程接口

#### POST /api/trip/generate
生成行程

**请求体**：
```json
{
  "destination": "string",
  "startDate": "string",
  "endDate": "string",
  "budget": number,
  "groupSize": number
}
```

#### POST /api/trip/save
保存行程

**请求体**：
```json
{
  "summary": { ... },
  "itinerary": { ... },
  "total_cost": number,
  "budget_breakdown": { ... },
  "hotel": { ... },
  "restaurants": [ ... ]
}
```

#### GET /api/trip/:id
获取行程详情

#### POST /api/trip/:id/complete
完成行程

#### POST /api/trip/:id/share
生成分享链接

### 景点接口

#### GET /api/spot/search
搜索景点

#### GET /api/spot/:id/images
获取景点图片

#### POST /api/spot/:id/images
上传景点图片

### 管理员接口

#### GET /api/admin/images/pending
获取待审核图片

#### POST /api/admin/images/:id/approve
审核通过

#### POST /api/admin/images/:id/reject
审核拒绝

---

## 🎨 前端架构

### 状态管理（Zustand）

```typescript
// store/index.ts
interface AppState {
  user: User | null;
  currentItinerary: FullItinerary | null;
  setUser: (user: User | null) => void;
  setCurrentItinerary: (itinerary: FullItinerary | null) => void;
}
```

### 路由配置

```typescript
// App.tsx
<Route path="/" element={<Home />} />
<Route path="/auth" element={<Auth />} />
<Route path="/plan" element={<AuthGuard><Plan /></AuthGuard>} />
<Route path="/itinerary" element={<AuthGuard><Itinerary /></AuthGuard>} />
<Route path="/trip/:id" element={<AuthGuard><TripDetail /></AuthGuard>} />
<Route path="/my-trips" element={<AuthGuard><MyTrips /></AuthGuard>} />
<Route path="/today" element={<AuthGuard><Today /></AuthGuard>} />
<Route path="/admin/spots" element={<AdminGuard><SpotManagePage /></AdminGuard>} />
<Route path="/admin/review" element={<AdminGuard><ReviewPage /></AdminGuard>} />
```

### 组件设计

**页面组件**：
- Home：首页
- Plan：行程规划
- Itinerary：行程展示
- TripDetail：行程详情
- Today：当前行程
- MyTrips：我的行程
- Auth：登录注册

**共享组件**：
- SharedSidebar：侧边栏
- ShareButton：分享按钮
- PDFExportButton：PDF导出按钮
- SpotImageUploadModal：图片上传弹窗

---

## 🚀 部署说明

### 开发环境部署

#### 1. 环境准备

```bash
# 安装Node.js (>= 18.0.0)
node --version

# 安装npm (>= 9.0.0)
npm --version
```

#### 2. 项目安装

```bash
# 克隆项目
git clone https://github.com/your-username/livetrip.git
cd livetrip

# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
npm install
```

#### 3. 环境配置

**后端配置**：
```bash
cd backend
cp .env.example .env
# 编辑.env文件，填入真实配置
```

**前端配置**：
```bash
cd ../frontend
cp .env.example .env
# 编辑.env文件，填入真实配置
```

#### 4. 数据库初始化

```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
```

#### 5. 启动服务

```bash
# 启动后端（终端1）
cd backend
npm run dev

# 启动前端（终端2）
cd frontend
npm run dev
```

### 生产环境部署

#### 1. 构建项目

```bash
# 构建前端
cd frontend
npm run build

# 构建后端
cd ../backend
npm run build
```

#### 2. 部署前端

**方式1：静态文件部署**
- 将 `frontend/dist` 目录部署到静态服务器
- 配置Nginx或Apache

**方式2：Vercel/Netlify部署**
- 连接GitHub仓库
- 配置构建命令：`npm run build`
- 配置输出目录：`dist`

#### 3. 部署后端

**方式1：VPS部署**
```bash
# 安装PM2
npm install -g pm2

# 启动服务
cd backend
pm2 start dist/index.js --name livetrip-backend

# 配置Nginx反向代理
```

**方式2：云服务部署**
- 部署到阿里云、腾讯云等
- 配置环境变量
- 启动服务

#### 4. 数据库部署

**SQLite**：
- 适合小型项目
- 数据库文件在 `backend/prisma/dev.db`

**PostgreSQL**（推荐生产环境）：
- 修改 `DATABASE_URL` 环境变量
- 运行迁移：`npm run prisma:migrate`

---

## 🔧 维护指南

### 日常维护

#### 1. 日志查看

```bash
# 查看后端日志
cd backend
tail -f logs/app.log

# PM2日志
pm2 logs livetrip-backend
```

#### 2. 数据库备份

```bash
# SQLite备份
cp backend/prisma/dev.db backend/prisma/backup_$(date +%Y%m%d).db

# PostgreSQL备份
pg_dump livetrip > backup_$(date +%Y%m%d).sql
```

#### 3. 性能监控

- 使用PM2监控：`pm2 monit`
- 使用New Relic等APM工具
- 监控API响应时间

### 故障排查

#### 1. 服务无法启动

**检查项**：
- 环境变量是否正确
- 端口是否被占用
- 依赖是否安装完整

**解决方法**：
```bash
# 检查端口
lsof -i :3001

# 重新安装依赖
rm -rf node_modules
npm install
```

#### 2. 数据库连接失败

**检查项**：
- DATABASE_URL是否正确
- 数据库服务是否运行
- 权限是否正确

**解决方法**：
```bash
# 检查数据库连接
npm run test:db

# 重置数据库
npm run prisma:reset
```

#### 3. AI服务调用失败

**检查项**：
- ZHIPUAI_API_KEY是否正确
- 网络是否正常
- API配额是否用完

**解决方法**：
- 检查API Key
- 查看智谱AI控制台
- 联系智谱AI客服

### 更新升级

#### 1. 代码更新

```bash
# 拉取最新代码
git pull origin main

# 安装新依赖
npm install

# 运行迁移
npm run prisma:migrate

# 重启服务
pm2 restart livetrip-backend
```

#### 2. 依赖更新

```bash
# 检查过期依赖
npm outdated

# 更新依赖
npm update

# 更新主要版本（谨慎）
npm install package@latest
```

---

## ❓ 常见问题

### Q1: 如何添加新的景点数据？

**方法1：通过Prisma Studio**
```bash
cd backend
npm run prisma:studio
```

**方法2：通过API**
- 使用管理员账号
- 调用景点创建API

### Q2: 如何修改AI推荐算法？

**位置**：`backend/src/services/ai.ts`

**修改步骤**：
1. 编辑AI服务文件
2. 调整提示词模板
3. 测试推荐效果
4. 部署更新

### Q3: 如何添加新的IoT数据源？

**位置**：`backend/src/services/iot.ts`

**修改步骤**：
1. 添加新的数据源配置
2. 实现数据获取函数
3. 更新前端显示组件
4. 测试数据流

### Q4: 如何自定义UI主题？

**位置**：`frontend/tailwind.config.js`

**修改步骤**：
1. 编辑Tailwind配置
2. 修改颜色变量
3. 重新构建前端
4. 部署更新

---

## 📞 技术支持

### 联系方式

- **项目主页**：https://github.com/your-username/livetrip
- **问题反馈**：https://github.com/your-username/livetrip/issues
- **文档更新**：提交Pull Request

### 相关文档

- [README.md](./README.md) - 项目说明
- [使用说明文档.md](./使用说明文档.md) - 用户手册
- [Prisma文档](https://www.prisma.io/docs)
- [React文档](https://react.dev)
- [智谱AI文档](https://open.bigmodel.cn/dev/api)

---

<div align="center">

**项目交接完成！**

Made with ❤️ by LiveTrip Team

</div>
