# LiveTrip 智能旅行规划系统 - 项目交接文档

## 📋 项目概述

**项目名称：** LiveTrip - AI + IoT 智能旅行规划系统

**项目描述：** 基于人工智能和物联网技术的智能行程规划系统，能够根据用户偏好、实时物联网数据动态优化旅行行程。

**技术栈：**
- **前端：** React 18 + TypeScript + Vite + Ant Design + Zustand
- **后端：** Node.js + Express + TypeScript + Prisma
- **地图服务：** 高德地图 API
- **AI 服务：** OpenAI API + 高德地图 API
- **数据库：** PostgreSQL（使用 Prisma ORM）

---

## 📁 项目结构

```
LiveTrip/
├── backend/                          # 后端项目
│   ├── src/                          # 源代码目录
│   │   ├── controllers/              # 控制器层
│   │   │   ├── planController.ts     # 行程规划控制器
│   │   │   └── iotController.ts      # IoT 数据控制器
│   │   ├── services/                 # 服务层
│   │   │   ├── aiRecommender.ts      # AI 推荐服务
│   │   │   ├── amapService.ts        # 高德地图服务
│   │   │   ├── itineraryAdjustService.ts  # 行程调整服务
│   │   │   ├── planService.ts        # 行程规划服务
│   │   │   └── routeOptimizer.ts     # 路线优化服务
│   │   ├── iot/                      # IoT 相关
│   │   │   └── iotDataGenerator.ts   # IoT 数据生成器
│   │   ├── data/                     # 数据层
│   │   │   └── alternativeSpots.ts   # 备选景点数据
│   │   ├── routes/                   # 路由层
│   │   │   ├── index.ts              # 路由聚合
│   │   │   ├── planRoutes.ts         # 行程规划路由
│   │   │   └── iotRoutes.ts          # IoT 数据路由
│   │   ├── middleware/               # 中间件
│   │   │   ├── errorHandler.ts       # 错误处理中间件
│   │   │   └── notFound.ts           # 404 处理中间件
│   │   ├── models/                   # 数据模型
│   │   ├── types/                    # 类型定义
│   │   ├── utils/                    # 工具函数
│   │   └── index.ts                  # 后端入口文件
│   ├── dist/                         # 编译输出目录
│   ├── prisma/                       # Prisma 配置
│   ├── scripts/                      # 脚本文件
│   ├── node_modules/                 # 依赖包
│   ├── package.json                  # 后端依赖配置
│   └── tsconfig.json                 # TypeScript 配置
│
├── frontend/                         # 前端项目
│   ├── src/                          # 源代码目录
│   │   ├── pages/                    # 页面组件
│   │   │   ├── Home.tsx              # 首页
│   │   │   ├── Plan.tsx              # 规划页面
│   │   │   ├── Itinerary.tsx         # 行程页面
│   │   │   └── Map.tsx               # 地图页面
│   │   ├── components/               # 公共组件
│   │   │   ├── AttractionCard.tsx    # 景点卡片组件
│   │   │   ├── BudgetChart.tsx       # 预算图表组件
│   │   │   └── index.ts              # 组件导出
│   │   ├── api/                      # API 客户端
│   │   │   └── client.ts             # API 封装
│   │   ├── services/                 # 前端服务
│   │   │   └── api.ts                # API 服务
│   │   ├── store/                    # 状态管理
│   │   │   └── index.ts              # Zustand Store
│   │   ├── data/                     # Mock 数据
│   │   ├── hooks/                    # 自定义 Hooks
│   │   ├── types/                    # 类型定义
│   │   ├── utils/                    # 工具函数
│   │   ├── assets/                   # 静态资源
│   │   ├── App.tsx                   # 应用主组件
│   │   ├── main.tsx                  # 应用入口
│   │   └── vite-env.d.ts             # Vite 类型定义
│   ├── public/                       # 公共资源
│   ├── node_modules/                 # 依赖包
│   ├── package.json                  # 前端依赖配置
│   ├── vite.config.ts                # Vite 配置
│   ├── tsconfig.json                 # TypeScript 配置
│   ├── .env                          # 环境变量
│   └── .env.example                  # 环境变量示例
│
├── image/                            # 图片资源
├── .gitignore                        # Git 忽略文件
├── package.json                      # 根目录依赖配置
├── README.md                         # 项目说明文档
└── 智能旅行规划系统_产品设计书.docx   # 产品设计文档
```

---

## ✅ 已实现的功能

### 1. 后端功能

#### 1.1 行程规划功能
**文件位置：** `backend/src/services/aiRecommender.ts`

**功能描述：**
- 基于用户偏好（目的地、日期、人数、预算、兴趣等）生成个性化行程
- 集成 OpenAI API 进行智能推荐
- 集成高德地图 API 获取景点信息
- 自动计算路线距离和优化路线

**API 接口：**
- `POST /api/plan` - 创建行程规划

**输入参数：**
```typescript
{
  destination: string;        // 目的地
  start_date: string;         // 出发日期 (YYYY-MM-DD)
  end_date: string;           // 返程日期 (YYYY-MM-DD)
  travelers: number;          // 出行人数
  budget?: number;            // 预算
  preferences?: {
    interests?: string;       // 兴趣偏好
    pace?: string;            // 出行节奏
    energy_level?: string;    // 体力值
  };
}
```

**返回数据：**
```typescript
{
  itinerary: DailyItinerary[];  // 每日行程
  total_cost: number;           // 总费用
  budget_breakdown: {           // 预算分配
    transportation: number;
    accommodation: number;
    dining: number;
    tickets: number;
  };
}
```

---

#### 1.2 IoT 数据模拟功能
**文件位置：** `backend/src/iot/iotDataGenerator.ts`

**功能描述：**
- 模拟 10 个著名景点的 IoT 实时数据
- 包含人流拥挤度、温度、降雨概率、开放状态
- 数据动态变化，模拟真实场景

**数据模型：**
```typescript
{
  id: string;              // 景点 ID
  name: string;            // 景点名称
  crowdLevel: number;      // 人流拥挤度 (0-100%)
  temperature: number;     // 温度 (°C)
  rainProbability: number; // 降雨概率 (0-100%)
  isOpen: boolean;         // 开放状态
}
```

**真实规律建模：**
- **人流规律：** 早上 8-10 点和下午 15-17 点为高峰期
- **天气规律：** 温度缓慢变化，降雨概率随机游走
- **开放状态：** 根据时间和天气综合判断

**API 接口：**
- `GET /api/iot/data` - 获取 IoT 实时数据

---

#### 1.3 行程动态调整功能
**文件位置：** `backend/src/services/itineraryAdjustService.ts`

**功能描述：**
- 根据 IoT 数据动态调整行程
- 支持三种调整策略：
  - **crowd:** 人流 > 80% → 推荐备选景点（地理位置近、同类型）
  - **weather:** 降雨概率 > 70% → 替换为室内景点
  - **closed:** 景点关闭 → 直接替换为备选景点

**备选景点数据：** `backend/src/data/alternativeSpots.ts`
- 每个景点有 3 个备选景点
- 包含类型、户外属性等信息

**API 接口：**
- `POST /api/adjust` - 调整行程

**输入参数：**
```typescript
{
  itinerary: FullItinerary;    // 当前行程
  reason: 'crowd' | 'weather' | 'closed';  // 调整原因
  targetAttractionId: string;  // 需要调整的景点 ID
}
```

**返回数据：**
```typescript
{
  adjustedItinerary: FullItinerary;  // 调整后的行程
  adjustments: Array<{              // 调整详情
    day: number;
    originalAttraction: AttractionItem;
    newAttraction: AttractionItem;
    reason: string;
  }>;
  message: string;  // 调整原因说明
}
```

---

#### 1.4 路线优化功能
**文件位置：** `backend/src/services/routeOptimizer.ts`

**功能描述：**
- 计算景点之间的距离
- 优化游览顺序
- 自动分配游览时间

---

#### 1.5 高德地图服务
**文件位置：** `backend/src/services/amapService.ts`

**功能描述：**
- 搜索景点
- 获取景点详情
- 计算路线距离
- 规划驾车路线

---

### 2. 前端功能

#### 2.1 首页
**文件位置：** `frontend/src/pages/Home.tsx`

**功能描述：**
- 美观的首页设计（渐变背景）
- 显示"智能旅行规划"标题
- "开始规划"按钮，跳转到规划页面

---

#### 2.2 规划页面
**文件位置：** `frontend/src/pages/Plan.tsx`

**功能描述：**
- 渐进式问答表单，收集用户偏好
- 支持 9 个问题：
  1. 目的地
  2. 出发日期（DatePicker）
  3. 返程日期（DatePicker）
  4. 旅行天数
  5. 出行人数
  6. 预算范围
  7. 兴趣偏好
  8. 出行节奏
  9. 体力值
- 调用后端 API 生成行程
- 保存行程数据到 Zustand Store
- 自动跳转到行程页面

---

#### 2.3 行程页面
**文件位置：** `frontend/src/pages/Itinerary.tsx`

**功能描述：**
- 显示完整的行程数据
- 支持拖拽排序景点
- 时间段自动重新计算
- 显示预算图表（饼图）
- 每个景点卡片支持"查看备选"功能
- 行程调整建议弹窗
- 应用调整后更新行程

**组件：**
- `SortableAttractionCard` - 可拖拽的景点卡片
- `BudgetChart` - 预算分配图表

---

#### 2.4 地图页面
**文件位置：** `frontend/src/pages/Map.tsx`

**功能描述：**
- 集成高德地图
- 显示所有景点的标记
- 不同天数使用不同颜色标记
- 显示景点之间的路线
- 左侧景点列表
- 点击列表项，地图定位到该景点
- 显示景点信息窗口

---

#### 2.5 API 客户端
**文件位置：** `frontend/src/api/client.ts`

**功能描述：**
- 封装所有后端 API 调用
- 完整的 TypeScript 类型定义
- 请求/响应拦截器
- 详细的日志输出

**主要函数：**
- `createPlan()` - 创建行程规划
- `getIoTData()` - 获取 IoT 数据
- `adjustItinerary()` - 调整行程

---

#### 2.6 状态管理
**文件位置：** `frontend/src/store/index.ts`

**功能描述：**
- 使用 Zustand 进行全局状态管理
- 存储：
  - `currentItinerary` - 当前行程数据
  - `user` - 用户信息
  - `isLoading` - 加载状态

---

#### 2.7 公共组件
**文件位置：** `frontend/src/components/`

**AttractionCard.tsx：**
- 显示景点信息
- 显示时间、名称、描述
- 支持"查看备选"按钮

**BudgetChart.tsx：**
- 使用 ECharts 渲染饼图
- 显示预算分配明细
- 响应式设计

---

## 🔧 环境配置

### 前端环境变量
**文件位置：** `frontend/.env`

```env
# 高德地图 Key
VITE_AMAP_KEY=b1f81dde480fbb6e2db41fa37940b35f

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3003/api
```

**环境变量示例：** `frontend/.env.example`

```env
VITE_AMAP_KEY=your_amap_key_here
VITE_API_BASE_URL=http://localhost:3003/api
```

---

### 后端环境变量
**文件位置：** `backend/.env`（需要创建）

```env
# 服务器配置
PORT=3003

# OpenAI API
OPENAI_API_KEY=your_openai_api_key_here

# 高德地图 API
AMAP_API_KEY=your_amap_api_key_here

# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/livetrip

# Redis 配置（可选）
REDIS_URL=redis://localhost:6379
```

---

## 🚀 启动指南

### 1. 安装依赖

**后端：**
```bash
cd backend
npm install
```

**前端：**
```bash
cd frontend
npm install
```

---

### 2. 配置环境变量

**前端：**
```bash
cd frontend
# 复制环境变量示例文件
cp .env.example .env
# 编辑 .env 文件，配置高德地图 Key 和后端 API 地址
```

**后端：**
```bash
cd backend
# 创建 .env 文件
# 配置 OpenAI API Key、高德地图 Key、数据库连接等
```

---

### 3. 启动开发服务器

**后端：**
```bash
cd backend
npm run dev
```

**预期输出：**
```
Server is running on http://localhost:3003
Health check: http://localhost:3003/health
API Documentation: http://localhost:3003/api-docs
```

**前端：**
```bash
cd frontend
npm run dev
```

**预期输出：**
```
VITE v6.0.7  ready in 1494 ms
➜  Local:   http://localhost:5173/
```

---

### 4. 访问应用

**前端应用：** http://localhost:5173
**后端 API：** http://localhost:3003/api
**健康检查：** http://localhost:3003/health

---

## 📊 API 接口文档

### 1. 行程规划
**接口：** `POST /api/plan`

**请求示例：**
```json
{
  "destination": "北京",
  "start_date": "2026-03-15",
  "end_date": "2026-03-17",
  "travelers": 2,
  "budget": 5000,
  "preferences": {
    "interests": "历史文化",
    "pace": "慢游",
    "energy_level": "中等"
  }
}
```

**响应示例：**
```json
{
  "success": true,
  "data": {
    "itinerary": [
      {
        "day": 1,
        "date": "2026-03-15",
        "attractions": [
          {
            "name": "故宫博物院",
            "time": "09:00-11:00",
            "location": "116.397029,39.917839",
            "estimated_cost": 60,
            "description": "风景名胜",
            "type": "风景名胜",
            "address": "景山前街4号"
          }
        ],
        "daily_cost": 240
      }
    ],
    "total_cost": 480,
    "budget_breakdown": {
      "transportation": 1000,
      "accommodation": 2000,
      "dining": 1250,
      "tickets": 750
    }
  }
}
```

---

### 2. 获取 IoT 数据
**接口：** `GET /api/iot/data`

**响应示例：**
```json
{
  "success": true,
  "data": {
    "timestamp": 1770223439870,
    "spots": [
      {
        "id": "1",
        "name": "故宫博物院",
        "crowdLevel": 63,
        "temperature": 22.2,
        "rainProbability": 35,
        "isOpen": false
      }
    ]
  }
}
```

---

### 3. 调整行程
**接口：** `POST /api/adjust`

**请求示例：**
```json
{
  "itinerary": { /* 完整的行程数据 */ },
  "reason": "crowd",
  "targetAttractionId": "1"
}
```

**响应示例：**
```json
{
  "success": true,
  "data": {
    "adjustedItinerary": { /* 调整后的行程 */ },
    "adjustments": [
      {
        "day": 1,
        "originalAttraction": { /* 原景点 */ },
        "newAttraction": { /* 新景点 */ },
        "reason": "当前人流 85% 超过阈值，已替换为人流较少的备选景点"
      }
    ],
    "message": "当前人流 85% 超过阈值，已替换为人流较少的备选景点"
  }
}
```

---

## 🐛 已知问题和修复记录

### 问题 1: 首页没有导航到规划页面
**问题描述：**
- 访问 localhost 后只显示标题，没有导航按钮
- 需要手动输入 URL 才能进入规划页面

**修复方案：**
- 修改了 `frontend/src/pages/Home.tsx`
- 添加了"开始规划"按钮
- 添加了美观的首页设计

**修复文件：** `frontend/src/pages/Home.tsx`

---

### 问题 2: 行程规划后显示空白页面
**问题描述：**
- 完成行程规划后点击"生成行程"
- 跳转到行程页面后显示"暂无行程数据"

**根本原因：**
- `BudgetChart` 组件期望的数据格式是：`{ category: string; amount: number }[]`
- 但在 `Itinerary` 页面中传递的数据格式是：`{ labels: string[], datasets: [...] }`
- 导致 `data.map` 报错，因为 `data` 是一个对象，不是数组

**错误信息：**
```
BudgetChart.tsx:44 Uncaught TypeError: data.map is not a function
```

**修复方案：**
- 修改了 `frontend/src/pages/Itinerary.tsx`
- 将传递给 `BudgetChart` 的数据格式从对象改为数组

**修复文件：** `frontend/src/pages/Itinerary.tsx`

---

### 问题 3: 刷新页面后数据丢失
**问题描述：**
- 生成行程后，刷新页面，数据显示"暂无行程数据"

**原因：**
- Zustand store 默认不持久化数据
- 页面刷新后，内存中的数据丢失

**解决方案：**
- 这是正常行为
- 如需持久化，可以使用 `zustand/middleware` 或 LocalStorage

---

## 📝 开发文档

### 测试指南
1. **BudgetChart错误修复测试指南.md** - BudgetChart 错误修复的详细测试步骤
2. **前端后端集成测试指南.md** - 前端和后端集成的完整测试流程
3. **最终验证清单.md** - 完整的验证清单

### 产品文档
- **智能旅行规划系统_产品设计书.docx** - 产品设计文档

---

## 🔮 接下来需要完成的工作

### 1. 数据库集成（高优先级）
**当前状态：** 数据库已配置（Prisma），但未实际使用

**需要完成：**
- [ ] 设计数据库 Schema（用户、行程、景点等）
- [ ] 编写 Prisma Schema
- [ ] 实现数据库连接
- [ ] 实现数据持久化
- [ ] 实现用户认证（JWT）
- [ ] 实现行程保存和加载

**预估工作量：** 2-3 天

---

### 2. 用户认证系统（高优先级）
**当前状态：** 未实现

**需要完成：**
- [ ] 用户注册功能
- [ ] 用户登录功能
- [ ] JWT Token 认证
- [ ] 用户信息管理
- [ ] 权限控制

**预估工作量：** 2-3 天

---

### 3. 行程保存和历史记录（中优先级）
**当前状态：** 使用内存存储，刷新后丢失

**需要完成：**
- [ ] 行程保存到数据库
- [ ] 行程历史记录
- [ ] 行程删除功能
- [ ] 行程分享功能
- [ ] 行程导出功能（PDF、图片等）

**预估工作量：** 2-3 天

---

### 4. 实时 IoT 数据集成（中优先级）
**当前状态：** 使用模拟数据

**需要完成：**
- [ ] 接入真实的 IoT 设备数据
- [ ] 实现数据缓存（Redis）
- [ ] 实现数据推送（WebSocket）
- [ ] 实现实时通知
- [ ] 优化数据更新频率

**预估工作量：** 3-5 天

---

### 5. 移动端适配（中优先级）
**当前状态：** 仅支持桌面端

**需要完成：**
- [ ] 响应式设计优化
- [ ] 移动端布局调整
- [ ] 触摸手势支持
- [ ] 移动端地图优化
- [ ] PWA 支持

**预估工作量：** 3-5 天

---

### 6. 性能优化（中优先级）
**当前状态：** 基础性能已优化

**需要完成：**
- [ ] 前端代码分割
- [ ] 图片懒加载
- [ ] API 请求优化（缓存、节流、防抖）
- [ ] 地图渲染优化
- [ ] 数据预加载

**预估工作量：** 2-3 天

---

### 7. 测试覆盖（低优先级）
**当前状态：** 无自动化测试

**需要完成：**
- [ ] 单元测试（Jest）
- [ ] 集成测试
- [ ] E2E 测试（Playwright）
- [ ] API 测试
- [ ] 测试覆盖率报告

**预估工作量：** 3-5 天

---

### 8. 部署和运维（低优先级）
**当前状态：** 仅本地开发环境

**需要完成：**
- [ ] Docker 容器化
- [ ] CI/CD 流程
- [ ] 生产环境配置
- [ ] 监控和日志
- [ ] 备份策略

**预估工作量：** 3-5 天

---

### 9. 额外功能建议（低优先级）

**社交功能：**
- [ ] 行程分享到社交平台
- [ ] 用户评论和评分
- [ ] 旅行社区

**智能推荐增强：**
- [ ] 基于历史行程的个性化推荐
- [ ] 季节性推荐
- [ ] 热门景点推荐

**费用优化：**
- [ ] 实时费用计算
- [ ] 费用预警
- [ ] 优惠券系统

**多语言支持：**
- [ ] 国际化（i18n）
- [ ] 多语言切换
- [ ] 本地化内容

---

## 📚 技术文档

### 关键文件说明

**后端核心文件：**
- `backend/src/index.ts` - 后端入口，配置 Express 服务器
- `backend/src/services/aiRecommender.ts` - AI 推荐，集成 OpenAI API
- `backend/src/services/amapService.ts` - 高德地图服务封装
- `backend/src/services/itineraryAdjustService.ts` - 行程动态调整逻辑
- `backend/src/iot/iotDataGenerator.ts` - IoT 数据模拟器
- `backend/src/data/alternativeSpots.ts` - 备选景点数据

**前端核心文件：**
- `frontend/src/App.tsx` - 应用主组件，配置路由
- `frontend/src/pages/Plan.tsx` - 规划页面，调用后端 API
- `frontend/src/pages/Itinerary.tsx` - 行程页面，显示行程数据
- `frontend/src/pages/Map.tsx` - 地图页面，集成高德地图
- `frontend/src/api/client.ts` - API 客户端，封装所有 API 调用
- `frontend/src/store/index.ts` - Zustand Store，全局状态管理

---

### 代码规范

**TypeScript 配置：**
- 严格模式开启
- 使用 ES2020+ 语法
- 完整的类型定义

**代码风格：**
- 使用 Prettier 格式化
- 使用 ESLint 检查
- 遵循 Airbnb JavaScript 风格指南

---

### Git 提交规范

**提交信息格式：**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型（type）：**
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例：**
```
feat(frontend): 添加用户注册功能

- 实现用户注册表单
- 集成后端注册 API
- 添加表单验证

Closes #123
```

---

## 🔑 关键配置

### 高德地图 API Key
**用途：**
- 搜索景点
- 获取景点详情
- 计算路线距离
- 渲染地图

**如何获取：**
1. 访问 https://console.amap.com/dev/key/app
2. 注册/登录账号
3. 创建应用，选择「Web端(JS API)」类型
4. 获取 API Key
5. 配置到 `.env` 文件

---

### OpenAI API Key
**用途：**
- 智能推荐景点
- 生成行程描述
- 优化行程顺序

**如何获取：**
1. 访问 https://platform.openai.com/api-keys
2. 创建 API Key
3. 配置到 `.env` 文件

---

## 📞 联系方式

如果遇到问题，可以通过以下方式联系：

- **项目仓库：** [GitHub 仓库地址]
- **文档：** [文档地址]
- **Issue：** [Issue 地址]

---

## 🎉 总结

LiveTrip 智能旅行规划系统已经完成了核心功能的开发，包括：

1. ✅ 智能行程规划（AI 推荐）
2. ✅ IoT 数据模拟
3. ✅ 行程动态调整
4. ✅ 前端和后端集成
5. ✅ 地图集成
6. ✅ 用户界面优化

**当前状态：** 可以在本地环境正常运行，核心功能完整可用。

**下一步：** 根据优先级完成数据库集成、用户认证、行程保存等功能。

---

**祝项目顺利！** 🚀
