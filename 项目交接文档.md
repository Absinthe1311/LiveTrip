# LiveTrip 项目交接文档

## 📋 文档说明

本文档用于项目交接，包含项目架构、技术选型、部署说明、维护指南等完整信息。

---

## 📖 目录

- [项目概述](#项目概述)
- [技术架构](#技术架构)
- [数据库设计](#数据库设计)
- [API接口文档](#api接口文档)
- [前端架构](#前端架构)
- [部署说明](#部署说明)
- [维护指南](#维护指南)
- [常见问题](#常见问题)

---

## 🎯 项目概述

### 项目简介

**LiveTrip** 是一个基于 AI + IoT 的智能旅行规划系统，为用户提供个性化的旅行规划服务。

### 核心功能

1. **AI智能助手**：基于智谱AI GLM-4的自然语言对话,支持"我想去北京玩三天"等自然表达
2. **AI智能规划**：基于自主研发的多因素评分引擎
3. **热门目的地**：6个热门城市展示（北京、上海、厦门、成都、杭州、西安），每个城市9个精选景点
4. **IoT实时数据**：景点拥挤度、天气等实时信息
5. **智能推荐**：景点、酒店、餐厅推荐
6. **行程管理**：拖拽排序、备选景点、分享导出
7. **图片管理**：上传、审核、管理景点图片，支持Cloudinary云存储
8. **游记撰写**：Markdown编辑器撰写游记
9. **协同规划**：多人协同编辑行程，实时同步和沟通

### 技术栈

**前端**：React 18.3.1 + TypeScript 5.7.3 + Vite 6.0.7 + Tailwind CSS

**后端**：Node.js + Express 4.21.2 + Prisma 6.1.0 + SQLite

**AI服务**：智谱AI GLM-4 (自然语言理解与对话)

**地图服务**：高德地图 API

---

## 🏗️ 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                      用户界面层                      │
│  React + TypeScript + Tailwind CSS + shadcn/ui        │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│                   API 网关层                       │
│              Express + CORS + JWT                 │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│                  业务逻辑层                         │
│  ┌──────────────────────────────────────────┐   │
│  │  AI Agent 服务 (智谱AI GLM-4)             │   │
│  │  - 自然语言理解                           │   │
│  │  - 意图识别                               │   │
│  │  - 工具调用编排                           │   │
│  │  - 对话历史管理                           │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  多因素评分引擎                            │   │
│  │  - 偏好匹配（Jaccard相似度）               │   │
│  │  - 质量评分                                │   │
│  │  - IoT评分                                 │   │
│  │  - 人群评分                                │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  K-means 地理聚类                         │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  多样性约束                               │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  2-opt 路径优化                            │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  动态预算计算                             │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  IoT 实时检查                             │   │
│  └──────────────────────────────────────────┘   │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│                   数据访问层                       │
│              Prisma ORM + SQLite                │
└─────────────────────────────────────────────┘
```

### 核心服务模块

| 服务模块 | 职责 | 文件位置 |
|---------|------|---------|
| agentService | AI Agent核心服务 | `src/services/agentService.ts` |
| mustVisitSpotExtractor | 必选景点提取 | `src/services/mustVisitSpotExtractor.ts` |
| constraintAwarePlanner | 约束感知规划 | `src/services/constraintAwarePlanner.ts` |
| chatHistoryService | 对话历史管理 | `src/services/chatHistoryService.ts` |
| scoringEngine | 多因素评分 | `src/services/scoringEngine.ts` |
| clusteringService | K-means聚类 | `src/services/clusteringService.ts` |
| diversityService | 多样性约束 | `src/services/diversityService.ts` |
| routeOptimizer | 2-opt路径优化 | `src/services/routeOptimizer.ts` |
| budgetOptimizer | 动态预算计算 | `src/services/budgetOptimizer.ts` |
| iotCheckService | IoT实时检查 | `src/services/iotCheckService.ts` |
| amapService | 高德地图服务 | `src/services/amapService.ts` |
| spotService | 景点数据服务 | `src/services/spotService.ts` |

---

## 🗄️ 数据库设计

### 主要数据表

#### 1. User（用户表）
```prisma
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  name      String?
  avatar    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  trips     Trip[]
  favorites Favorite[]
  blogs     Blog[]
}
```

#### 2. Spot（景点表）
```prisma
model Spot {
  id            String   @id @default(cuid())
  name          String
  city          String
  location      String   // 经纬度: "lng,lat"
  address       String?
  category      String?
  typecode      String?
  rating        Float?
  ticketPrice   Int?
  openTime      String?
  description   String?
  isOutdoor     Boolean?
  images        Image[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([city])
  @@index([category])
}
```

#### 3. Trip（行程表）
```prisma
model Trip {
  id              String   @id @default(cuid())
  userId          String
  user            User     @relation(fields: [userId], references: [id])
  title           String
  destination     String
  startDate       DateTime
  endDate         DateTime
  budget          Float?
  totalCost       Float    @default(0)
  itinerary       Json     // 行程数据
  shareToken      String?  @unique
  isShared        Boolean  @default(false)
  isCompleted     Boolean  @default(false)
  hotel           Json?
  restaurants     Json?
  packingItems    PackingItem[]
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([userId])
}
```

#### 4. SpotIoTData（景点IoT数据表）
```prisma
model SpotIoTData {
  id              String   @id @default(cuid())
  spotId          String   @unique
  crowdLevel      Int      @default(0)
  temperature     Float    @default(20)
  humidity        Float?
  rainProbability Int      @default(0)
  isOpen          Boolean  @default(true)
  generatedAt     DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([spotId])
}
```

#### 5. AlternativeSpot（备选景点表）
```prisma
model AlternativeSpot {
  id          String   @id @default(cuid())
  oldSpotId   String
  newSpotId   String
  city        String
  reason      String
  createdAt   DateTime @default(now())

  @@unique([oldSpotId, newSpotId])
  @@index([oldSpotId])
  @@index([newSpotId])
}
```

### 数据迁移

```bash
# 生成 Prisma Client
npm run prisma:generate

# 运行数据库迁移
npm run prisma:migrate

# 重置数据库（开发环境）
npm run prisma:reset

# 查看数据库
npm run prisma:studio
```

---

## 🔌 API接口文档

### 核心API端点

#### 1. 行程规划
```http
POST /api/plan
Content-Type: application/json

{
  "origin": "北京",
  "destination": "上海",
  "start_date": "2024-05-01",
  "end_date": "2024-05-03",
  "budget": 5000,
  "groupSize": 2,
  "groupType": "couple",
  "hasChildren": false,
  "hasElderly": false,
  "preferences": {
    "pace": "moderate",
    "energy_level": "medium",
    "categories": ["history", "art", "city"]
  }
}
```

#### 2. 热门目的地（新增）
```http
GET /api/destinations/cities
```
返回6个热门城市及其统计信息

```http
GET /api/destinations/cities/:city/spots?limit=9
```
返回指定城市的热门景点（用于首页展示）

```http
GET /api/destinations/cities/:city/all?page=1&pageSize=12
```
返回指定城市的所有热门景点（用于城市详情页，支持分页）

#### 3. 获取景点数据
```http
GET /api/destinations/:city
```

#### 4. 获取IoT数据
```http
GET /api/iot/data
```

#### 5. 获取备选景点
```http
GET /api/spots/alternatives/:spotId?city=上海
```

#### 6. AI智能助手
```http
POST /api/agent/chat
Content-Type: application/json

{
  "question": "我想去北京玩三天",
  "userId": "user_id"
}
```

### 完整API文档

启动后端服务后，访问：http://localhost:3003/api-docs

---

## 💻 前端架构

### 技术栈
- **框架**：React 18.3.1
- **语言**：TypeScript 5.7.3
- **构建工具**：Vite 6.0.7
- **样式**：Tailwind CSS
- **UI组件**：shadcn/ui
- **路由**：React Router
- **状态管理**：Zustand
- **HTTP客户端**：Axios

### 目录结构
```
frontend/
├── src/
│   ├── components/      # 共享组件
│   │   ├── LocationInput.tsx
│   │   ├── DateRangeInput.tsx
│   │   ├── BudgetRangeInput.tsx
│   │   ├── PreferenceInput.tsx
│   │   ├── GroupInput.tsx
│   │   └── ...
│   ├── pages/          # 页面组件
│   │   ├── Home.tsx
│   │   ├── Plan.tsx
│   │   ├── Itinerary.tsx
│   │   └── ...
│   ├── api/            # API调用
│   │   └── client.ts
│   ├── store/          # 状态管理
│   │   └── index.ts
│   └── main.tsx        # 入口文件
└── package.json
```

### 状态管理

使用 Zustand 进行全局状态管理：

```typescript
// store/index.ts
interface AppState {
  currentItinerary: FullItinerary | null;
  setCurrentItinerary: (itinerary: FullItinerary) => void;
  user: User | null;
  setUser: (user: User | null) => void;
}

export const useAppStore = create<AppState>((set) => ({
  currentItinerary: null,
  setCurrentItinerary: (itinerary) => set({ currentItinerary: itinerary }),
  user: null,
  setUser: (user) => set({ user }),
}));
```

---

## 🚀 部署说明

### 环境要求
- Node.js >= 18.0.0
- npm >= 9.0.0
- Git

### 环境变量配置

创建 `.env` 文件：

```env
# 数据库配置
DATABASE_URL="file:./dev.db"

# 服务器配置
PORT=3003
NODE_ENV=development

# JWT配置
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=7d

# 高德地图API配置
AMAP_JS_KEY=your-amap-js-key-here
AMAP_JS_SECRET=your-amap-js-secret-here

# Cloudinary配置（可选）
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# OpenWeatherMap天气API配置
OPENWEATHERMAP_API_KEY=your-openweathermap-api-key-here
OPENWEATHERMAP_API_URL=https://api.openweathermap.org/data/2.5

# CORS配置
CORS_ORIGIN=http://localhost:5173
```

### 部署步骤

#### 1. 后端部署
```bash
cd backend

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入真实的API密钥

# 生成Prisma Client
npm run prisma:generate

# 运行数据库迁移
npm run prisma:migrate

# 填充示例数据（可选）
npm run prisma:seed

# 构建项目
npm run build

# 启动服务（生产环境）
npm start

# 开发环境
npm run dev
```

#### 2. 前端部署
```bash
cd frontend

# 安装依赖
npm install

# 配置API地址（如果需要）
# 编辑 vite.config.ts，修改 proxy 配置

# 构建项目
npm run build

# 预览构建结果
npm run preview

# 开发环境
npm run dev
```

#### 3. 生产环境部署

**使用 PM2 部署后端：**
```bash
# 安装 PM2
npm install -g pm2

# 启动后端
pm2 start ecosystem.config.js

# 查看日志
pm2 logs

# 重启服务
pm2 restart all
```

**使用 Nginx 部署前端：**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:3003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 🔧 维护指南

### 日常维护

#### 1. 数据库备份
```bash
# 备份数据库
cp dev.db dev.db.backup.$(date +%Y%m%d)

# 恢复数据库
cp dev.db.backup.20240301 dev.db
```

#### 2. 日志查看
```bash
# 查看后端日志
pm2 logs livetrip-backend

# 查看前端日志
pm2 logs livetrip-frontend
```

#### 3. 更新依赖
```bash
# 后端
cd backend
npm update

# 前端
cd frontend
npm update
```

### 监控和告警

#### 1. 健康检查
```bash
curl http://localhost:3003/health
```

#### 2. 性能监控
- 使用 PM2 监控进程状态
- 查看数据库查询性能
- 监控API响应时间

#### 3. 错误日志
- 查看后端错误日志
- 查看前端错误日志
- 设置错误告警

### 数据更新

#### 1. 更新景点数据
```bash
cd backend
npm run preload-city-spots
```

#### 2. 更新IoT数据
```bash
# IoT数据会自动更新
# 也可以手动触发更新
```

---

## ❓ 常见问题

### 1. 数据库连接失败
**问题**：`Error: Database connection failed`

**解决**：
```bash
# 检查数据库文件是否存在
ls -la dev.db

# 重新生成Prisma Client
npm run prisma:generate

# 重启服务
pm2 restart all
```

### 2. API调用失败
**问题**：`Error: API request failed`

**解决**：
- 检查API密钥是否正确配置
- 检查网络连接
- 查看后端日志获取详细错误信息

### 3. 前端无法连接后端
**问题**：`Error: Network Error`

**解决**：
- 检查后端服务是否启动
- 检查CORS配置
- 检查API地址配置

### 4. 算法优化后行程为空
**问题**：生成的行程中没有景点

**解决**：
- 检查IoT数据是否正常
- 查看后端日志了解详细信息
- 检查景点数据是否存在

### 5. 预算计算不准确
**问题**：预算分配不合理

**解决**：
- 检查城市等级配置
- 检查季节性参数
- 调整预算档次阈值

---

## 📞 联系方式

如有问题或需要技术支持，请通过以下方式联系：

- 提交 GitHub Issue
- 发送邮件至：tech-support@example.com

---

## 📄 许可证

本项目采用 MIT 许可证

---

**LiveTrip 项目团队** 🚀
