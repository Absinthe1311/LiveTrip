# LiveTrip 智能旅行规划系统 - 使用说明文档

## 📋 目录

- [系统概述](#系统概述)
- [环境准备](#环境准备)
- [启动系统](#启动系统)
- [使用流程](#使用流程)
- [功能说明](#功能说明)
- [管理员功能](#管理员功能)
- [常见问题](#常见问题)

---

## 系统概述

**项目名称**: LiveTrip - AI + IoT 智能旅行规划系统

**项目描述**: 基于人工智能和物联网技术的智能行程规划系统，能够根据用户偏好、实时物联网数据动态优化旅行行程。

**主要功能**:
- 智能行程规划（AI推荐）
- 备选景点推荐
- IoT实时数据展示
- 拖拽排序
- 实时地图显示
- 智能缓存优化
- 预算管理与计算
- 酒店推荐
- 餐厅推荐
- 景点收藏
- 行程分享与PDF导出
- 游记功能
- 景点图片上传与管理
- 管理员后台

---

## 环境准备

### 系统要求

- **操作系统**: Windows / macOS / Linux
- **Node.js**: v16 或更高版本
- **npm**: v8 或更高版本
- **浏览器**: Chrome / Firefox / Safari / Edge

### 环境变量配置

#### 后端环境变量

文件位置: `backend/.env`

```env
# 服务器配置
PORT=3003
NODE_ENV=development

# 数据库配置
DATABASE_URL="file:./dev.db"

# 高德地图 API 配置
AMAP_API_KEY=your_amap_api_key_here

# 智谱AI配置（可选，不配置则使用规则引擎）
ZHIPUAI_API_KEY=your_zhipuai_api_key_here

# Cloudinary 图片存储配置
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Unsplash API 配置（可选）
UNSPLASH_ACCESS_KEY=your_unsplash_key

# CORS 配置
CORS_ORIGIN=http://localhost:5174
```

#### 前端环境变量

文件位置: `frontend/.env`

```env
# 高德地图 Key
VITE_AMAP_KEY=your_amap_api_key_here
VITE_AMAP_JS_KEY=your_amap_js_key_here
VITE_AMAP_JS_SECRET=your_amap_js_secret_here

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3003/api
```

---

## 启动系统

### 1. 安装依赖

#### 后端依赖
```bash
cd backend
npm install
```

#### 前端依赖
```bash
cd frontend
npm install
```

### 2. 初始化数据库

```bash
cd backend

# 生成Prisma客户端
npx prisma generate

# 运行数据库迁移
npx prisma migrate dev

# 初始化管理员账号（可选）
npx ts-node prisma/seed.ts
```

### 3. 启动后端服务

```bash
cd backend
npm run dev
```

**预期输出**:
```
Server is running on http://localhost:3003
Health check: http://localhost:3003/health
API Documentation: http://localhost:3003/api-docs
```

**访问地址**: http://localhost:3003

### 4. 启动前端服务

```bash
cd frontend
npm run dev
```

**预期输出**:
```
VITE v6.0.7  ready in 235 ms
➜  Local:   http://localhost:5174/
```

**访问地址**: http://localhost:5174

### 5. 生产环境构建

#### 后端构建
```bash
cd backend
npm run build
npm start
```

#### 前端构建
```bash
cd frontend
npm run build
```

构建后的文件位于 `frontend/dist` 目录，可部署到任何静态文件服务器。

---

## 使用流程

### 步骤 1: 用户注册/登录

1. 访问 http://localhost:5174
2. 点击右上角"登录"按钮
3. 新用户点击"注册"，填写用户名、邮箱、密码
4. 已有账号直接登录

**默认管理员账号**:
- 用户名: `666`
- 密码: `666666`

### 步骤 2: 创建行程

1. 登录后点击"开始规划"或"创建行程"
2. 填写行程信息：
   - **出发地**（可选）: 输入出发城市
   - **目的地**（必填）: 输入目的地城市
   - **出发日期**（必填）: 选择出发日期
   - **返程日期**（必填）: 选择返程日期
   - **旅行偏好**（可选）: 选择旅行风格
   - **预算范围**（可选）: 选择预算区间
3. 点击"生成行程"
4. 等待 5-15 秒，系统自动生成行程

### 步骤 3: 查看和编辑行程

#### 行程列表
- 显示每天的行程安排
- 包含景点名称、时间、描述
- 显示IoT数据（天气、人流、开放状态）

#### 拖拽排序
- 鼠标按住景点卡片拖拽
- 松开后自动重新计算时间

#### 查看备选景点
- 点击景点卡片右侧"查看备选"按钮
- 显示可替换的备选景点列表
- 点击"替换此景点"进行替换

#### 查看地图
- 点击每天标题旁的"查看地图"按钮
- 显示该天行程的地图视图
- 包含景点、酒店、餐厅标记

### 步骤 4: 选择酒店和餐厅

#### 选择酒店
- 在行程页面下方查看酒店推荐
- 点击"选择"按钮选择酒店
- 酒店信息会保存到行程

#### 选择餐厅
- 每天的行程下方有餐厅推荐
- 点击"选择"按钮选择餐厅
- 餐厅信息会保存到该天行程

### 步骤 5: 保存行程

- 点击底部"确认并保存"按钮
- 行程保存到数据库
- 可在"我的行程"页面查看

### 步骤 6: 完成行程

- 保存行程后，点击"完成行程"按钮
- 确认后行程状态变为"已完成"
- 完成后可以：
  - 上传景点图片
  - 写游记
  - 分享行程

### 步骤 7: 上传图片

- 行程完成后，每个景点下方显示"上传图片"按钮
- 点击按钮打开上传弹窗
- 选择图片（支持JPG、PNG，最大20MB）
- 图片上传后等待管理员审核

### 步骤 8: 写游记

- 行程完成后，点击"写游记"按钮
- 进入游记编辑页面
- 支持Markdown格式编辑
- 发布后可在"游记"页面查看

### 步骤 9: 分享行程

- 点击"分享"按钮生成分享链接
- 支持PDF导出
- 可分享给朋友查看

---

## 功能说明

### 1. 智能行程规划

**功能**: 根据用户偏好生成个性化行程

**工作原理**:
- 从高德地图 API 获取景点数据
- 使用智谱AI（ChatGLM）或规则引擎生成行程
- 自动计算路线距离和优化路线
- 自动分配游览时间
- 景点数据存储到数据库

### 2. 备选景点推荐

**功能**: 为每个景点推荐1-2个备选景点

**工作原理**:
- 从数据库获取同一城市的所有景点
- 排除行程中已有的景点
- 优先推荐IoT数据良好的景点
- 替换后动态更新备选关系

### 3. IoT数据展示

**功能**: 实时显示景点的IoT数据

**数据内容**:
- **天气标签**: 晴天/小雨/中雨/暴雨
- **人流标签**: 人流较少/适中/较多/极度拥挤
- **开放状态**: 正常开放/已关闭

### 4. 数据缓存优化

**功能**: 缓存地点和景点数据，减少 API 调用

**缓存内容**:
- 出发地和目的地搜索结果
- 高德POI数据
- 目的地热门景点
- 缓存有效期24小时

### 5. 拖拽排序

**功能**: 拖拽景点卡片改变顺序

**特点**:
- 使用 @dnd-kit 库实现
- 自动重新计算时间段
- 考虑景点之间的移动时间
- 自动添加午餐时间

### 6. 地图可视化

**功能**: 显示行程地图和周围环境

**特点**:
- 使用高德地图 API
- 显示景点、酒店、餐厅标记
- 显示道路、建筑、兴趣点
- 支持交互式操作

### 7. 预算管理

**功能**: 自动计算和分配旅行预算

**特点**:
- 根据酒店、餐厅、景点计算实际预算
- 实时预算计算和预警
- 预算分配可视化

### 8. 酒店推荐

**功能**: 基于目的地推荐酒店

**特点**:
- 使用高德地图API搜索
- 显示酒店详细信息
- 支持选择并保存到行程

### 9. 餐厅推荐

**功能**: 基于景点位置推荐餐厅

**特点**:
- 使用高德地图API搜索
- 显示餐厅详细信息
- 支持选择并保存到行程

### 10. 收藏功能

**功能**: 收藏喜欢的景点

**特点**:
- 添加景点到收藏列表
- 查看收藏列表
- 添加收藏备注

### 11. 行程分享

**功能**: 分享行程给他人

**特点**:
- 生成分享链接
- 支持PDF导出
- 无需登录即可查看

### 12. 游记功能

**功能**: 记录旅行经历

**特点**:
- Markdown编辑器
- 支持图片上传
- 发布后公开可见

### 13. 图片上传

**功能**: 上传景点图片

**特点**:
- 支持JPG、PNG格式
- 最大20MB
- 管理员审核后展示

---

## 管理员功能

### 访问管理后台

1. 使用管理员账号登录（用户名: `666`，密码: `666666`）
2. 登录后自动跳转到管理后台
3. 或直接访问 http://localhost:5174/admin

### 景点图片管理

**路径**: `/admin/spots`

**功能**:
- 查看所有景点列表
- 查看每个景点的图片数量
- 管理景点图片（查看、删除）
- 上传新图片（直接审核通过）

### 图片审核

**路径**: `/admin/review`

**功能**:
- 查看待审核图片列表
- 审核通过/拒绝图片
- 添加拒绝原因
- 批量审核

---

## 常见问题

### Q1: 启动后端服务时报错 "AMAP_API_KEY 未配置"

**A**: 请检查 `backend/.env` 文件中是否配置了 `AMAP_API_KEY`:
```env
AMAP_API_KEY=your_amap_api_key_here
```

### Q2: 启动前端服务时报错 "高德地图 Key 未配置"

**A**: 请检查 `frontend/.env` 文件中是否配置了 `VITE_AMAP_KEY`:
```env
VITE_AMAP_KEY=your_amap_api_key_here
VITE_AMAP_JS_KEY=your_amap_js_key_here
VITE_AMAP_JS_SECRET=your_amap_js_secret_here
```

### Q3: 图片上传失败

**A**: 请检查:
- Cloudinary 配置是否正确
- 图片大小是否超过20MB
- 图片格式是否为JPG或PNG
- 后端服务是否正常运行

### Q4: 管理员登录后跳转到普通用户页面

**A**: 请确保:
- 使用正确的管理员账号登录
- 用户角色为 `admin`
- 检查数据库中用户的 `role` 字段

### Q5: 地图不显示

**A**: 请检查:
- 高德地图 Key 是否正确配置
- 网络连接是否正常
- 景点的 location 字段是否正确

### Q6: 如何创建新的管理员账号

**A**: 运行种子脚本:
```bash
cd backend
npx ts-node prisma/seed.ts
```

### Q7: 如何查看数据库中的数据

**A**: 可以使用 Prisma Studio:
```bash
cd backend
npx prisma studio
```
访问地址: http://localhost:5555

### Q8: AI推荐失败怎么办

**A**: 
- 检查 `ZHIPUAI_API_KEY` 是否正确配置
- 系统会自动降级使用规则引擎
- 规则引擎也能生成合理的行程

### Q9: 如何清空数据库

**A**: 
```bash
cd backend
npx prisma migrate reset
```
注意：这会删除所有数据

### Q10: 行程完成后无法上传图片

**A**: 请确保:
- 行程状态为"已完成"
- 已登录用户账号
- 景点在数据库中存在

---

## 技术支持

如果您在使用过程中遇到任何问题，请:
1. 查看本使用说明文档
2. 查看项目交接文档
3. 检查浏览器控制台的错误信息
4. 检查后端控制台的日志信息
5. 使用 Prisma Studio 查看数据库数据

---

**文档版本**: 4.0
**最后更新**: 2026-03-14
**系统版本**: LiveTrip v1.0.0
