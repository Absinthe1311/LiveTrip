# LiveTrip 智能旅行规划系统 - 使用说明文档

## 📋 目录

- [系统概述](#系统概述)
- [环境准备](#环境准备)
- [启动系统](#启动系统)
- [使用流程](#使用流程)
- [功能说明](#功能说明)
- [数据库查看](#数据库查看)
- [常见问题](#常见问题)

---

## 系统概述

**项目名称**: LiveTrip - AI + IoT 智能旅行规划系统

**项目描述**: 基于人工智能和物联网技术的智能行程规划系统,能够根据用户偏好、实时物联网数据动态优化旅行行程。

**主要功能**:
- 智能行程规划
- 拖拽排序
- 实时地图显示
- 周围环境查询
- 数据缓存优化

---

## 环境准备

### 系统要求

- **操作系统**: Windows / macOS / Linux
- **Node.js**: v16 或更高版本
- **npm**: v8 或更高版本
- **浏览器**: Chrome / Firefox / Safari / Edge

### 环境变量配置

#### 后端环境变量

文件位置: `backend/.env`

```env
# 服务器配置
PORT=3003
NODE_ENV=development

# 数据库配置
DATABASE_URL="file:./dev.db"

# 高德地图 API 配置
AMAP_API_KEY=b1f81dde480fbb6e2db41fa37940b35f

# Anthropic Claude API 配置(可选)
ANTHROPIC_API_KEY=

# OpenAI API 配置(可选)
OPENAI_API_KEY=

# CORS 配置
CORS_ORIGIN=http://localhost:5174
```

#### 前端环境变量

文件位置: `frontend/.env`

```env
# 高德地图 Key
VITE_AMAP_KEY=b1f81dde480fbb6e2db41fa37940b35f

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3003/api
```

---

## 启动系统

### 1. 安装依赖

#### 后端依赖
```bash
cd backend
npm install
```

#### 前端依赖
```bash
cd frontend
npm install
```

### 2. 启动后端服务

```bash
cd backend
npm run dev
```

**预期输出**:
```
Server is running on http://localhost:3003
Health check: http://localhost:3003/health
```

**访问地址**: http://localhost:3003

### 3. 启动前端服务

```bash
cd frontend
npm run dev
```

**预期输出**:
```
VITE v6.0.7  ready in 235 ms
➜  Local:   http://localhost:5174/
```

**访问地址**: http://localhost:5174

---

## 使用流程

### 步骤 1: 访问首页

1. 打开浏览器
2. 访问 http://localhost:5174
3. 看到首页,显示"智能旅行规划"标题
4. 点击"开始规划"按钮

**预期反馈**:
- 页面跳转到规划页面
- 显示第一个问题

### 步骤 2: 填写旅行信息

规划页面共有 6 个问题,依次填写:

#### 问题 1: 输入出发地

- **操作**: 在文本框中输入出发城市
- **示例**: 上海、广州、深圳
- **可选**: 可以不填

**预期反馈**:
- 输入框显示你输入的城市名称

#### 问题 2: 输入目的地

- **操作**: 在文本框中输入目的地
- **示例**: 北京、云南、日本东京
- **必填**: 必须填写

**预期反馈**:
- 输入框显示你输入的目的地名称

#### 问题 3: 选择出发日期

- **操作**: 点击日期选择器,选择出发日期
- **格式**: YYYY-MM-DD
- **示例**: 2026-03-15
- **必填**: 必须填写

**预期反馈**:
- 日期选择器显示你选择的日期

#### 问题 4: 选择返程日期

- **操作**: 点击日期选择器,选择返程日期
- **格式**: YYYY-MM-DD
- **示例**: 2026-03-17
- **必填**: 必须填写
- **注意**: 返程日期必须晚于出发日期

**预期反馈**:
- 日期选择器显示你选择的日期
- 系统自动计算游玩天数

#### 问题 5: 选择旅行偏好

- **操作**: 从下拉选择框中选择一个偏好
- **选项**:
  - 历史文化
  - 自然风光
  - 美食探索
  - 城市体验
  - 休闲度假
  - 户外探险
  - 购物娱乐
  - 艺术文化
- **可选**: 可以不填

**预期反馈**:
- 下拉框显示你选择的偏好

#### 问题 6: 选择预算范围

- **操作**: 从单选框中选择一个预算范围
- **选项**:
  - 5000-10000元
  - 10000-20000元
  - 20000-30000元
  - 30000-50000元
  - 50000元以上
- **可选**: 可以不填

**预期反馈**:
- 单选框显示你选择的预算范围

### 步骤 3: 生成行程

- **操作**: 点击"生成行程"按钮
- **等待**: 通常需要 5-10 秒
- **跳转**: 生成成功后自动跳转到行程页面

**预期反馈**:
- 按钮显示"生成中..."状态
- 生成成功后自动跳转到行程页面
- 显示完整的行程数据

### 步骤 4: 查看行程

行程页面显示以下内容:

#### 行程列表

- **显示内容**: 每天的行程安排
- **包含信息**:
  - 天数(第1天、第2天...)
  - 日期
  - 景点列表
  - 景点时间
  - 景点描述

**预期反馈**:
- 显示完整的行程列表
- 每个景点都有详细的时间安排

#### 拖拽排序

- **操作**: 鼠标悬停在景点卡片上,按住鼠标左键拖拽
- **功能**: 改变景点顺序
- **自动**: 时间会自动重新计算

**预期反馈**:
- 拖拽时景点卡片会有阴影效果
- 松开鼠标后景点顺序改变
- 时间自动重新计算
- 不会出现时间乱码

#### 查看地图

- **操作**: 点击任意一天标题旁边的"查看地图"按钮
- **显示**: 右侧显示该天的行程地图
- **内容**:
  - 行程景点标记(紫色圆形,编号1,2,3...)
  - 周围餐厅标记(红色圆形,🍴图标)
  - 周围娱乐场所标记(青色圆形,🎮图标)
  - 道路、建筑、兴趣点等周围环境
  - 文字标注

**预期反馈**:
- 右侧显示地图
- 地图不是空白地图
- 显示各种类型的标记
- 点击标记显示详细信息

#### 预算图表

- **显示**: 页面底部显示预算分配饼图
- **内容**:
  - 交通
  - 住宿
  - 餐饮
  - 门票

**预期反馈**:
- 显示预算分配饼图
- 各部分比例清晰可见

---

## 功能说明

### 1. 智能行程规划

**功能**: 根据用户偏好生成个性化行程

**工作原理**:
- 从高德地图 API 获取景点数据
- 使用 fallback 规则引擎生成行程
- 自动计算路线距离和优化路线
- 自动分配游览时间

### 2. 数据缓存优化

**功能**: 缓存景点数据,减少 API 调用

**工作原理**:
- 首次请求调用高德地图 API
- 将数据保存到数据库(缓存24小时)
- 再次请求相同目的地时使用缓存
- 减少 API 调用次数,节省配额

**优势**:
- 减少高德地图 API 调用次数 80-90%
- 提高响应速度(缓存命中时 <1秒)
- 节省 API 配额消耗

### 3. 拖拽排序

**功能**: 拖拽景点卡片改变顺序

**工作原理**:
- 使用 dnd-kit 库实现拖拽功能
- 自动重新计算时间段
- 考虑景点之间的移动时间
- 自动添加午餐时间(11:30-13:30)

### 4. 实时地图显示

**功能**: 显示行程地图和周围环境

**工作原理**:
- 使用高德地图 API
- 显示行程景点标记
- 搜索周围的餐厅和娱乐场所
- 显示道路、建筑、兴趣点等周围环境

**标记说明**:
- 紫色圆形: 行程景点
- 红色圆形: 餐厅
- 青色圆形: 娱乐场所

### 5. 查看备选景点

**功能**: 查看并替换备选景点

**工作原理**:
- 获取 IoT 实时数据
- 根据人流、天气、开放状态判断是否需要调整
- 推荐备选景点
- 应用调整后更新行程

---

## 数据库查看

### 方法 1: 使用 Prisma Studio

#### 启动 Prisma Studio

```bash
cd backend
npx prisma studio
```

**预期输出**:
```
Prisma Studio is up on http://localhost:5555
```

**访问地址**: http://localhost:5555

#### 查看缓存数据

1. 在 Prisma Studio 中找到 `AmapPOICache` 表
2. 点击表名查看数据
3. 检查以下内容:
   - 总缓存数量
   - 有效缓存数量
   - 按城市分组的缓存数量
   - 最近缓存的景点数据

**预期结果**:
- 显示缓存的景点数据
- 每条记录包含:
  - 景点名称
  - 城市
  - 类型
  - 缓存时间
  - 过期时间
  - 命中次数

### 方法 2: 使用脚本检查

#### 创建检查脚本

文件位置: `backend/src/scripts/check-cache.ts`

```typescript
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function checkCache() {
  try {
    console.log('📊 检查数据库中的缓存数据...\n');

    // 检查 AmapPOICache 表
    const totalCache = await prisma.amapPOICache.count();
    console.log(`总缓存数量: ${totalCache}`);

    if (totalCache === 0) {
      console.log('⚠️  数据库中没有缓存数据');
      return;
    }

    // 检查有效缓存
    const validCache = await prisma.amapPOICache.count({
      where: {
        expireTime: {
          gte: new Date(),
        },
      },
    });
    console.log(`有效缓存数量: ${validCache}`);

    // 检查过期缓存
    const expiredCache = totalCache - validCache;
    console.log(`过期缓存数量: ${expiredCache}`);

    // 按城市统计
    const byCity = await prisma.amapPOICache.groupBy({
      by: ['city'],
      _count: {
        city: true,
      },
      where: {
        expireTime: {
          gte: new Date(),
        },
      },
    });

    console.log('\n按城市统计:');
    byCity.forEach(group => {
      console.log(`  ${group.city}: ${group._count.city} 个`);
    });

    console.log('\n✅ 缓存数据检查完成');
  } catch (error) {
    console.error('❌ 检查缓存数据失败:', error);
  } finally {
    await prisma.$disconnect();
  }
}

checkCache();
```

#### 运行检查脚本

```bash
cd backend
npx ts-node src/scripts/check-cache.ts
```

**预期输出**:
```
📊 检查数据库中的缓存数据...

总缓存数量: 43
有效缓存数量: 43
过期缓存数量: 0

按城市统计:
  北京: 43 个

✅ 缓存数据检查完成
```

### 方法 3: 直接查看数据库文件

#### 数据库文件位置

`backend/prisma/dev.db`

#### 使用 SQLite 工具查看

可以使用以下工具查看 SQLite 数据库:
- DB Browser for SQLite
- DBeaver
- VS Code SQLite 插件

#### 查看缓存数据表

```sql
SELECT * FROM AmapPOICache WHERE expireTime >= datetime('now');
```

---

## 常见问题

### Q1: 启动后端服务时报错 "AMAP_API_KEY 未配置"

**A**: 请检查 `backend/.env` 文件中是否配置了 `AMAP_API_KEY`:
```env
AMAP_API_KEY=b1f81dde480fbb6e2db41fa37940b35f
```

### Q2: 启动前端服务时报错 "高德地图 Key 未配置"

**A**: 请检查 `frontend/.env` 文件中是否配置了 `VITE_AMAP_KEY`:
```env
VITE_AMAP_KEY=b1f81dde480fbb6e2db41fa37940b35f
```

### Q3: 生成行程时报错 "未找到目的地的景点数据"

**A**: 请检查:
- 目的地名称是否正确
- 网络连接是否正常
- 高德地图 API 是否正常
- 后端控制台是否有错误信息

### Q4: 地图不显示

**A**: 请检查:
- 高德地图 Key 是否正确配置
- 网络连接是否正常
- 浏览器控制台是否有错误信息
- 景点的 location 字段是否正确

### Q5: 拖拽排序后时间出现乱码

**A**: 请检查:
- 是否正确拖拽
- 浏览器控制台是否有错误信息
- 如果问题持续,请刷新页面重试

### Q6: 再次生成行程时仍然调用 API

**A**: 请检查:
- 目的地是否与之前相同
- 数据库中是否有缓存数据
- 后端控制台日志是否显示"从缓存获取"
- 缓存是否已过期(24小时)

### Q7: 如何查看数据库中的缓存数据

**A**: 有三种方法:
1. 使用 Prisma Studio: `npx prisma studio`
2. 使用检查脚本: `npx ts-node src/scripts/check-cache.ts`
3. 直接查看数据库文件: `backend/prisma/dev.db`

### Q8: 缓存数据如何过期清理

**A**:
- 缓存过期时间: 24小时
- 过期后会自动调用高德地图 API
- 可以手动清理过期缓存(需要实现清理功能)

### Q9: 如何清空所有缓存数据

**A**: 可以通过以下方式:
```bash
cd backend
npx prisma studio
# 在 Prisma Studio 中删除 AmapPOICache 表的所有数据
```

或者使用 SQL:
```sql
DELETE FROM AmapPOICache;
```

### Q10: 地图显示为空白地图

**A**: 请检查:
- 地图是否完全加载
- 缩放级别是否合适
- 景点位置是否正确
- 网络连接是否正常

---

## 技术支持

如果您在使用过程中遇到任何问题,请:
1. 查看本使用说明文档
2. 查看项目交接文档
3. 检查浏览器控制台的错误信息
4. 检查后端控制台的日志信息

---

**文档版本**: 1.0
**最后更新**: 2026-02-05
**系统版本**: LiveTrip v1.0.0
