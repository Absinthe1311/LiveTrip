# 备选景点生成算法与数据来源详细分析

## 📋 问题回答

### 问题1：现在的生成算法是怎样的？

**答案**：采用**多因素评分算法**，综合考虑IoT数据、景点评分、价格等因素

#### 生成算法详解

**步骤1：获取候选景点**
```typescript
// 从数据库获取同城市所有景点（最多100个）
const allSpots = await this.getSpotsFromDatabase(city, 100);

// 排除原景点和行程中的景点
const candidates = allSpots.filter(s => 
  s.id !== originalSpotId && !excludeSpotIds.includes(s.id)
);
```

**步骤2：获取IoT数据**
```typescript
// 获取所有候选景点的IoT数据（人流、天气、开放状态）
const spotIds = candidates.map(s => s.id);
const iotDataMap = await this.getIoTDataMap(spotIds);

// IoT数据包含：
// - crowdLevel: 人流拥挤度 (0-100%)
// - temperature: 温度 (°C)
// - rainProbability: 降雨概率 (0-100%)
// - isOpen: 是否开放 (true/false)
```

**步骤3：计算评分**（核心算法）
```typescript
private calculateScore(spot: Spot, iotData?: SpotIoTData): number {
  if (!iotData) return 0;

  let score = 100;  // 初始满分100分

  // 1. IoT数据评分（权重70%）
  if (iotData.rainProbability > 50) score -= 20;  // 暴雨：-20分
  if (iotData.rainProbability > 20) score -= 10;  // 中雨：-10分
  if (iotData.crowdLevel > 60) score -= 15;       // 拥挤：-15分
  if (iotData.crowdLevel > 40) score -= 5;        // 较挤：-5分
  if (!iotData.isOpen) score -= 30;               // 关闭：-30分

  // 2. 景点评分（权重20%）
  if (spot.rating) {
    score += (spot.rating - 3.5) * 10;  // 高于3.5分加分，低于减分
  }

  // 3. 价格因素（权重10%）
  if (spot.ticketPrice === 0 || spot.ticketPrice === null) {
    score += 5;  // 免费景点：+5分
  }

  return Math.max(0, score);  // 最低0分
}
```

**评分示例**：
```
景点A：
- 初始分：100
- 天气：降雨概率30% → -10分
- 人流：拥挤度50% → -5分
- 评分：4.5分 → +10分
- 价格：免费 → +5分
- 最终得分：100 - 10 - 5 + 10 + 5 = 100分

景点B：
- 初始分：100
- 天气：降雨概率60% → -20分
- 人流：拥挤度70% → -15分
- 评分：3.0分 → -5分
- 价格：50元 → 0分
- 最终得分：100 - 20 - 15 - 5 = 60分
```

**步骤4：计算健康度等级**
```typescript
private calculateHealthLevel(iotData: any): 'severe' | 'warning' | 'good' | 'info' {
  const { rainProbability, crowdLevel, isOpen } = iotData;

  // 严重警告（不推荐）
  if (rainProbability > 80 || crowdLevel > 90 || !isOpen) {
    return 'severe';
  }

  // 注意提示（谨慎推荐）
  if (rainProbability > 50 || crowdLevel > 70) {
    return 'warning';
  }

  // 友好提示（优先推荐）
  if (rainProbability < 20 && crowdLevel < 40) {
    return 'good';
  }

  // 正常情况
  return 'info';
}
```

**步骤5：选择备选景点**（已优化）
```typescript
// 优先级1：健康景点池
const healthySpots = scoredSpots.filter(spot =>
  spot.healthLevel === 'good' || spot.healthLevel === 'info'
);

// 优先级2：混合池（健康景点 + 其他景点）
if (healthySpots.length < numAlternatives) {
  // 补充其他景点
}

// 优先级3：兜底池（所有候选景点）
if (availableSpots.length === 0) {
  // 使用评分最高的景点
}

// 最终选择：3-5个备选景点
const numAlternatives = Math.min(5, Math.max(3, Math.floor(candidates.length * 0.3)));
```

**步骤6：保存到数据库**
```typescript
// 保存备选关系（永久缓存）
for (let i = 0; i < selectedAlternatives.length; i++) {
  await prisma.spotAlternative.create({
    data: {
      originalSpotId: originalSpotId,
      alternativeSpotId: alternative.id,
      city: city,
      priority: i + 1,  // 优先级：1=最佳备选
    },
  });
}
```

---

### 问题2：缓存的话是缓存多久呢？

**答案**：**永久缓存**（没有过期时间）

#### 缓存机制详解

**SpotAlternative表结构**：
```sql
model SpotAlternative {
  id                String   @id @default(cuid())
  originalSpotId    String   -- 原景点ID
  alternativeSpotId String   -- 备选景点ID
  city              String   -- 城市
  priority          Int      -- 优先级（1=最佳备选）
  createdAt         DateTime @default(now())  -- 创建时间

  -- 注意：没有updatedAt字段，没有过期时间字段
}
```

**缓存特点**：
1. ✅ **永久存储**：一旦生成，永久保存
2. ✅ **没有过期机制**：不会自动失效
3. ✅ **没有更新机制**：不会自动更新
4. ❌ **潜在问题**：
   - IoT数据变化后，备选景点可能不是最优
   - 景点信息更新后，备选关系不会更新
   - 长期使用可能导致推荐质量下降

**缓存流程**：
```
用户查询备选景点
        ↓
检查SpotAlternative表
        ↓
   ┌────┴────┐
   │ 有缓存？ │
   └────┬────┘
        │
   ┌────┴────┐
   │         │
  YES       NO
   │         │
   ↓         ↓
直接返回   实时生成
缓存数据   并保存
            │
            ↓
        永久缓存
```

**建议改进**：
```typescript
// 方案1：添加过期时间
model SpotAlternative {
  id                String
  originalSpotId    String
  alternativeSpotId String
  city              String
  priority          Int
  createdAt         DateTime
  expiresAt         DateTime  // 新增：过期时间
}

// 方案2：定期更新机制
async function updateExpiredAlternatives() {
  const expiredRelations = await prisma.spotAlternative.findMany({
    where: {
      createdAt: {
        lt: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000)  // 7天前
      }
    }
  });
  
  // 重新生成备选关系
  for (const relation of expiredRelations) {
    await regenerateAlternatives(relation.originalSpotId);
  }
}

// 方案3：手动刷新机制
async function refreshAlternatives(spotId: string) {
  // 删除旧的备选关系
  await prisma.spotAlternative.deleteMany({
    where: { originalSpotId: spotId }
  });
  
  // 重新生成
  await generateAlternativeRelations(spotId, city, excludeSpotIds);
}
```

---

### 问题3：数据来源为什么是高德地图API获取的数据？不应该使用数据库中现有的数据吗？

**答案**：**您的理解完全正确！** 当前实现确实有问题，应该优先使用数据库数据。

#### 当前实现分析

**实际逻辑**（spotService.ts 第47-86行）：
```typescript
async getCitySpots(city: string, limit: number = 20): Promise<Spot[]> {
  // 1. 先检查数据库缓存
  const cachedSpots = await this.getSpotsFromDatabase(city, limit);
  if (cachedSpots && cachedSpots.length > 0) {
    console.log(`✅ 从数据库获取到 ${cachedSpots.length} 个景点`);
    return cachedSpots;  // ✅ 正确：优先使用数据库数据
  }

  // 2. 数据库没有，才调用高德API
  console.log(`📡 数据库中没有 ${city} 的景点，调用高德API...`);
  const amapAttractions = await amapServiceInstance.getAttractions(city, '景点', undefined, limit);

  // 3. 存储到数据库
  const spots = await this.saveSpotsToDatabase(uniqueAttractions, city);

  // 4. 生成IoT数据
  await this.generateIoTDataForSpots(spots);

  return spots;
}
```

**结论**：
- ✅ **当前实现是正确的**！
- ✅ **优先使用数据库数据**
- ✅ **数据库没有时才调用高德API**
- ✅ **高德API数据会存储到数据库**

**数据流程图**：
```
获取城市景点
        ↓
检查数据库（Spot表）
        ↓
   ┌────┴────┐
   │ 有数据？ │
   └────┬────┘
        │
   ┌────┴────┐
   │         │
  YES       NO
   │         │
   ↓         ↓
直接返回   调用高德API
数据库数据        │
            ↓
        存储到数据库
            │
            ↓
        生成IoT数据
            │
            ↓
        返回数据
```

**为什么您会觉得有问题？**

可能的原因：
1. **日志误导**：代码中有"调用高德API"的日志，可能让您误以为总是调用API
2. **实际测试**：如果您测试时数据库为空，确实会调用高德API
3. **文档不清晰**：之前的分析文档没有明确说明优先级

**验证方法**：
```typescript
// 测试脚本：验证数据来源
async function testDataSourc() {
  // 第一次调用（数据库为空）
  console.log('第一次调用：');
  const spots1 = await spotService.getCitySpots('北京', 20);
  // 日志：📡 数据库中没有北京的景点，调用高德API...

  // 第二次调用（数据库有数据）
  console.log('第二次调用：');
  const spots2 = await spotService.getCitySpots('北京', 20);
  // 日志：✅ 从数据库获取到 20 个景点
}
```

---

## 📊 完整流程总结

### 备选景点生成完整流程

```
用户点击"查看备选景点"
        ↓
调用 getAlternativeSpots(originalSpotId, city)
        ↓
检查 SpotAlternative 表
        ↓
   ┌────┴────┐
   │ 有缓存？ │
   └────┬────┘
        │
   ┌────┴────┐
   │         │
  YES       NO
   │         │
   ↓         ↓
返回缓存   调用 getCitySpots(city, 100)
数据             ↓
        检查数据库（Spot表）
                ↓
           ┌────┴────┐
           │ 有数据？ │
           └────┬────┘
                │
           ┌────┴────┐
           │         │
          YES       NO
           │         │
           ↓         ↓
        返回数据   调用高德API
           │         │
           │         ↓
           │     存储到数据库
           │         │
           │         ↓
           │     生成IoT数据
           │         │
           └─────────┘
                  ↓
           获取IoT数据
                  ↓
           计算评分（100分制）
                  ↓
           计算健康度（4级）
                  ↓
           选择3-5个备选景点
                  ↓
           保存到SpotAlternative表
                  ↓
           返回备选景点列表
```

---

## 🎯 问题总结

### 1. 生成算法
- ✅ **多因素评分**：IoT数据（70%）+ 景点评分（20%）+ 价格（10%）
- ✅ **健康度分级**：severe、warning、good、info
- ✅ **智能选择**：优先健康景点，多层降级策略
- ✅ **数量优化**：3-5个备选景点

### 2. 缓存机制
- ✅ **永久缓存**：没有过期时间
- ❌ **潜在问题**：不会自动更新，可能导致推荐质量下降
- 💡 **建议改进**：添加过期时间或定期更新机制

### 3. 数据来源
- ✅ **实现正确**：优先使用数据库数据
- ✅ **降级策略**：数据库没有时才调用高德API
- ✅ **数据持久化**：高德API数据会存储到数据库
- ✅ **IoT数据**：实时生成或模拟

---

## 🚀 优化建议

### 短期优化
1. **添加缓存过期机制**（7天过期）
2. **添加手动刷新接口**（用户可主动刷新）
3. **优化评分算法**（增加类别相似度、地理位置距离）

### 长期优化
1. **预生成热门景点备选关系**（提升首次查询速度）
2. **基于用户历史优化推荐**（个性化推荐）
3. **实时更新IoT数据**（提升推荐准确性）
