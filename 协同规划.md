# LiveTrip 协同规划功能文档

## 📋 文档说明

本文档详细描述 LiveTrip 协同规划功能的设计理念、功能实现、技术架构和美化方向。

**最后更新时间**：2026-04-15

---

## 1. 功能概述

### 1.1 核心价值

协同规划是 LiveTrip 的核心功能之一，解决团队旅行协调难题：

- **多人实时协同**：支持多人同时编辑行程，实时同步所有修改
- **草稿提交机制**：成员提交自己的路线方案，Host审核后合并
- **实时沟通**：内置聊天功能，方便团队讨论
- **地图可视化**：直观展示所有成员的路线方案
- **权限管理**：Host和Collaborator角色区分，确保规划有序进行

### 1.2 使用场景

| 场景 | 描述 | 参与人数 |
|------|------|---------|
| 家庭旅行 | 家庭成员共同规划行程，考虑每个人的偏好 | 3-6人 |
| 朋友结伴 | 朋友一起出游，共同决定去哪些景点 | 2-8人 |
| 团队建设 | 公司团队出游，需要协调多人意见 | 5-20人 |
| 旅行社团 | 旅行社团组织活动，多人协同规划 | 10-20人 |

---

## 2. 功能架构

### 2.1 核心功能模块

```
┌─────────────────────────────────────────────────────────────┐
│                    协同规划功能架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 房间管理  │  │ 草案系统  │  │ 实时通信  │  │ 地图可视化 │   │
│  │  模块    │  │   模块   │  │   模块   │  │   模块   │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 权限控制  │  │ 消息系统  │  │ 路线编辑  │  │ 最终合并  │   │
│  │  模块    │  │   模块   │  │   模块   │  │   模块   │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 功能流程

```
┌─────────────┐
│ Host创建房间 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 生成邀请链接 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 成员加入房间 │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ 实时编辑（WebSocket）│
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│ 实时消息沟通 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 提交路线草稿 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Host审核草稿 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 合并最终行程 │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 为所有成员创建副本│
└─────────────┘
```

---

## 3. 数据模型

### 3.1 核心数据表

#### CollabRoom（协同房间）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 房间ID |
| tripId | String | 关联的行程ID |
| hostId | String | 房主ID |
| phase | String | 房间阶段（EDITING/FINALIZING/LOCKED） |
| inviteToken | String | 邀请令牌 |
| inviteExpiresAt | DateTime | 邀请过期时间 |
| createdAt | DateTime | 创建时间 |

#### TripMember（房间成员）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 成员ID |
| roomId | String | 房间ID |
| userId | String | 用户ID |
| role | String | 角色（HOST/COLLABORATOR） |
| assignedDays | String | 分配的天数（JSON数组） |
| joinedAt | DateTime | 加入时间 |

#### DraftRoute（路线草稿）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 草稿ID |
| roomId | String | 房间ID |
| userId | String | 用户ID |
| dayNumber | Int | 天数 |
| spotSequence | String | 景点序列（JSON） |
| polylineData | String | 路线数据（JSON） |
| isSubmitted | Boolean | 是否已提交 |
| version | Int | 版本号 |

#### CollabMessage（聊天消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 消息ID |
| roomId | String | 房间ID |
| userId | String | 发送者ID |
| content | String | 消息内容 |
| createdAt | DateTime | 发送时间 |

---

## 4. 技术实现

### 4.1 后端架构

**核心服务**：

| 服务 | 文件 | 职责 |
|------|------|------|
| 协同服务 | collabService.ts | 房间管理、草稿管理、消息管理 |
| Socket服务 | socketService.ts | WebSocket连接管理、房间广播 |
| 协同控制器 | collabController.ts | HTTP请求处理 |

**核心API接口**：

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/collab/rooms | POST | 创建协同房间 |
| /api/collab/rooms/join | POST | 加入协同房间 |
| /api/collab/rooms/:roomId | GET | 获取房间信息 |
| /api/collab/rooms/:roomId/stats | GET | 获取景点统计 |
| /api/collab/rooms/:roomId/lock | POST | 锁定房间 |
| /api/collab/drafts | POST | 创建/更新草稿 |
| /api/collab/drafts/:draftId/submit | POST | 提交草稿 |
| /api/collab/rooms/:roomId/drafts | GET | 获取用户草稿 |
| /api/collab/rooms/:roomId/drafts/all | GET | 获取所有成员草稿 |
| /api/collab/messages | POST | 发送消息 |
| /api/collab/messages/:roomId | GET | 获取消息列表 |
| /api/collab/finalize | POST | 保存最终行程 |

### 4.2 前端架构

**核心页面**：

| 页面 | 文件 | 功能 |
|------|------|------|
| 协同入口 | CollabEntry.tsx | 选择创建或加入房间 |
| 创建房间 | CollabSetup.tsx | 填写房间信息并创建 |
| 加入房间 | JoinCollabRoom.tsx | 通过邀请链接加入 |
| 协同房间 | CollabRoom.tsx | 主协同编辑界面 |

**核心组件**：

| 组件 | 文件 | 功能 |
|------|------|------|
| 路线编辑器 | RouteEditor.tsx | 编辑每日路线 |
| 天数规划器 | DayRoutePlanner.tsx | 多天行程规划 |
| 景点统计面板 | SpotStatsPanel.tsx | 显示景点选择统计 |
| 图层控制 | LayerControl.tsx | 地图图层控制 |
| 最终路线编辑器 | FinalRouteEditor.tsx | Host合并最终路线 |

**状态管理**：

- **collabStore.ts**：Zustand状态管理
  - 当前房间信息
  - 成员列表
  - 消息列表
  - 草稿数据
  - 在线用户
  - 可见图层

**WebSocket通信**：

- **collabSocket.ts**：Socket.io客户端
  - 连接管理
  - 房间加入/离开
  - 草稿更新
  - 消息接收

### 4.3 实时通信

**WebSocket事件**：

| 事件 | 方向 | 数据 | 说明 |
|------|------|------|------|
| join-room | 客户端→服务器 | roomId, userId | 加入房间 |
| leave-room | 客户端→服务器 | roomId, userId | 离开房间 |
| edit-spot | 客户端→服务器 | spotData | 编辑景点 |
| draft:update | 服务器→客户端 | draftData | 草稿更新 |
| message:new | 服务器→客户端 | messageData | 新消息 |
| room:lock | 服务器→客户端 | timestamp | 房间锁定 |
| draft:submitted | 服务器→客户端 | userId, dayNumber | 草稿提交 |

---

## 5. 功能详解

### 5.1 房间管理

**创建房间**：
1. Host填写房间信息（名称、目的地、日期、人数）
2. 系统创建Trip和CollabRoom
3. 生成唯一邀请令牌
4. Host自动成为房间成员（HOST角色）

**加入房间**：
1. 成员通过邀请链接访问
2. 验证令牌有效性
3. 创建TripMember记录（COLLABORATOR角色）
4. WebSocket连接并加入房间

**房间状态**：
- **EDITING**：编辑中，成员可以修改草稿
- **FINALIZING**：合并中，Host正在审核
- **LOCKED**：已锁定，不可修改

### 5.2 草案系统

**草稿创建**：
- 每个成员可以为每一天创建路线草稿
- 草稿包含：景点序列、路线数据、版本号
- 支持多次更新（版本控制）

**草稿提交**：
- 成员完成编辑后提交草稿
- 提交后不可修改（除非房间未锁定）
- 广播提交事件通知其他成员

**草稿查看**：
- Host可以查看所有成员的草稿
- 成员只能查看自己的草稿
- 地图上可视化显示所有路线

### 5.3 实时沟通

**消息功能**：
- 实时发送和接收消息
- 消息持久化存储
- 自动滚动到最新消息
- 显示发送者和时间

**在线状态**：
- 实时显示在线成员
- 成员加入/离开通知
- 在线用户高亮显示

### 5.4 地图可视化

**地图功能**：
- 显示城市所有景点
- 显示成员路线（不同颜色区分）
- 景点点击查看详情
- 路线拖拽编辑
- 图层控制（显示/隐藏）

**路线编辑**：
- 拖拽调整景点顺序
- 添加/删除景点
- 自动计算路线
- 保存路线数据

### 5.5 最终合并

**Host审核**：
- 查看所有成员提交的草稿
- 景点统计（被选次数）
- 选择最佳路线方案

**合并流程**：
1. Host选择最终路线
2. 更新Host的Trip
3. 为所有成员创建Trip副本
4. 锁定房间
5. 广播完成事件

---

## 6. UI/UX设计

### 6.1 当前UI结构

**CollabRoom页面布局**：

```
┌─────────────────────────────────────────────────────────┐
│ 顶部导航栏（房间名称、成员、邀请链接）                    │
├───────────┬─────────────────────────────┬───────────────┤
│           │                             │               │
│  左侧面板  │        地图区域              │   右侧面板     │
│           │                             │               │
│ - 成员列表 │  - 景点标记                 │  - 路线编辑    │
│ - 消息列表 │  - 路线显示                 │  - 天数选择    │
│ - 消息输入 │  - 图层控制                 │  - 景点列表    │
│           │                             │               │
├───────────┴─────────────────────────────┴───────────────┤
│ 底部操作栏（保存草稿、提交草稿、锁定房间）                │
└─────────────────────────────────────────────────────────┘
```

### 6.2 视觉风格

**当前风格**：
- 毛玻璃效果（Glass Morphism）
- 深色主题
- 渐变背景
- 圆角卡片
- 半透明面板

**颜色方案**：
- 主色：蓝色系（#3B82F6）
- 辅助色：紫色系（#8B5CF6）
- 背景：深色渐变（slate-900 → slate-800）
- 文字：白色/半透明白色

### 6.3 交互设计

**成员交互**：
- 点击成员头像查看详情
- 在线状态实时更新
- 成员加入/离开动画

**消息交互**：
- 消息发送动画
- 新消息提示
- 消息时间显示

**地图交互**：
- 景点悬停高亮
- 路线拖拽编辑
- 图层切换动画

**草稿交互**：
- 草稿保存提示
- 提交确认对话框
- 版本更新通知

---

## 7. 美化方向

### 7.1 视觉优化建议

#### 7.1.1 整体风格提升

**建议1：增强毛玻璃效果**
- 增加背景模糊度
- 添加边框光晕
- 优化透明度层次

**建议2：优化颜色方案**
- 引入更多渐变色
- 为不同成员分配不同主题色
- 增强视觉层次感

**建议3：添加动画效果**
- 成员加入/离开动画
- 消息发送动画
- 路线绘制动画
- 景点选择动画

#### 7.1.2 组件美化

**顶部导航栏**：
- 添加房间图标和背景
- 成员头像环形排列
- 邀请链接一键复制动画
- 在线人数动态显示

**左侧面板**：
- 成员列表卡片化
- 在线状态呼吸灯效果
- 消息气泡样式优化
- 输入框聚焦动画

**地图区域**：
- 景点标记自定义图标
- 路线渐变色显示
- 添加地图主题切换
- 景点信息卡片美化

**右侧面板**：
- 天数选择器美化
- 景点列表卡片化
- 拖拽手柄优化
- 路线预览增强

**底部操作栏**：
- 按钮渐变效果
- 操作状态动画
- 进度条显示

### 7.2 交互优化建议

#### 7.2.1 实时反馈增强

**建议1：操作反馈**
- 草稿保存成功动画
- 提交草稿确认动画
- 房间锁定警告动画

**建议2：状态提示**
- 成员编辑状态显示
- 草稿版本更新提示
- 路线冲突警告

**建议3：进度可视化**
- 协同进度条
- 成员完成度统计
- 整体完成度显示

#### 7.2.2 协作体验优化

**建议1：成员协作**
- 成员光标显示
- 编辑区域锁定
- 实时编辑预览

**建议2：沟通增强**
- 表情支持
- 图片消息
- @提及功能

**建议3：草稿对比**
- 多草稿并排对比
- 差异高亮显示
- 一键合并功能

### 7.3 功能增强建议

#### 7.3.1 新功能建议

**建议1：投票系统**
- 成员对景点投票
- 投票结果可视化
- 自动推荐热门景点

**建议2：任务分配**
- 为成员分配任务
- 任务完成度跟踪
- 任务提醒通知

**建议3：时间线视图**
- 行程时间线展示
- 时间冲突检测
- 自动时间调整

**建议4：模板系统**
- 保存协同模板
- 快速创建相似行程
- 模板分享功能

#### 7.3.2 性能优化

**建议1：数据加载**
- 景点数据懒加载
- 地图瓦片缓存
- 草稿数据压缩

**建议2：实时通信**
- WebSocket心跳优化
- 消息队列管理
- 断线重连机制

**建议3：渲染优化**
- 虚拟滚动列表
- 地图渲染优化
- 动画性能优化

---

## 8. 技术挑战与解决方案

### 8.1 实时同步挑战

**挑战**：多人同时编辑可能导致数据冲突

**解决方案**：
- 操作锁机制（房间锁定）
- 版本控制系统
- 冲突检测和提示
- 最终由Host审核合并

### 8.2 性能挑战

**挑战**：大量景点和路线数据导致渲染缓慢

**解决方案**：
- 地图标记聚合
- 虚拟滚动列表
- 数据懒加载
- 渲染节流

### 8.3 用户体验挑战

**挑战**：协同过程复杂，用户可能困惑

**解决方案**：
- 清晰的流程引导
- 实时状态提示
- 操作帮助文档
- 示例演示

---

## 9. 未来规划

### 9.1 短期优化（1-2周）

- [ ] UI视觉美化（毛玻璃效果增强）
- [ ] 动画效果添加
- [ ] 交互反馈优化
- [ ] 性能优化

### 9.2 中期增强（1-2月）

- [ ] 投票系统
- [ ] 任务分配
- [ ] 表情和图片消息
- [ ] 草稿对比功能

### 9.3 长期规划（3-6月）

- [ ] 时间线视图
- [ ] 模板系统
- [ ] AI辅助协同
- [ ] 移动端适配

---

## 10. 总结

协同规划是 LiveTrip 的核心功能，通过实时协同、草稿系统、地图可视化等技术手段，有效解决了团队旅行协调难题。当前实现已经具备完整的功能闭环，但在视觉设计、交互体验、功能丰富度等方面仍有提升空间。

通过本次美化工作，我们将：
1. 提升视觉设计质量，打造更现代、更美观的界面
2. 优化交互体验，提供更流畅、更直观的操作
3. 增强功能丰富度，满足更多协同场景需求
4. 提升性能表现，确保大规模协同的流畅性

最终目标是打造一个既美观又实用的协同规划系统，让团队旅行规划成为一种愉悦的体验。
