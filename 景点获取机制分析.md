# 景点获取机制分析报告

**分析时间**: 2026-04-23 18:45
**分析范围**: 景点获取方式、时机和城市匹配逻辑

---

## 一、景点获取方式

### 1.1 主要获取方法

#### 方法1: `spotService.getCitySpots(city, limit)`

**位置**: `backend/src/services/spotService.ts`

**工作流程**:
```
1. 检查数据库缓存
   ↓ (如果有数据)
   → 直接返回数据库中的景点
   
   ↓ (如果没有数据)
2. 调用高德地图API
   ↓
3. 去重处理
   ↓
4. 存储到数据库
   ↓
5. 生成IoT数据
   ↓
6. 返回景点列表
```

**关键代码**:
```typescript
async getCitySpots(city: string, limit: number = 20): Promise<Spot[]> {
  // 1. 检查数据库缓存
  const cachedSpots = await this.getSpotsFromDatabase(city, limit);
  if (cachedSpots && cachedSpots.length > 0) {
    return cachedSpots;  // 直接返回缓存
  }
  
  // 2. 调用高德API
  const amapAttractions = await amapServiceInstance.getAttractions(city, '景点', undefined, limit);
  
  // 3. 去重处理
  const uniqueAttractions = deduplicateSpots(amapAttractions);
  
  // 4. 存储到数据库
  const spots = await this.saveSpotsToDatabase(uniqueAttractions, city);
  
  // 5. 生成IoT数据
  await this.generateIoTDataForSpots(spots);
  
  return spots;
}
```

#### 方法2: 数据库直接查询

**位置**: `backend/src/controllers/destinationController.ts`

**工作流程**:
```
1. 接收城市参数
   ↓
2. 查询数据库中的热门景点
   ↓
3. 返回景点列表
```

**关键代码**:
```typescript
// 获取城市热门景点（用于首页展示）
export const getCitySpots = async (req: Request, res: Response) => {
  const city = req.params.city;  // 从URL参数获取城市
  const limit = parseInt(req.query.limit) || 9;
  
  const spots = await prisma.spot.findMany({
    where: {
      city: city,      // 按城市筛选
      isHot: true,     // 只查询热门景点
    },
    take: limit,
  });
  
  return spots;
};
```

---

## 二、城市匹配逻辑

### 2.1 城市名称匹配规则

**问题**: 如果目的地设置为"北京市"，是否能匹配数据库中的"北京"？

**答案**: ❌ **不能直接匹配**

**原因**:
1. 数据库查询使用**精确匹配** (`city: city`)
2. "北京市" ≠ "北京"（字符串不完全相同）
3. 会导致查询结果为空

**示例**:
```typescript
// 数据库中的数据
{ city: "北京", name: "故宫" }

// 查询条件
where: { city: "北京市" }  // ❌ 查询不到

// 正确的查询
where: { city: "北京" }    // ✅ 可以查询到
```

### 2.2 城市名称处理

**当前实现**:
- 高德API返回的城市名称可能包含"市"后缀
- 数据库存储时保留原始名称
- 查询时需要完全匹配

**建议改进**:
```typescript
// 城市名称标准化函数
function normalizeCityName(city: string): string {
  return city
    .replace(/市$/, '')     // 移除"市"后缀
    .replace(/省$/, '')     // 移除"省"后缀
    .trim();
}

// 使用示例
const normalizedCity = normalizeCityName("北京市");  // "北京"
```

---

## 三、景点获取时机

### 3.1 自动获取时机

#### 时机1: 创建行程时（最主要）

**触发条件**: 用户在前端创建新行程

**执行位置**: `backend/src/controllers/planController.ts`

**流程**:
```typescript
export const createPlan = async (req: Request, res: Response) => {
  const planData = req.body;
  
  // 步骤1: 获取景点数据并存储到数据库
  const spots = await spotService.getCitySpots(
    planData.destination,  // 用户输入的目的地
    50                     // 获取50个景点
  );
  
  // 后续步骤: 推荐行程、优化路径等
}
```

**特点**:
- ✅ 自动调用高德API
- ✅ 自动存储到数据库
- ✅ 生成IoT数据
- ⚠️ 仅当数据库中没有该城市景点时才调用API

#### 时机2: 预热脚本（手动执行）

**脚本位置**: `backend/scripts/preloadCitySpots.ts`

**执行命令**:
```bash
cd backend
npx ts-node scripts/preloadCitySpots.ts
```

**预热城市**:
```typescript
const hotCities = [
  { name: '北京市', limit: 30 },
  { name: '上海市', limit: 30 },
  { name: '成都市', limit: 30 },
  { name: '杭州市', limit: 30 },
  { name: '厦门市', limit: 30 },
  { name: '西安市', limit: 30 },
];
```

**特点**:
- ✅ 批量获取多个城市
- ✅ 检查数据库避免重复
- ⚠️ 需要手动执行

#### 时机3: 热门景点标记（手动执行）

**脚本位置**: `backend/scripts/seedHotSpots.ts`

**执行命令**:
```bash
cd backend
npx ts-node scripts/seedHotSpots.ts
```

**功能**:
- 获取热门城市的景点
- 标记为热门景点（`isHot: true`）
- 存储到数据库

### 3.2 不自动获取的场景

#### 场景1: 热门目的地展示

**API**: `GET /api/destinations/cities/:city/spots`

**行为**:
- ❌ 不调用高德API
- ✅ 只查询数据库中已有的景点
- ⚠️ 如果数据库中没有，返回空数组

**代码**:
```typescript
const spots = await prisma.spot.findMany({
  where: {
    city: city,
    isHot: true,
  },
  take: limit,
});
// 不调用API，只查数据库
```

#### 场景2: 收藏列表

**行为**:
- ❌ 不调用API
- ✅ 只查询数据库

#### 场景3: 行程详情查看

**行为**:
- ❌ 不调用API
- ✅ 只查询数据库

---

## 四、完整流程图

### 4.1 创建行程流程

```
用户输入目的地: "北京"
        ↓
前端发送请求: POST /api/plan
        ↓
后端接收: planController.createPlan()
        ↓
调用: spotService.getCitySpots("北京", 50)
        ↓
检查数据库: SELECT * FROM Spot WHERE city = "北京"
        ↓
    ┌───────────────────┐
    │ 数据库中有数据？   │
    └───────────────────┘
        ↓           ↓
       有           无
        ↓           ↓
   返回缓存      调用高德API
                    ↓
              获取50个景点
                    ↓
                去重处理
                    ↓
              存储到数据库
                    ↓
              生成IoT数据
                    ↓
                返回景点
        ↓           ↓
    推荐行程算法
        ↓
    路径优化
        ↓
    返回行程给前端
```

### 4.2 热门目的地展示流程

```
用户访问热门目的地页面
        ↓
前端请求: GET /api/destinations/cities/北京/spots?limit=9
        ↓
后端接收: destinationController.getCitySpots()
        ↓
查询数据库: SELECT * FROM Spot WHERE city = "北京" AND isHot = true
        ↓
    ┌───────────────────┐
    │ 数据库中有数据？   │
    └───────────────────┘
        ↓           ↓
       有           无
        ↓           ↓
   返回景点      返回空数组
                (不调用API)
```

---

## 五、关键发现

### 5.1 城市名称问题

**问题**: "北京市" vs "北京"

**影响**:
- ❌ 用户输入"北京市"可能查询不到"北京"的景点
- ❌ 不同来源的城市名称格式不统一

**解决方案**:
1. 前端统一城市名称格式
2. 后端添加城市名称标准化
3. 数据库查询使用模糊匹配或标准化后的名称

### 5.2 景点获取策略

**当前策略**: 按需获取 + 缓存

**优点**:
- ✅ 减少API调用次数
- ✅ 提高响应速度
- ✅ 节省API配额

**缺点**:
- ⚠️ 首次访问需要等待API调用
- ⚠️ 热门目的地可能没有数据

**建议**:
1. 定期执行预热脚本
2. 添加后台定时任务自动更新
3. 实现城市名称智能匹配

### 5.3 数据一致性

**问题**: 不同时机获取的数据可能不一致

**示例**:
- 预热脚本获取: 30个景点
- 创建行程获取: 50个景点
- 可能导致数据重复或覆盖

**建议**:
- 统一获取数量
- 实现数据更新策略
- 添加数据版本管理

---

## 六、总结

### 6.1 核心问题回答

**问题1**: 目的地设置为"北京市"是否能获取"北京"的景点？

**答案**: ❌ 不能。当前使用精确匹配，"北京市" ≠ "北京"。

**问题2**: 什么时候会获取景点并保存到数据库？

**答案**: 
1. ✅ **创建行程时**（自动，最主要）
2. ✅ **执行预热脚本时**（手动）
3. ✅ **执行热门景点标记脚本时**（手动）
4. ❌ 热门目的地展示时不会获取（只查数据库）

### 6.2 改进建议

**优先级高**:
1. 实现城市名称标准化
2. 定期执行预热脚本
3. 添加城市名称智能匹配

**优先级中**:
1. 实现后台定时更新
2. 优化数据一致性
3. 添加数据版本管理

**优先级低**:
1. 实现多城市联动
2. 添加景点数据质量监控
3. 实现智能缓存更新

---

**分析完成时间**: 2026-04-23 18:45
**分析状态**: ✅ 完成
