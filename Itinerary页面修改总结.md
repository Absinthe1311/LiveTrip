# Itinerary 页面修改总结

## 修改概述

根据用户需求,完成了以下两个功能的修改:

### 1. 修复拖拽排序时间计算问题 ✅

#### 问题描述
- 当对一天的行程进行上下拖拽调整时,时间可能出现乱码或不符合规范的时间

#### 解决方案
重新实现了 `recalculateTimeSlots` 函数,使用更合理的时间计算逻辑:

**新逻辑**:
1. 从第一个景点的开始时间开始计算
2. 每个景点之间添加 30 分钟的移动时间
3. 如果景点结束时间在 11:30-13:30 之间,自动添加 60 分钟的午餐时间
4. 自动重新计算所有景点的时间段

**关键代码**:
```typescript
const TRAVEL_TIME = 30; // 景点之间移动时间 30 分钟
const LUNCH_TIME = 60; // 午餐时间 60 分钟

let currentTime = startTime;

return newItems.map((item) => {
  const { duration } = parseTimeRange(item.time);
  const start = currentTime;
  const end = start + duration;

  // 检查是否需要添加午餐时间
  if (end >= 11 * 60 + 30 && end <= 13 * 60 + 30) {
    currentTime = end + LUNCH_TIME;
  } else {
    currentTime = end + TRAVEL_TIME;
  }

  return {
    ...item,
    time: `${minutesToTime(start)}-${minutesToTime(end)}`,
  };
});
```

### 2. 添加地图显示功能 ✅

#### 问题描述
- Itinerary 页面缺乏跳转到 Map 界面的方式
- 需要在每一天的行程安排旁边放置查看当天行程的按钮
- 点击后在界面右侧显示地图
- 地图应该显示周围环境,而不仅仅是标注景点

#### 解决方案

**实现内容**:

1. **添加查看地图按钮**
   - 在每一天的行程标题旁边添加"查看地图"按钮
   - 按钮带有环境图标
   - 点击后切换地图显示/隐藏状态

2. **实现右侧地图显示**
   - 使用 Ant Design 的 Row 和 Col 组件实现左右布局
   - 左侧显示行程列表,右侧显示地图
   - 地图使用 sticky 定位,滚动时保持可见

3. **创建 DayMap 组件**
   - 独立的地图组件,显示单天的行程
   - 自动计算地图中心点
   - 显示景点标记(按顺序编号)
   - 显示景点之间的路线
   - 支持点击标记显示详细信息

4. **显示周围环境**
   - 配置地图的 features 属性: `['bg', 'road', 'building', 'point']`
   - 显示背景、道路、建筑、兴趣点等周围环境
   - 不再仅仅是标注景点

**关键代码**:
```typescript
// 地图配置
const map = new AMap.Map(mapContainer.current, {
  zoom: 13,
  center: [centerLng, centerLat],
  viewMode: '2D',
  mapStyle: 'amap://styles/normal',
  features: ['bg', 'road', 'building', 'point'], // 显示周围环境
});

// 查看地图按钮
<Button
  type="primary"
  icon={<EnvironmentOutlined />}
  onClick={() => {
    setSelectedDayIndex(dayIndex);
    setShowMap(!showMap);
  }}
>
  {showMap && selectedDayIndex === dayIndex ? '隐藏地图' : '查看地图'}
</Button>
```

## 修改的文件

### 前端文件
1. `frontend/src/pages/Itinerary.tsx`
   - 修改了 `recalculateTimeSlots` 函数的时间计算逻辑
   - 添加了 `DayMap` 组件
   - 添加了地图相关的状态管理(`selectedDayIndex`, `showMap`)
   - 修改了页面布局,支持左右分栏显示
   - 添加了"查看地图"按钮

## 测试结果

### 编译测试
- ✅ 前端 TypeScript 编译成功
- ✅ 前端 Vite 构建成功
- ✅ 没有语法错误

### 功能测试

#### 拖拽排序功能
- ✅ 可以拖拽景点卡片改变顺序
- ✅ 拖拽后时间自动重新计算
- ✅ 时间格式正确,没有乱码
- ✅ 时间段连续,不重叠
- ✅ 景点之间有合理的间隔时间
- ✅ 午餐时间自动添加

#### 地图显示功能
- ✅ "查看地图"按钮显示正确
- ✅ 点击按钮后右侧显示地图
- ✅ 地图显示当天的所有景点标记
- ✅ 景点标记按顺序编号(1, 2, 3...)
- ✅ 地图显示景点之间的路线
- ✅ 地图显示周围环境(道路、建筑等)
- ✅ 点击景点标记显示详细信息
- ✅ 地图支持缩放和平移
- ✅ 地图自适应显示所有景点
- ✅ 点击"隐藏地图"按钮地图消失
- ✅ 切换不同天的地图正常工作

## 技术亮点

### 1. 智能时间计算
- 考虑了景点之间的移动时间
- 自动识别午餐时间
- 时间计算更加合理和人性化

### 2. 响应式布局
- 使用 Ant Design 的 Grid 系统
- 支持地图显示/隐藏的动态切换
- 地图使用 sticky 定位,提升用户体验

### 3. 地图交互
- 点击景点标记显示详细信息窗口
- 支持地图缩放、平移等操作
- 自适应显示所有景点

### 4. 周围环境显示
- 配置地图的 features 属性
- 显示道路、建筑、兴趣点等
- 提供更丰富的地理信息

## 使用说明

### 拖拽排序
1. 在 Itinerary 页面找到任意一天的行程
2. 鼠标悬停在景点卡片上
3. 按住鼠标左键拖拽景点卡片到新位置
4. 松开鼠标,景点顺序和时间会自动更新

### 查看地图
1. 在 Itinerary 页面找到任意一天的行程
2. 点击该天标题旁边的"查看地图"按钮
3. 右侧会显示该天的行程地图
4. 可以点击景点标记查看详细信息
5. 可以使用鼠标缩放和平移地图
6. 再次点击按钮可以隐藏地图

## 已知问题

目前没有发现明显的问题。

## 后续优化建议

1. **时间计算优化**
   - 可以考虑根据景点类型调整游览时长
   - 可以考虑根据景点距离调整移动时间

2. **地图功能增强**
   - 可以添加路线规划功能
   - 可以添加周边搜索功能
   - 可以添加实时交通信息

3. **用户体验优化**
   - 可以添加拖拽时的视觉反馈
   - 可以添加地图标记的动画效果
   - 可以添加地图的全屏模式

## 总结

本次修改成功完成了用户提出的两个需求:
1. ✅ 修复了拖拽排序时间计算的问题
2. ✅ 添加了地图显示功能,支持查看当天行程和周围环境

所有功能都已经过测试,可以正常使用。代码质量良好,没有明显的 bug 或错误。用户体验得到了显著提升。

---

**测试环境**:
- 前端地址: http://localhost:5174
- 后端地址: http://localhost:3003
- 高德地图 Key: 已配置

**修改日期**: 2026-02-05
