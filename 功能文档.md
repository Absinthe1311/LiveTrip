# LiveTrip 功能文档

## 文档说明

本文档详细记录 LiveTrip 智能旅行规划系统的所有功能模块、API接口和使用说明。

**最后更新时间**：2026-04-15

---

## 目录

1. [用户认证功能](#1-用户认证功能)
2. [用户管理功能](#2-用户管理功能)
3. [行程规划功能](#3-行程规划功能)
4. [行程管理功能](#4-行程管理功能)
5. [景点数据功能](#5-景点数据功能)
6. [收藏功能](#6-收藏功能)
7. [评价功能](#7-评价功能)
8. [博客功能](#8-博客功能)
9. [协同规划功能](#9-协同规划功能)
10. [打包清单功能](#10-打包清单功能)
11. [分享功能](#11-分享功能)
12. [推荐功能](#12-推荐功能)
13. [IoT数据功能](#13-iot数据功能)
14. [通知功能](#14-通知功能)
15. [图片管理功能](#15-图片管理功能)
16. [地点搜索功能](#16-地点搜索功能)
17. [AI助手功能](#17-ai助手功能)
18. [管理员功能](#18-管理员功能)

---

## 1. 用户认证功能

### 1.1 功能概述

用户认证模块提供用户注册、登录、Token验证等功能，是系统的基础安全模块。

### 1.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 用户注册 | 新用户注册账号 | authController.ts |
| 用户登录 | 已注册用户登录系统 | authController.ts |
| 获取当前用户 | 获取当前登录用户信息 | authController.ts |
| 更新用户信息 | 更新用户基本信息 | authController.ts |
| Token验证 | 中间件验证JWT Token | authController.ts |

### 1.3 API接口详情

#### POST /api/auth/register
**功能**：用户注册

**请求参数**：
```json
{
  "username": "string (必填)",
  "password": "string (必填，至少6位)",
  "email": "string (可选)",
  "avatar": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "avatar": "string",
      "role": "string",
      "createdAt": "datetime"
    },
    "token": "string (JWT Token)"
  }
}
```

---

#### POST /api/auth/login
**功能**：用户登录

**请求参数**：
```json
{
  "username": "string (必填)",
  "password": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "avatar": "string",
      "role": "string",
      "createdAt": "datetime"
    },
    "token": "string (JWT Token)"
  }
}
```

---

#### GET /api/auth/me
**功能**：获取当前用户信息

**请求头**：
```
Authorization: Bearer <token>
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "avatar": "string",
      "createdAt": "datetime"
    }
  }
}
```

---

#### PUT /api/auth/profile
**功能**：更新用户信息

**请求头**：
```
Authorization: Bearer <token>
```

**请求参数**：
```json
{
  "username": "string (可选)",
  "email": "string (可选)",
  "avatar": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "avatar": "string",
      "role": "string",
      "createdAt": "datetime"
    }
  }
}
```

---

## 2. 用户管理功能

### 2.1 功能概述

用户管理模块提供用户个人信息管理、头像上传、统计数据等功能。

### 2.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取用户资料 | 获取用户完整信息（含统计数据） | userController.ts |
| 更新用户资料 | 更新用户基本信息 | userController.ts |
| 上传头像 | 上传用户头像图片 | userController.ts |

### 2.3 API接口详情

#### GET /api/users/profile
**功能**：获取用户完整信息

**请求头**：
```
Authorization: Bearer <token>
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "username": "string",
    "email": "string",
    "avatar": "string",
    "nickname": "string",
    "gender": "string",
    "bio": "string",
    "role": "string",
    "totalTrips": "number",
    "totalCities": "number",
    "completedTrips": "number",
    "createdAt": "datetime"
  }
}
```

---

#### PUT /api/users/profile
**功能**：更新用户基本信息

**请求头**：
```
Authorization: Bearer <token>
```

**请求参数**：
```json
{
  "nickname": "string (可选)",
  "gender": "string (可选)",
  "bio": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "username": "string",
    ...
  },
  "message": "用户信息更新成功"
}
```

---

#### POST /api/users/avatar
**功能**：上传头像

**请求头**：
```
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

**请求参数**：
```
file: 图片文件 (必填)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "url": "string (图片URL)",
    "publicId": "string (Cloudinary ID)"
  },
  "message": "头像上传成功"
}
```

---

## 3. 行程规划功能

### 3.1 功能概述

行程规划模块是系统的核心功能，提供**两种行程规划方法**：
1. **AI Agent对话规划**（智能助手模式）：通过自然语言对话创建行程
2. **传统评分算法规划**（手动表单模式）：通过表单填写参数，使用多因素评分引擎生成行程

### 3.2 行程规划两种方法

#### 方法一：AI Agent对话规划（智能助手模式）

**触发方式**：用户通过AI助手输入自然语言（如"我想去北京玩三天"）

**技术实现**：
- `agentService.ts` - AI Agent核心服务（智谱AI GLM-4 + Function Calling）
- `aiTripRecommender.ts` - AI行程推荐服务

**工作流程**：
```
用户输入自然语言（"我想去北京玩三天"）
        ↓
智谱AI GLM-4意图识别
        ↓
Function Calling参数提取
        ↓
调用工具函数（create_smart_trip）
        ↓
AI Trip Recommender生成行程
        ↓
返回完整行程给用户
```

**特点**：
- 自然语言交互，降低用户操作成本
- AI自动识别意图和提取参数
- 支持必选景点、约束条件
- 适合快速创建行程

**API接口**：`POST /api/agent/chat`

---

#### 方法二：传统评分算法规划（手动表单模式）

**触发方式**：用户通过表单手动选择目的地、日期、预算、偏好等

**技术实现**：
- `traditionalRecommender.ts` - 传统推荐服务
- `scoringEngine.ts` - 多因素评分引擎
- `clusteringService.ts` - K-means地理聚类
- `diversityService.ts` - 多样性约束
- `routeOptimizer.ts` - 2-opt路径优化
- `iotCheckService.ts` - IoT实时检查

**工作流程**：
```
用户填写表单（目的地、日期、预算、偏好等）
        ↓
Traditional Recommender处理请求
        ↓
多因素评分引擎计算景点评分
        ↓
K-means地理聚类分组景点
        ↓
多样性约束选择景点
        ↓
2-opt路径优化
        ↓
IoT实时检查和景点替换
        ↓
动态预算计算
        ↓
生成完整行程
```

**特点**：
- 精确控制规划参数
- 自主研发的评分算法，不依赖外部AI
- 综合考虑偏好、质量、IoT、人群因素
- 适合需要精细控制的规划场景

**API接口**：`POST /api/plan`

---

### 3.3 功能列表

| 功能名称 | 描述 | 控制器 | 规划方法 |
|---------|------|--------|---------|
| 创建行程规划（传统） | 基于表单参数生成行程 | planController.ts | 传统评分算法 |
| AI Agent对话规划 | 自然语言创建行程 | agentController.ts | AI Agent |
| 调整行程 | 根据IoT数据调整行程 | planController.ts | - |
| 获取行程详情 | 获取已规划的行程详情 | planController.ts | - |
| AI顾问对话 | 获取旅行建议 | advisorController.ts | - |
| 获取会话列表 | 获取AI对话会话 | advisorController.ts | - |
| 获取会话消息 | 获取会话历史消息 | advisorController.ts | - |
| 删除会话 | 删除AI对话会话 | advisorController.ts | - |

### 3.3 API接口详情

#### POST /api/plan
**功能**：创建行程规划

**请求参数**：
```json
{
  "destination": "string (必填，目的地城市)",
  "start_date": "string (必填，开始日期 YYYY-MM-DD)",
  "end_date": "string (必填，结束日期 YYYY-MM-DD)",
  "origin": "string (可选，出发地)",
  "budget": "number (可选，预算)",
  "preferences": {
    "pace": "string (可选，节奏：relaxed/moderate/fast)",
    "energy_level": "string (可选，体力：low/medium/high)",
    "categories": ["string"] (可选，兴趣类别)
  },
  "groupSize": "number (可选，人数)",
  "groupType": "string (可选，群体类型：solo/couple/family/friends)",
  "hasChildren": "boolean (可选，是否有儿童)",
  "hasElderly": "boolean (可选，是否有老人)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "itinerary": {
      "itinerary": [
        {
          "day": 1,
          "date": "string",
          "attractions": [
            {
              "id": "string",
              "name": "string",
              "location": "string",
              "time": "string",
              "type": "string",
              "estimated_cost": "number"
            }
          ]
        }
      ]
    },
    "total_cost": "number",
    "total_distance": "number",
    "summary": {
      "origin": "string",
      "destination": "string",
      "days": "number",
      "budget": "number",
      "start_date": "string",
      "end_date": "string"
    }
  }
}
```

---

#### POST /api/adjust
**功能**：调整行程

**请求参数**：
```json
{
  "itinerary": "object (必填，行程数据)",
  "reason": "string (必填，调整原因：crowd/weather/closed)",
  "targetAttractionId": "string (必填，目标景点ID)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "adjustedItinerary": "object",
    "adjustments": ["object"],
    "message": "string"
  }
}
```

---

#### POST /api/agent/chat
**功能**：AI Agent对话

**请求参数**：
```json
{
  "question": "string (必填，用户问题)"
}
```

**请求头**：
```
x-user-id: string (用户ID)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "answer": "string",
    "toolCalls": [
      {
        "name": "string",
        "result": "object"
      }
    ]
  },
  "needsConfirmation": "boolean (可选)",
  "previewData": "object (可选)"
}
```

---

#### POST /api/advisor/chat
**功能**：AI顾问对话

**请求参数**：
```json
{
  "question": "string (必填，用户问题)",
  "planContext": "object (可选，行程上下文)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "answer": "string",
    "suggestions": ["string"]
  }
}
```

---

#### GET /api/advisor/sessions
**功能**：获取用户会话列表

**请求头**：
```
x-user-id: string (用户ID)
```

**请求参数**：
```
mode: string (可选，会话模式：advisor/agent)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "title": "string",
      "createdAt": "datetime",
      "updatedAt": "datetime"
    }
  ]
}
```

---

#### GET /api/advisor/sessions/:sessionId/messages
**功能**：获取会话消息历史

**请求参数**：
```
limit: number (可选，消息数量限制，默认10)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "role": "string (user/assistant)",
      "content": "string",
      "createdAt": "datetime"
    }
  ]
}
```

---

#### DELETE /api/advisor/sessions/:sessionId
**功能**：删除会话

**响应格式**：
```json
{
  "success": true,
  "message": "会话已删除"
}
```

---

## 4. 行程管理功能

### 4.1 功能概述

行程管理模块提供行程的增删改查、酒店餐厅选择、预算计算等功能。

### 4.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取用户行程 | 获取用户所有行程列表 | tripController.ts |
| 保存行程 | 保存规划好的行程 | tripController.ts |
| 获取行程详情 | 获取单个行程详细信息 | tripController.ts |
| 删除行程 | 删除指定行程 | tripController.ts |
| 更新酒店信息 | 更新行程的酒店选择 | tripController.ts |
| 更新餐厅信息 | 更新某天的餐厅选择 | tripController.ts |
| 完成行程 | 标记行程为已完成 | tripController.ts |
| 计算实时预算 | 计算当前选择的预算 | tripController.ts |

### 4.3 API接口详情

#### GET /api/trips
**功能**：获取用户所有行程

**请求头**：
```
Authorization: Bearer <token>
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "title": "string",
      "description": "string",
      "destination": "string",
      "startDate": "datetime",
      "endDate": "datetime",
      "totalBudget": "number",
      "actualBudget": "number",
      "status": "string",
      "days": [
        {
          "dayNumber": "number",
          "date": "datetime",
          "itineraryItems": ["object"]
        }
      ]
    }
  ]
}
```

---

#### POST /api/trips
**功能**：保存行程

**请求头**：
```
Authorization: Bearer <token>
```

**请求参数**：
```json
{
  "summary": {
    "destination": "string",
    "start_date": "string",
    "end_date": "string",
    "budget": "number"
  },
  "itinerary": {
    "itinerary": ["object"]
  },
  "total_cost": "number",
  "budget_breakdown": {
    "transportation": "number",
    "accommodation": "number",
    "dining": "number",
    "tickets": "number"
  },
  "hotel": "object (可选)",
  "hotelRecommendations": ["object"] (可选),
  "restaurantRecommendations": ["object"] (可选),
  "restaurants": ["object"] (可选),
  "customization": {
    "tripName": "string (可选)",
    "tripDescription": "string (可选)",
    "coverImage": "string (可选)"
  }
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "行程保存成功",
  "data": {
    "tripId": "string",
    "budgetInfo": {
      "actualBudget": "number",
      "budgetStatus": "string",
      "breakdown": {
        "accommodation": "number",
        "dining": "number",
        "transportation": "number",
        "tickets": "number"
      }
    }
  }
}
```

---

#### GET /api/trips/:id
**功能**：获取行程详情

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "title": "string",
    "description": "string",
    "destination": "string",
    "startDate": "datetime",
    "endDate": "datetime",
    "totalBudget": "number",
    "actualBudget": "number",
    "status": "string",
    "days": ["object"],
    "budget": "object"
  }
}
```

---

#### DELETE /api/trips/:id
**功能**：删除行程

**响应格式**：
```json
{
  "success": true,
  "message": "行程删除成功"
}
```

---

#### PUT /api/trips/:id/hotel
**功能**：更新酒店信息

**请求参数**：
```json
{
  "name": "string",
  "address": "string",
  "location": "string",
  "tel": "string",
  "type": "string",
  "rating": "number"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "酒店信息更新成功",
  "data": "object (更新后的行程)"
}
```

---

#### PUT /api/trips/:tripId/days/:dayNumber/restaurant
**功能**：更新餐厅信息

**请求参数**：
```json
{
  "name": "string",
  "address": "string",
  "location": "string",
  "tel": "string",
  "type": "string",
  "rating": "number"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "餐厅信息更新成功",
  "data": "object (更新后的天数记录)"
}
```

---

#### PUT /api/trips/:tripId/complete
**功能**：完成行程

**响应格式**：
```json
{
  "success": true,
  "message": "行程已完成",
  "data": {
    "id": "string",
    "status": "completed",
    "completedAt": "datetime"
  }
}
```

---

#### POST /api/trips/calculate-budget
**功能**：计算实时预算

**请求参数**：
```json
{
  "totalBudget": "number (必填)",
  "days": "number (必填)",
  "groupSize": "number (可选)",
  "hotel": "object (可选)",
  "restaurants": "object (可选)",
  "spots": ["object"] (可选)
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "total": "number",
    "status": "string",
    "breakdown": "object",
    "warningLevel": "string",
    "warningMessage": "string"
  }
}
```

---

## 5. 景点数据功能

### 5.1 功能概述

景点数据模块提供热门景点、热门城市、景点同步等功能。

### 5.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取热门景点 | 获取所有或指定城市的热门景点 | hotSpotController.ts |
| 获取热门城市 | 获取热门城市列表 | hotSpotController.ts |
| 获取热门城市（含景点） | 获取热门城市及其景点信息 | hotSpotController.ts |
| 获取城市列表 | 获取热门城市列表（目的地） | destinationController.ts |
| 获取城市景点 | 获取指定城市的热门景点 | destinationController.ts |
| 获取城市所有景点 | 获取城市所有热门景点（分页） | destinationController.ts |
| 同步景点 | 同步景点到数据库 | spotSyncController.ts |
| 批量同步景点 | 批量同步景点数据 | spotSyncController.ts |

### 5.3 API接口详情

#### GET /api/hot-spots
**功能**：获取热门景点

**请求参数**：
```
city: string (可选，城市名称)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "name": "string",
      "image": "string",
      "rating": "number",
      "description": "string",
      "openTime": "string",
      "ticketPrice": "number",
      "category": "string",
      "city": "string",
      "location": "string",
      "address": "string"
    }
  ]
}
```

---

#### GET /api/hot-cities
**功能**：获取热门城市列表

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "name": "string",
      "count": "number (景点数量)"
    }
  ]
}
```

---

#### GET /api/hot-spots/cities
**功能**：获取热门城市（含景点信息）

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "city": "string",
      "count": "number",
      "avgRating": "number",
      "spots": ["object"]
    }
  ]
}
```

---

#### GET /api/destinations/cities
**功能**：获取热门城市列表

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "name": "string",
      "coverImage": "string",
      "description": "string",
      "spotCount": "number",
      "avgRating": "number",
      "categories": ["string"]
    }
  ]
}
```

---

#### GET /api/destinations/cities/:city/spots
**功能**：获取城市热门景点

**请求参数**：
```
limit: number (可选，数量限制，默认9)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "name": "string",
      "image": "string",
      "rating": "number",
      "description": "string",
      "openTime": "string",
      "ticketPrice": "number",
      "category": "string",
      "city": "string",
      "location": "string",
      "address": "string"
    }
  ]
}
```

---

#### GET /api/destinations/cities/:city/all
**功能**：获取城市所有热门景点（分页）

**请求参数**：
```
page: number (可选，页码，默认1)
pageSize: number (可选，每页数量，默认12)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "spots": ["object"],
    "total": "number",
    "page": "number",
    "pageSize": "number",
    "totalPages": "number"
  }
}
```

---

#### POST /api/spots/sync
**功能**：同步景点到数据库

**请求参数**：
```json
{
  "name": "string (必填)",
  "city": "string (必填)",
  "category": "string (可选)",
  "ticketPrice": "number (可选)",
  "openTime": "string (可选)",
  "rating": "number (可选)",
  "description": "string (可选)",
  "isOutdoor": "boolean (可选)",
  "location": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": "object (景点数据)",
  "message": "string"
}
```

---

#### POST /api/spots/sync/batch
**功能**：批量同步景点

**请求参数**：
```json
{
  "spots": [
    {
      "name": "string",
      "city": "string",
      ...
    }
  ]
}
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "success": "boolean",
      "spot": "object",
      "message": "string"
    }
  ],
  "count": "number"
}
```

---

## 6. 收藏功能

### 6.1 功能概述

收藏模块提供景点收藏、取消收藏、收藏列表查询等功能。

### 6.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取收藏列表 | 获取用户收藏的景点列表 | favoriteController.ts |
| 添加收藏 | 收藏指定景点 | favoriteController.ts |
| 取消收藏 | 取消收藏指定景点 | favoriteController.ts |
| 检查收藏状态 | 检查是否已收藏某景点 | favoriteController.ts |
| 获取收藏数量 | 获取用户收藏总数 | favoriteController.ts |

### 6.3 API接口详情

#### GET /api/favorites
**功能**：获取收藏列表

**请求头**：
```
x-user-id: string (用户ID)
```

**请求参数**：
```
includeIoT: boolean (可选，是否包含IoT数据)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "spotId": "string",
      "spot": {
        "id": "string",
        "name": "string",
        "city": "string",
        "rating": "number",
        ...
      },
      "notes": "string",
      "createdAt": "datetime"
    }
  ],
  "count": "number"
}
```

---

#### POST /api/favorites
**功能**：添加收藏

**请求参数**：
```json
{
  "spotId": "string (必填)",
  "notes": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": "object (收藏记录)",
  "message": "收藏成功"
}
```

---

#### DELETE /api/favorites/:spotId
**功能**：取消收藏

**响应格式**：
```json
{
  "success": true,
  "message": "取消收藏成功"
}
```

---

#### GET /api/favorites/check/:spotId
**功能**：检查收藏状态

**响应格式**：
```json
{
  "success": true,
  "data": {
    "isFavorited": "boolean",
    "spotId": "string"
  }
}
```

---

#### GET /api/favorites/count
**功能**：获取收藏数量

**响应格式**：
```json
{
  "success": true,
  "data": {
    "count": "number"
  }
}
```

---

## 7. 评价功能

### 7.1 功能概述

评价模块提供景点评价、评价管理、点赞等功能。

### 7.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 创建评价 | 对景点发表评价 | reviewController.ts |
| 获取景点评价 | 获取指定景点的所有评价 | reviewController.ts |
| 获取用户评价 | 获取用户发表的所有评价 | reviewController.ts |
| 删除评价 | 删除指定评价 | reviewController.ts |
| 点赞评价 | 点赞/取消点赞评价 | reviewController.ts |
| 批量获取评价统计 | 批量获取多个景点的评价统计 | reviewController.ts |

### 7.3 API接口详情

#### POST /api/reviews
**功能**：创建评价

**请求参数**：
```json
{
  "spotId": "string (必填)",
  "userId": "string (必填)",
  "rating": "number (必填，1-5)",
  "comment": "string (可选)",
  "images": ["string"] (可选)
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "评价创建成功",
  "data": "object (评价数据)"
}
```

---

#### GET /api/reviews/spot/:spotId
**功能**：获取景点评价

**请求参数**：
```
page: number (可选，页码，默认1)
pageSize: number (可选，每页数量，默认10)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "reviews": [
      {
        "id": "string",
        "rating": "number",
        "comment": "string",
        "images": ["string"],
        "user": {
          "id": "string",
          "username": "string",
          "avatar": "string"
        },
        "likeCount": "number",
        "createdAt": "datetime"
      }
    ],
    "total": "number",
    "page": "number",
    "pageSize": "number"
  }
}
```

---

#### GET /api/reviews/user/:userId
**功能**：获取用户评价

**请求参数**：
```
page: number (可选，页码，默认1)
pageSize: number (可选，每页数量，默认10)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "reviews": ["object"],
    "total": "number",
    "page": "number",
    "pageSize": "number"
  }
}
```

---

#### DELETE /api/reviews/:reviewId
**功能**：删除评价

**请求参数**：
```json
{
  "userId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "评价删除成功"
}
```

---

#### POST /api/reviews/:reviewId/like
**功能**：点赞评价

**请求参数**：
```json
{
  "userId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "string",
  "data": {
    "liked": "boolean",
    "likeCount": "number"
  }
}
```

---

#### POST /api/reviews/stats
**功能**：批量获取评价统计

**请求参数**：
```json
{
  "spotIds": ["string"] (必填)
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "spotId1": {
      "totalReviews": "number",
      "avgRating": "number",
      "ratingDistribution": {
        "5": "number",
        "4": "number",
        "3": "number",
        "2": "number",
        "1": "number"
      }
    }
  }
}
```

---

## 8. 博客功能

### 8.1 功能概述

博客模块提供游记撰写、发布、评论、点赞等功能。

### 8.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 创建博客 | 创建游记文章 | blogController.ts |
| 获取博客列表 | 获取游记列表 | blogController.ts |
| 获取博客详情 | 获取单篇游记详情 | blogController.ts |
| 更新博客 | 更新游记内容 | blogController.ts |
| 删除博客 | 删除游记 | blogController.ts |
| 增加浏览量 | 增加博客浏览次数 | blogController.ts |
| 点赞博客 | 点赞/取消点赞博客 | blogController.ts |
| 添加评论 | 对博客发表评论 | blogController.ts |
| 删除评论 | 删除评论 | blogController.ts |
| 点赞评论 | 点赞/取消点赞评论 | blogController.ts |
| 获取热门标签 | 获取热门标签列表 | blogController.ts |

### 8.3 API接口详情

#### POST /api/blogs
**功能**：创建博客

**请求参数**：
```json
{
  "userId": "string (必填)",
  "title": "string (必填)",
  "content": "string (必填)",
  "coverImage": "string (可选)",
  "tags": ["string"] (可选),
  "city": "string (可选)",
  "spotIds": ["string"] (可选),
  "isPublished": "boolean (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "博客文章创建成功",
  "data": "object (博客数据)"
}
```

---

#### GET /api/blogs
**功能**：获取博客列表

**请求参数**：
```
userId: string (可选)
city: string (可选)
tags: string | string[] (可选)
isPublished: string (可选)
page: number (可选，默认1)
pageSize: number (可选，默认10)
sortBy: string (可选，排序方式：latest/popular)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "posts": [
      {
        "id": "string",
        "title": "string",
        "content": "string",
        "coverImage": "string",
        "tags": ["string"],
        "city": "string",
        "viewCount": "number",
        "likeCount": "number",
        "commentCount": "number",
        "author": {
          "id": "string",
          "username": "string",
          "avatar": "string"
        },
        "createdAt": "datetime"
      }
    ],
    "total": "number",
    "page": "number",
    "pageSize": "number"
  }
}
```

---

#### GET /api/blogs/:id
**功能**：获取博客详情

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "title": "string",
    "content": "string",
    "coverImage": "string",
    "tags": ["string"],
    "city": "string",
    "viewCount": "number",
    "likeCount": "number",
    "commentCount": "number",
    "author": "object",
    "spots": ["object"],
    "comments": ["object"],
    "createdAt": "datetime"
  }
}
```

---

#### PUT /api/blogs/:id
**功能**：更新博客

**请求参数**：
```json
{
  "userId": "string (必填)",
  "title": "string (可选)",
  "content": "string (可选)",
  "coverImage": "string (可选)",
  "tags": ["string"] (可选),
  "city": "string (可选)",
  "spotIds": ["string"] (可选),
  "isPublished": "boolean (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "博客文章更新成功",
  "data": "object (更新后的博客)"
}
```

---

#### DELETE /api/blogs/:id
**功能**：删除博客

**请求参数**：
```json
{
  "userId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "博客文章删除成功"
}
```

---

#### POST /api/blogs/:id/view
**功能**：增加浏览量

**响应格式**：
```json
{
  "success": true,
  "message": "浏览量增加成功"
}
```

---

#### POST /api/blogs/:postId/like
**功能**：点赞博客

**请求参数**：
```json
{
  "userId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "string",
  "data": {
    "liked": "boolean",
    "likeCount": "number"
  }
}
```

---

#### POST /api/blogs/:postId/comments
**功能**：添加评论

**请求参数**：
```json
{
  "userId": "string (必填)",
  "content": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "评论添加成功",
  "data": "object (评论数据)"
}
```

---

#### DELETE /api/blogs/comments/:commentId
**功能**：删除评论

**请求参数**：
```json
{
  "userId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "评论删除成功"
}
```

---

#### GET /api/blogs/tags/popular
**功能**：获取热门标签

**请求参数**：
```
limit: number (可选，默认20)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "name": "string",
      "count": "number"
    }
  ]
}
```

---

## 9. 协同规划功能

### 9.1 功能概述

协同规划模块提供多人实时协同编辑行程的功能，支持实时消息沟通和草稿提交。

### 9.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 创建协同房间 | 创建协同规划房间 | collabController.ts |
| 加入协同房间 | 通过邀请链接加入房间 | collabController.ts |
| 获取房间信息 | 获取房间详细信息 | collabController.ts |
| 获取景点统计 | 获取景点投票统计 | collabController.ts |
| 锁定房间 | 锁定房间禁止编辑 | collabController.ts |
| 创建/更新草案 | 创建或更新个人草案 | collabController.ts |
| 提交草案 | 提交草案供审核 | collabController.ts |
| 获取用户草案 | 获取用户的草案列表 | collabController.ts |
| 获取所有草案 | 获取所有成员的草案 | collabController.ts |
| 发送消息 | 发送房间消息 | collabController.ts |
| 获取消息列表 | 获取房间消息历史 | collabController.ts |
| 保存最终行程 | 保存协同完成的行程 | collabController.ts |

### 9.3 API接口详情

#### POST /api/collab/rooms
**功能**：创建协同房间

**请求参数**：
```json
{
  "tripId": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "roomId": "string",
    "inviteToken": "string",
    "inviteUrl": "string",
    "hostId": "string",
    "expiresAt": "datetime"
  }
}
```

---

#### POST /api/collab/rooms/join
**功能**：加入协同房间

**请求参数**：
```json
{
  "token": "string (必填，邀请token)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "tripId": "string",
    "hostId": "string",
    "members": ["object"],
    "status": "string"
  }
}
```

---

#### GET /api/collab/rooms/:roomId
**功能**：获取房间信息

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "tripId": "string",
    "hostId": "string",
    "members": [
      {
        "userId": "string",
        "role": "string",
        "user": {
          "id": "string",
          "username": "string",
          "avatar": "string"
        }
      }
    ],
    "status": "string",
    "isLocked": "boolean"
  }
}
```

---

#### GET /api/collab/rooms/:roomId/stats
**功能**：获取景点统计

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "spotId": "string",
      "spotName": "string",
      "votes": "number",
      "voters": ["string"]
    }
  ]
}
```

---

#### POST /api/collab/rooms/:roomId/lock
**功能**：锁定房间

**响应格式**：
```json
{
  "success": true,
  "data": "object (更新后的房间)"
}
```

---

#### POST /api/collab/drafts
**功能**：创建/更新草案

**请求参数**：
```json
{
  "roomId": "string (必填)",
  "dayNumber": "number (必填)",
  "spotSequence": ["string"] (必填，景点ID序列),
  "polylineData": "object (必填，路线数据)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": "object (草案数据)"
}
```

---

#### POST /api/collab/drafts/:draftId/submit
**功能**：提交草案

**响应格式**：
```json
{
  "success": true,
  "data": "object (草案数据)"
}
```

---

#### GET /api/collab/rooms/:roomId/drafts
**功能**：获取用户草案

**响应格式**：
```json
{
  "success": true,
  "data": ["object (草案数据)"]
}
```

---

#### GET /api/collab/rooms/:roomId/drafts/all
**功能**：获取所有草案

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "userId": "string",
      "username": "string",
      "drafts": ["object"]
    }
  ]
}
```

---

#### POST /api/collab/messages
**功能**：发送消息

**请求参数**：
```json
{
  "roomId": "string (必填)",
  "content": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "userId": "string",
    "content": "string",
    "createdAt": "datetime"
  }
}
```

---

#### GET /api/collab/messages/:roomId
**功能**：获取消息列表

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "userId": "string",
      "user": {
        "username": "string",
        "avatar": "string"
      },
      "content": "string",
      "createdAt": "datetime"
    }
  ]
}
```

---

#### POST /api/collab/finalize
**功能**：保存最终行程

**请求参数**：
```json
{
  "roomId": "string (必填)",
  "finalRoute": ["object"] (必填，最终路线)
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "tripId": "string",
    "message": "string"
  }
}
```

---

## 10. 打包清单功能

### 10.1 功能概述

打包清单模块提供行李打包管理功能，支持预设物品和自定义添加。

### 10.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取打包清单 | 获取行程的打包清单 | packingController.ts |
| 初始化打包清单 | 初始化默认打包物品 | packingController.ts |
| 添加打包物品 | 添加自定义打包物品 | packingController.ts |
| 批量保存清单 | 批量保存打包清单 | packingController.ts |
| 更新物品状态 | 更新打包状态 | packingController.ts |
| 删除打包物品 | 删除打包物品 | packingController.ts |
| 获取所有分类 | 获取物品分类列表 | packingController.ts |
| 获取打包进度 | 获取打包完成进度 | packingController.ts |

### 10.3 API接口详情

#### GET /api/trips/:tripId/packing
**功能**：获取打包清单

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "itemName": "string",
      "category": "string",
      "isPacked": "boolean",
      "createdAt": "datetime"
    }
  ]
}
```

---

#### POST /api/trips/:tripId/packing/initialize
**功能**：初始化打包清单

**响应格式**：
```json
{
  "success": true,
  "data": ["object (物品数据)"]
}
```

---

#### POST /api/trips/:tripId/packing
**功能**：添加打包物品

**请求参数**：
```json
{
  "itemName": "string (必填)",
  "category": "string (必填)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": "object (物品数据)"
}
```

---

#### POST /api/trips/:tripId/packing/batch
**功能**：批量保存清单

**请求参数**：
```json
{
  "items": [
    {
      "itemName": "string",
      "category": "string",
      "isPacked": "boolean"
    }
  ]
}
```

**响应格式**：
```json
{
  "success": true,
  "data": ["object (物品数据)"]
}
```

---

#### PATCH /api/packing/:itemId
**功能**：更新物品状态

**请求参数**：
```json
{
  "isPacked": "boolean (可选)",
  "itemName": "string (可选)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": "object (更新后的物品)"
}
```

---

#### DELETE /api/packing/:itemId
**功能**：删除打包物品

**响应格式**：
```json
{
  "success": true,
  "message": "string"
}
```

---

#### GET /api/packing/categories
**功能**：获取所有分类

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "name": "string",
      "icon": "string",
      "items": ["string"]
    }
  ]
}
```

---

#### GET /api/trips/:tripId/packing/progress
**功能**：获取打包进度

**响应格式**：
```json
{
  "success": true,
  "data": {
    "total": "number",
    "packed": "number",
    "percentage": "number"
  }
}
```

---

## 11. 分享功能

### 11.1 功能概述

分享模块提供行程分享、公开行程获取、行程复刻等功能。

### 11.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 分享行程 | 生成行程分享链接 | shareController.ts |
| 获取公开行程 | 通过分享token获取行程 | shareController.ts |
| 复刻行程 | 复刻他人的公开行程 | shareController.ts |

### 11.3 API接口详情

#### POST /api/trips/:id/share
**功能**：分享行程

**响应格式**：
```json
{
  "success": true,
  "data": {
    "shareToken": "string",
    "shareUrl": "string",
    "expiresAt": "datetime"
  }
}
```

---

#### GET /api/trips/shared/:token
**功能**：获取公开行程

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "title": "string",
    "description": "string",
    "destination": "string",
    "startDate": "datetime",
    "endDate": "datetime",
    "days": ["object"],
    "author": {
      "username": "string",
      "avatar": "string"
    }
  }
}
```

---

#### POST /api/trips/shared/:token/clone
**功能**：复刻行程

**响应格式**：
```json
{
  "success": true,
  "data": {
    "tripId": "string",
    "message": "string"
  }
}
```

---

## 12. 推荐功能

### 12.1 功能概述

推荐模块提供酒店推荐和餐厅推荐功能。

### 12.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取酒店推荐 | 根据景点位置推荐酒店 | recommendationController.ts |
| 获取餐厅推荐 | 根据每天景点推荐餐厅 | recommendationController.ts |

### 12.3 API接口详情

#### POST /api/recommendations/hotels
**功能**：获取酒店推荐

**请求参数**：
```json
{
  "spots": [
    {
      "name": "string (必填)",
      "location": "string (必填)"
    }
  ],
  "budget": "number (必填)",
  "tripId": "string (可选，用于缓存)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "name": "string",
      "address": "string",
      "location": "string",
      "tel": "string",
      "type": "string",
      "rating": "number",
      "price": "number",
      "distance": "number"
    }
  ],
  "count": "number",
  "fromCache": "boolean"
}
```

---

#### POST /api/recommendations/restaurants
**功能**：获取餐厅推荐

**请求参数**：
```json
{
  "days": [
    {
      "day": "number (必填)",
      "date": "string (可选)",
      "spots": [
        {
          "name": "string (必填)",
          "location": "string (必填)"
        }
      ]
    }
  ],
  "tripId": "string (可选，用于缓存)"
}
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "day": "number",
      "date": "string",
      "centerSpot": "string",
      "centerLocation": "string",
      "restaurants": [
        {
          "id": "string",
          "name": "string",
          "address": "string",
          "location": "string",
          "tel": "string",
          "type": "string",
          "rating": "number",
          "price": "number"
        }
      ]
    }
  ],
  "count": "number",
  "fromCache": "boolean"
}
```

---

## 13. IoT数据功能

### 13.1 功能概述

IoT数据模块提供景点实时数据，包括拥挤度、天气状况等。

### 13.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取所有IoT数据 | 获取所有景点的实时数据 | iotController.ts |
| 获取景点IoT数据 | 获取指定景点的实时数据 | iotController.ts |

### 13.3 API接口详情

#### GET /api/iot/data
**功能**：获取所有IoT数据

**响应格式**：
```json
{
  "success": true,
  "data": {
    "timestamp": "number",
    "spots": [
      {
        "id": "string",
        "name": "string",
        "crowdLevel": "number (0-100)",
        "temperature": "number",
        "humidity": "number",
        "rainProbability": "number",
        "weatherDescription": "string",
        "weatherIcon": "string",
        "isOpen": "boolean"
      }
    ]
  }
}
```

---

#### GET /api/iot/spot/:id
**功能**：获取景点IoT数据

**响应格式**：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "name": "string",
    "crowdLevel": "number",
    "temperature": "number",
    "humidity": "number",
    "rainProbability": "number",
    "weatherDescription": "string",
    "weatherIcon": "string",
    "isOpen": "boolean"
  }
}
```

---

## 14. 通知功能

### 14.1 功能概述

通知模块提供环境感知推送、站内通知管理等功能，支持WebSocket实时推送和持久化存储。

### 14.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取通知列表 | 获取用户的所有通知 | notificationController.ts |
| 标记通知已读 | 标记单个通知为已读 | notificationController.ts |
| 标记所有已读 | 标记所有通知为已读 | notificationController.ts |
| 删除通知 | 删除单个通知 | notificationController.ts |
| 清空所有通知 | 清空用户所有通知 | notificationController.ts |
| 手动触发感知 | 手动触发环境感知 | notificationController.ts |

### 14.3 环境感知类型

系统自动监控以下环境因素并推送通知：

| 感知类型 | 触发条件 | 级别 | 建议 |
|---------|---------|------|------|
| 降雨感知 | 降雨概率≥50% | WARNING | 准备雨具，注意防滑 |
| 降雨感知 | 降雨概率≥80% | DANGER | 调整行程或携带雨具 |
| 人流感知 | 拥挤度≥60% | WARNING | 可能需要排队 |
| 人流感知 | 拥挤度≥90% | DANGER | 建议避开或选择其他时间 |
| 温度感知 | 温度≤5°C或≥35°C | WARNING | 做好保暖或防暑 |
| 温度感知 | 温度≤0°C或≥40°C | DANGER | 极端天气，注意安全 |
| 关闭感知 | 景点已关闭 | DANGER | 调整行程 |

### 14.4 定时任务

系统自动执行以下定时任务：

- **每15分钟**：更新所有景点的IoT数据（天气+人流）
- **每15分钟**：执行用户行程环境感知（未来24小时内的行程）
- **每30分钟**：执行全局环境感知（所有热门景点）

### 14.5 API接口详情

#### GET /api/notifications
**功能**：获取通知列表

**请求参数**：
```
page: number (可选，默认1)
limit: number (可选，默认20)
unreadOnly: boolean (可选，默认false)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "notifications": [
      {
        "id": "string",
        "type": "string (sensor/system)",
        "title": "string",
        "content": "string",
        "isRead": "boolean",
        "createdAt": "datetime"
      }
    ],
    "total": "number",
    "unreadCount": "number"
  }
}
```

---

#### PUT /api/notifications/:id/read
**功能**：标记通知已读

**响应格式**：
```json
{
  "success": true,
  "message": "通知已标记为已读"
}
```

---

#### PUT /api/notifications/read-all
**功能**：标记所有通知已读

**响应格式**：
```json
{
  "success": true,
  "message": "所有通知已标记为已读"
}
```

---

#### DELETE /api/notifications/:id
**功能**：删除单个通知

**响应格式**：
```json
{
  "success": true,
  "message": "通知已删除"
}
```

---

#### DELETE /api/notifications/clear-all
**功能**：清空所有通知

**响应格式**：
```json
{
  "success": true,
  "message": "所有通知已清空"
}
```

---

#### POST /api/notifications/sensor/run
**功能**：手动触发环境感知

**请求参数**：
```json
{
  "spotIds": ["string"] (可选，指定景点ID数组)
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "环境感知已触发"
}
```

---

## 15. 图片管理功能

### 15.1 功能概述

图片管理模块提供图片上传、获取、批量查询等功能。

### 15.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 上传图片 | 上传景点图片 | imageController.ts |
| 上传博客图片 | 上传博客内容图片 | imageController.ts |
| 获取景点图片 | 获取景点的所有图片 | imageController.ts |
| 获取景点封面 | 获取景点封面图片 | imageController.ts |
| 批量获取图片 | 批量获取景点图片 | imageController.ts |
| 批量获取图片（按ID） | 根据景点ID批量获取图片 | imageController.ts |
| 上传到Cloudinary | 上传图片到Cloudinary | uploadController.ts |
| 删除Cloudinary图片 | 删除Cloudinary上的图片 | uploadController.ts |

### 15.3 API接口详情

#### POST /api/images/upload
**功能**：上传图片

**请求头**：
```
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

**请求参数**：
```
spotId: string (必填)
file: 图片文件 (必填)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "imageId": "string",
    "cloudinaryUrl": "string",
    "status": "string (approved/pending)"
  },
  "message": "图片上传成功"
}
```

---

#### POST /api/images/blog-upload
**功能**：上传博客图片

**请求头**：
```
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

**请求参数**：
```
file: 图片文件 (必填)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "url": "string",
    "cloudinaryId": "string"
  },
  "message": "博客图片上传成功"
}
```

---

#### GET /api/images/spot/:spotId
**功能**：获取景点图片

**响应格式**：
```json
{
  "success": true,
  "data": {
    "spotId": "string",
    "images": [
      {
        "id": "string",
        "url": "string",
        "source": "string",
        "status": "string",
        "isPrimary": "boolean",
        "createdAt": "datetime"
      }
    ],
    "total": "number",
    "page": "number",
    "limit": "number",
    "count": "number"
  }
}
```

---

#### GET /api/images/spot/:spotName/cover
**功能**：获取景点封面

**请求参数**：
```
city: string (可选)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "spotName": "string",
    "city": "string",
    "imageUrl": "string",
    "source": "string"
  }
}
```

---

#### POST /api/images/batch
**功能**：批量获取图片

**请求参数**：
```json
{
  "spots": [
    {
      "name": "string",
      "city": "string"
    }
  ]
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "images": {
      "spotName": "string (图片URL)"
    },
    "count": "number"
  }
}
```

---

#### POST /api/images/batch-by-ids
**功能**：批量获取图片（按ID）

**请求参数**：
```json
{
  "spotIds": ["string"]
}
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "images": {
      "spotId": "string (图片URL)"
    },
    "count": "number"
  }
}
```

---

#### POST /api/upload/image
**功能**：上传到Cloudinary

**请求头**：
```
Content-Type: multipart/form-data
```

**请求参数**：
```
file: 图片文件 (必填，支持JPG/PNG/GIF/WebP，最大10MB)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "url": "string",
    "publicId": "string",
    "width": "number",
    "height": "number"
  }
}
```

---

#### DELETE /api/upload/image/:publicId
**功能**：删除Cloudinary图片

**响应格式**：
```json
{
  "success": true,
  "message": "图片删除成功"
}
```

---

## 16. 地点搜索功能

### 16.1 功能概述

地点搜索模块提供地点搜索、热门地点、缓存管理等功能。

### 16.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 搜索地点 | 搜索地点（带缓存） | locationCacheController.ts |
| 获取热门地点 | 获取热门搜索地点 | locationCacheController.ts |
| 清空缓存 | 清空地点搜索缓存 | locationCacheController.ts |

### 16.3 API接口详情

#### GET /api/location/search
**功能**：搜索地点

**请求参数**：
```
keywords: string (必填，搜索关键词)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "name": "string",
      "address": "string",
      "location": "string",
      "type": "string"
    }
  ]
}
```

---

#### GET /api/location/popular
**功能**：获取热门地点

**请求参数**：
```
limit: number (可选，默认10)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "keyword": "string",
      "count": "number"
    }
  ]
}
```

---

#### DELETE /api/location/cache
**功能**：清空缓存

**响应格式**：
```json
{
  "success": true,
  "message": "string"
}
```

---

## 17. AI助手功能

### 17.1 功能概述

AI助手模块提供**两种AI功能模式**：
1. **传统对话功能**（旅行顾问模式）：纯对话交互，回答旅行相关问题
2. **Agent功能**（智能助手模式）：支持Function Calling，可执行实际操作（创建行程、查询景点、生成游记等）

### 17.2 AI两种功能模式

#### 功能一：传统对话功能（旅行顾问模式）

**触发方式**：用户在AI助手面板中选择"问答助手"模式

**技术实现**：
- `advisorService.ts` - AI顾问服务（智谱AI GLM-4对话）
- `advisorController.ts` - AI顾问控制器

**功能范围**：
- 回答旅行相关问题
- 提供旅行建议
- 推荐景点和美食
- 解答当地文化、注意事项
- 提供行程优化建议

**特点**：
- 纯对话交互，不执行实际操作
- 基于智谱AI GLM-4的对话能力
- 支持上下文记忆
- 适合咨询类场景

**API接口**：`POST /api/advisor/chat`

---

#### 功能二：Agent功能（智能助手模式）

**触发方式**：用户在AI助手面板中选择"智能助手"模式

**技术实现**：
- `agentService.ts` - AI Agent服务（Function Calling）
- `agentController.ts` - AI Agent控制器

**功能范围**：
- 创建智能行程（create_smart_trip）
- 查看我的行程（get_my_trips）
- 生成游记（generate_blog）
- 查询景点信息（get_spot_info）
- 调整行程（adjust_itinerary）

**特点**：
- 支持Function Calling，可执行实际操作
- 自然语言理解 + 意图识别 + 参数提取
- 自动调用工具函数完成任务
- 适合操作类场景

**API接口**：`POST /api/agent/chat`

---

### 17.3 功能列表

| 功能名称 | 描述 | 控制器 | AI模式 |
|---------|------|--------|--------|
| AI顾问对话 | 旅行问答和建议 | advisorController.ts | 传统对话 |
| AI Agent对话 | 智能操作和任务执行 | agentController.ts | Agent |
| 获取会话列表 | 获取AI对话会话列表 | advisorController.ts | - |
| 获取会话消息 | 获取会话历史消息 | advisorController.ts | - |
| 删除会话 | 删除AI对话会话 | advisorController.ts | - |

### 17.4 模式切换

前端组件支持在"问答助手"和"智能助手"之间切换：
- **问答助手**：调用 `POST /api/advisor/chat`
- **智能助手**：调用 `POST /api/agent/chat`

### 17.5 API接口详情

（详见第3节行程规划功能中的AI相关接口）

---

## 18. 管理员功能

### 18.1 功能概述

管理员模块提供后台管理功能，包括仪表板统计、图片审核、景点管理等。

### 18.2 功能列表

| 功能名称 | 描述 | 控制器 |
|---------|------|--------|
| 获取仪表板统计 | 获取管理仪表板数据 | adminController.ts |
| 获取景点配图状态 | 获取景点图片配置状态 | adminController.ts |
| 上传景点图片 | 管理员上传景点图片 | adminController.ts |
| 获取用户行程景点 | 获取用户行程中的景点 | adminController.ts |
| 设置主图 | 设置景点主图 | adminController.ts |
| 获取景点列表 | 获取所有景点列表 | adminController.ts |
| 获取景点图片列表 | 获取景点所有图片 | adminController.ts |
| 审核图片 | 审核用户上传的图片 | adminController.ts |
| 删除图片 | 删除景点图片 | adminController.ts |
| 获取待审核图片 | 获取待审核图片列表 | adminController.ts |

### 18.3 API接口详情

#### GET /api/admin/dashboard/stats
**功能**：获取仪表板统计

**响应格式**：
```json
{
  "success": true,
  "data": {
    "totalSpots": "number",
    "hasImage": "number",
    "noImage": "number",
    "pending": "number",
    "fromUserTrip": "number"
  }
}
```

---

#### GET /api/admin/spots/image-status
**功能**：获取景点配图状态

**请求参数**：
```
status: string (可选，筛选状态)
keyword: string (可选，搜索关键词)
city: string (可选，城市筛选)
page: number (可选，页码)
limit: number (可选，每页数量)
```

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": "string",
      "name": "string",
      "city": "string",
      "rating": "number",
      "viewCount": "number",
      "imageCount": "number",
      "approvedCount": "number",
      "hasPrimary": "boolean",
      "status": "string",
      "isFromUserTrip": "boolean"
    }
  ],
  "pagination": {
    "total": "number",
    "page": "number",
    "limit": "number"
  }
}
```

---

#### POST /api/admin/spots/:spotId/images
**功能**：上传景点图片

**请求头**：
```
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

**请求参数**：
```
files: 图片文件数组 (必填)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "images": ["object"],
    "count": "number"
  },
  "message": "string"
}
```

---

#### PUT /api/admin/spots/:spotId/images/:imageId/primary
**功能**：设置主图

**响应格式**：
```json
{
  "success": true,
  "message": "主图设置成功"
}
```

---

#### GET /api/admin/spots
**功能**：获取景点列表

**请求参数**：
```
page: number (可选，页码)
pageSize: number (可选，每页数量)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "string",
        "name": "string",
        "city": "string",
        "approvedImageCount": "number",
        "pendingImageCount": "number",
        "coverImageUrl": "string"
      }
    ],
    "total": "number",
    "page": "number",
    "pageSize": "number"
  }
}
```

---

#### GET /api/admin/spots/:spotId/images
**功能**：获取景点图片列表

**响应格式**：
```json
{
  "success": true,
  "data": {
    "approved": ["object"],
    "pending": ["object"],
    "rejected": ["object"]
  }
}
```

---

#### PUT /api/admin/images/:imageId/review
**功能**：审核图片

**请求参数**：
```json
{
  "action": "string (必填，approve/reject)",
  "note": "string (拒绝时必填，原因)"
}
```

**响应格式**：
```json
{
  "success": true,
  "message": "string"
}
```

---

#### DELETE /api/admin/images/:imageId
**功能**：删除图片

**响应格式**：
```json
{
  "success": true,
  "message": "图片删除成功"
}
```

---

#### GET /api/admin/images/pending
**功能**：获取待审核图片

**请求参数**：
```
page: number (可选，页码)
pageSize: number (可选，每页数量)
```

**响应格式**：
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "string",
        "cloudinaryUrl": "string",
        "spotId": "string",
        "spotName": "string",
        "uploaderName": "string",
        "uploaderEmail": "string",
        "createdAt": "datetime"
      }
    ],
    "total": "number"
  }
}
```

---

## 附录：控制器总览

| 控制器名称 | 文件路径 | 主要功能 |
|-----------|---------|---------|
| AdminController | adminController.ts | 管理员后台管理 |
| AdvisorController | advisorController.ts | AI旅行顾问 |
| AgentController | agentController.ts | AI Agent对话 |
| AuthController | authController.ts | 用户认证 |
| BlogController | blogController.ts | 博客管理 |
| CollabController | collabController.ts | 协同规划 |
| DestinationController | destinationController.ts | 热门目的地 |
| FavoriteController | favoriteController.ts | 收藏管理 |
| HotSpotController | hotSpotController.ts | 热门景点 |
| ImageController | imageController.ts | 图片管理 |
| IoTController | iotController.ts | IoT数据 |
| LocationCacheController | locationCacheController.ts | 地点搜索缓存 |
| PackingController | packingController.ts | 打包清单 |
| PlanController | planController.ts | 行程规划 |
| RecommendationController | recommendationController.ts | 酒店餐厅推荐 |
| ReviewController | reviewController.ts | 评价管理 |
| ShareController | shareController.ts | 行程分享 |
| SpotSyncController | spotSyncController.ts | 景点同步 |
| TripController | tripController.ts | 行程管理 |
| UploadController | uploadController.ts | 图片上传 |
| UserController | userController.ts | 用户信息管理 |

---

**文档维护者**：LiveTrip 开发团队  
**最后更新**：2026-04-15
